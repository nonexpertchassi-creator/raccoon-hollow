# 그림 에셋 목록

그림을 언제·어떤 규격으로 만들지 정해두는 문서. **기획과 그림 사이의 계약서**다.

이게 없으면 기능이 바뀔 때마다 그린 그림이 버려진다. 반대로 이 목록에 있는 것만
그리면, 기능이 아무리 바뀌어도 그림은 안 버려진다.

## 원칙: 그림은 "기능 완성"을 기다리지 않는다

기다려야 하는 건 **기능**이 아니라 **안 변할 것**이다.
동물과 물건은 이름·개수가 이미 확정됐으므로 지금 그려도 안전하다.
가게 건물과 배경은 화면 구조가 아직 움직이므로 지금 그리면 버려진다.

## 1단계 — 지금 그려도 되는 것 (34장 + 점원 10장)

이만큼 들어가면 화면이 완전히 달라진다.

### 점장 너구리 5가게 × 3모습 · `72×72`

`art/clerks/`. **제일 먼저 그릴 것.** 가게마다 한 마리가 붙어서 만들고 판다.
'사장 너구리'는 뺐다 — 사장은 플레이어다.

| 파일 | 언제 | 자세 |
|---|---|---|
| `<가게>-idle.png` | 걸어갈 때 · 기본값 | **차렷.** 앞발을 내리고 편히 선 자세 |
| `<가게>-work.png` | 평소(작업대 옆) | 살짝 숙여 일하는 자세 |
| `<가게>-sell.png` | 손님이 왔을 때 | **앞발을 몸 밖으로 확실히 내밀 것** |
| `<가게>-sleep.png` | 진열대가 꽉 참 | 앉아서 졺 |

`<가게>`는 `smith` · `brush` · `paper` · `pot` · `herb`. 네 장씩 20장이다.
**대장간(`smith`) 네 장만 먼저 줘도 된다** — 나머지 가게는 지금 그림으로 버틴다.

| 가게 | 차림 |
|---|---|
| 대장간 `smith` | 무명 앞치마 · 두건 |
| 필방 `brush` | 선비 두루마기 · 먹 묻은 소매 |
| 지물포 `paper` | 팔 걷은 저고리 |
| 옹기점 `pot` | 흙 묻은 앞치마 |
| 약재상 `herb` | 약초 망태 · 수염 |

- **오른쪽을 보는 한 장씩만.** 왼쪽은 코드가 뒤집는다

#### 걷는 모습 — 그림은 0장이어도 되고, 많아야 2장이다

지금 걷는 동안 코드가 몸통을 **좌우로 ±6도 기울인다.** 위아래 들썩임과
반박자 어긋나게 둬서 두 발로 뒤뚱거리는 것처럼 보인다.

꼬리만 흔드는 것보다 이게 훨씬 세게 읽힌다 — 뒤뚱거림의 정체가 사실
**무게중심이 좌우로 넘어가는 것**이라 몸 전체가 기울어야 한다.

더 하고 싶으면 **딱 두 장이면 충분하다**(`<가게>-walk1.png` · `-walk2.png`,
왼발/오른발). 초당 4~5번 번갈아 쓰고 기울기는 그대로 얹힌다. 30px짜리
캐릭터에서 4장은 2장과 구별이 안 된다. **두 장은 반드시 같이 줄 것** —
하나만 있으면 나머지 반박자가 다른 그림으로 튄다.

#### 상하좌우는 안 만들어도 된다

점장이 오가는 두 자리는 **가로로 64px, 세로로 3px** 떨어져 있다.
사실상 좌우로만 움직인다 — 정면·뒷면은 화면에 나올 일이 없다.

손님은 길을 따라 위아래로 걷지만, 이런 그림체(위에서 살짝 내려다본
3/4 시점)에서는 **움직이는 방향과 상관없이 카메라를 보는 게 표준**이다.
뒷모습을 넣으면 그림이 두 배가 되는데 얻는 건 거의 없다.

정리: **오른쪽 보는 것 하나만.** 왼쪽은 코드가 뒤집는다.
- **연장은 그리지 말 것.** 작업대(모루)는 코드가 옆에 놓는다.
  `work`은 빠른 동작이 아니어도 된다 — 제작이 얼마나 남았는지는 좌판의
  고리가 알린다. 점장은 "일하고 있다"는 분위기만 맡는다
- `sleep`은 **한눈에 달라야 한다.** 이건 "진열대가 꽉 차서 생산이 멈췄다"는
  알림이다
- 화면에 30×30으로 그려진다. 손님과 같은 크기다 — 다 같은 마을 사람이다
- 없으면 아래 공용 너구리로 대신 나온다. **대장간 것만 먼저 줘도 안 깨진다**

#### 공용 너구리 2장 (이미 들어감)

`art/hero/raccoon-make.png` · `raccoon-sell.png`. 가게 전용 그림이 없을 때
쓰는 기본값이다. 이미 게임에 들어가 있다.

#### 실제로 넣어보고 안 것 — 연장은 손에 못 붙인다

받은 그림에 망치를 쥐여 주려고 세 번 고쳤고 세 번 다 실패했다.

1. 눈대중으로 얹었더니 손보다 7px 위에서 허공을 두드렸다
2. 알파값으로 '제일 오른쪽 픽셀'을 찾아 맞췄더니 — 그건 앞발이 아니라 **뺨**이었다
3. 눈금을 얹어 진짜 앞발(37,40)을 찾았더니 이번엔 망치가 **머리 뒤로 숨었다**

원인은 위치가 아니라 자세였다. **선 자세에 도구를 갖다 붙이면 어떻게 놔도
남의 손에 든 것처럼 보인다.** 그래서 붙이는 걸 그만두고 옆에 작업대를 놓았다.

파는 그림은 앞발을 확실히 내밀고 있어서 거기엔 물건이 붙는다(60,38)/72.
**`sell`은 앞발을 몸 밖으로 확실히 빼 달라.** 그게 물건이 붙을 수 있는
유일한 조건이다.

### 손님 12종 · `64×64`

`content.js`의 `GUESTS`. 지금은 이모지로 대신하고 있다.

| id | 이름 | 임시 이모지 | 성격 (그림에 드러나야 하는 것) |
|---|---|---|---|
| `rabbit` | 토끼 | 🐰 | 작고 날쌔다 |
| `magpie` | 까치 | 🐦 | 제일 빠르다. 값을 깎는 얄미운 얼굴 |
| `squirrel` | 다람쥐 | 🐿️ | 작다. 두 개만 들고 간다 |
| `badger` | 오소리 | 🦡 | 느긋하고 둥글다 |
| `fox` | 여우 | 🦊 | 날렵하고 약삭빠르다 |
| `deer` | 사슴 | 🦌 | 기품 있다. 값을 후하게 쳐준다 |
| `boar` | 멧돼지 | 🐗 | 우악스럽다. 한 종류를 쓸어간다 |
| `bear` | 곰 | 🐻 | 제일 크고 육중하다 |
| `turtle` | 거북 | 🐢 | 제일 느리다. 부티가 난다 |
| `crane` | 두루미 | 🦢 | 길쭉하다 |
| `ox` | 소 | 🐂 | 크고 느리다. 수레와 어울려야 한다 |
| `tiger` | 호랑이 | 🐯 | 가장 귀하고 위압적이다 |

- 정면 또는 살짝 3/4. 전신. 서 있는 자세 하나면 충분하다(걷기 애니메이션은 코드가 위아래로 흔들어 준다)
- 발끝이 이미지 아래 변에 닿아야 한다 — 코드가 발끝 높이로 앞뒤를 가린다
- 조선 마을에 어울리는 차림. **크기 차이가 곧 씀씀이 차이다** — 다람쥐는 작고 소·곰은 커야
  한눈에 읽힌다. 걷는 속도도 손님마다 다르니(까치 1.7배, 소 0.4배) 몸집으로 짐작되게
- **짐을 들려서 그리지 말 것.** 봇짐·지게·달구지는 코드가 따로 그려 붙인다.
  동물이 이미 가방을 메고 있으면 짐이 두 개가 된다. 맨몸으로만 그린다.

### 나쁜 놈 2종 · `64×64`

`content.js`의 `PESTS`. 손님과 **한눈에 구별**돼야 한다 — 누를지 말지를
0.5초 안에 정해야 하는 그림이다.

| id | 이름 | 임시 이모지 | 어떻게 다르게 |
|---|---|---|---|
| `rat` | 쥐 | 🐀 | 땅을 뛴다. 보따리를 들고 몸을 낮춰 도망치는 자세 |
| `crow` | 까마귀 | 🐦‍⬛ | 난다. **날개를 편 옆모습.** 부리에 엽전을 물었다 |

- 손님은 조선 차림에 정면·서 있는 자세다. 이 둘은 **차림이 없고 옆을 보고
  움직이는 자세**여야 한다. 그 차이 하나로 "손님이 아니다"가 읽힌다
- 까마귀는 발끝이 아니라 **몸 가운데**가 이미지 한가운데에 와야 한다.
  땅에 안 붙는 유일한 그림이다
- 코드가 뒤에 빨간 시간 고리를 그린다. 그림에 테두리 장식을 넣지 말 것

### 삽살개 1종 · `64×64`

`content.js`의 `GUARD`. 나쁜 놈을 대신 잡아주는 편이다.

| id | 이름 | 임시 이모지 |
|---|---|---|
| `dog` | 삽살개 | 🐕 |

- **앉은 자세 하나**면 된다. 개집 앞에 앉아 있고, 나쁜 놈이 나오면 코드가
  들썩이게 만든다
- 삽살개는 털이 길고 눈이 덮인 조선 개다. 그 실루엣이 살아야 다른 동물과 안 섞인다
- 옆이 아니라 **살짝 3/4 앞모습**. 손님과 같은 시점이다
- 개집(`64×64` 건물)은 **2단계로 미룬다** — 화면 구조가 아직 움직인다

### 물건 17종 · `64×112`

`content.js`의 `SHOPS[].items`. 좌판 한 칸에 세로로 들어간다.

| id | 이름 | 가게 |
|---|---|---|
| `pick` | 곡괭이 | 대장간 |
| `sickle` | 낫 | 대장간 |
| `hoe` | 호미 | 대장간 |
| `axe` | 도끼 | 대장간 |
| `brush` | 붓 | 필방 |
| `ink` | 먹 | 필방 |
| `inkstone` | 벼루 | 필방 |
| `hanji` | 한지 | 지물포 |
| `fan` | 부채 | 지물포 |
| `window` | 창호지 | 지물포 |
| `jar` | 옹기 | 옹기점 |
| `bowl` | 사발 | 옹기점 |
| `celad` | 청자 | 옹기점 |
| `root` | 도라지 | 약재상 |
| `ginseng` | 산삼 | 약재상 |
| `antler` | 녹용 | 약재상 |
| `bezoar` | 우황 | 약재상 |

- 같은 가게 물건끼리는 **한눈에 구분**돼야 한다. 곡괭이·낫·호미·도끼가 다 회색 쇳덩이면 실패다
- **이 17장이 들어오면 좌판의 물건 이름 글자를 뺀다.** 대장간 칸이 37px이라
  글자가 폰에서 9px(2.4mm)까지 내려간다 — 그림이 유일한 해결책이다.
  그러니 **작게 봐도 뭔지 알아볼 수 있는 실루엣**이 제일 중요하다
- 물건 하나만 덩그러니. 배경·그림자·받침 없음

### 짐 4종 · `96×96` (선택)

손님이 지고 오는 짐. `qty`(한 번에 사가는 개수)를 눈에 보이게 하는 장치다.
지금은 코드로 그려 두었고, 그림으로 바꾸면 더 좋아진다.

| 짐 | 해당 손님 | 사가는 개수 |
|---|---|---|
| 맨손 (그림 없음) | 토끼·까치·다람쥐·오소리 | 2~5개 |
| 봇짐 | 여우·사슴 | 8~9개 |
| 지게 | 멧돼지·거북 | 12~14개 |
| 달구지 | 곰·두루미·소·호랑이 | 16개 이상 |

동물 왼쪽에 붙는다. 동물과 겹쳐도 되지만 동물 얼굴은 가리면 안 된다.

## 2단계 — 화면 구조가 굳은 뒤

아직 그리지 말 것. 지금 그리면 버려진다.

| 대상 | 크기 | 왜 기다리나 |
|---|---|---|
| 가게 건물 5종 | `384×304` | 스와이프가 들어가며 크기가 다시 바뀌었다. 한 번 더 굳는지 보고 |
| 무너진 집 | `384×304` | 위와 같다 |
| 개집 | `168×120` | 자리가 방금 잡혔다. 한 번 더 굳는지 보고 |
| 바닥·길 타일 | 미정 | 길 모양이 방금 바뀌었다 |
| 창(패널) 장식 | 미정 | 형태가 아직 안 굳었다 |

지금 코드로 그린 것(기와지붕·흙벽·툇마루)으로 충분히 버틴다.

## 규격

### 크기는 왜 이 값인가

엔진(`core/engine.js`)이 **가로 480을 화면 폭에 맞추고 세로는 기기가 주는 대로**
받는다(`fit: 'width'`). 화면 폭이 최대 480px(`#app`의 max-width)이므로 가상 1px은
최대 1배로 그려지고, 해상도 배율 상한이 2라(`Math.min(devicePixelRatio, 2)`)
실제 픽셀로는 최대 **2배**다. 그래서 **가상 크기의 2배**면 어떤 기기에서도
안 뭉갠다. 그보다 크게 만들면 용량만 늘고 이득이 없다.

| 대상 | 화면에 보이는 크기 | 만들 크기 |
|---|---|---|
| 점장 | 30×30 | `72×72` |
| 손님 | 30×30 | `64×64` |
| 물건 | 30×52 | `64×112` |
| 엽전 | 22×22 | `48×48` |
| 가게 건물 | 188×150 | `384×304` |
| 현판 | 118×25 | `240×64` |

### 형식

- **PNG, 배경 투명.** 흰 배경으로 오면 못 쓴다
- 여백 없이 꽉 채우기. 그림 주위 빈 공간은 코드가 배치할 때 어긋나게 만든다
- 파일명은 위 표의 `id` 그대로. `pick.png`, `rabbit.png`
- 넣을 자리: `art/guests/`, `art/items/`

### 그림체 통일 규칙

따로따로 만들면 24장이 다 다른 그림체가 된다. 아래를 고정한다.

- **시점** 살짝 위에서 내려다본 정면 (지금 장면이 그 시점이다)
- **광원** 왼쪽 위에서 온다. 그림자는 오른쪽 아래
- **테두리** 굵은 갈색 선. 검정은 쓰지 않는다
- **팔레트** 아래 색에서만 고른다 (`scene.js`의 색과 같다)

```
한지  #ece2cb    흙벽  #dccfb2    나무  #8a6a45 / #6d5236
기와  #4a4139    먹    #2b241b    금   #a8763e
풀    #8b9e74    길    #d9cba9    비취  #4a7c59
```

## 만드는 순서

한 장씩 따로 만들면 그림체가 매번 달라진다. **한 장에 몰아서 그린 뒤 잘라 쓴다.**

1. **앵커 한 장** — 토끼 하나만 만들어서 게임에 넣고 **폰에서 실제 크기로 본다.**
   십중팔구 "너무 작아서 뭔지 모르겠다"가 나온다. 여기서 크기·선 굵기를 고친다.
   *(이건 이미 했다 — 아래 '앵커 시험' 참고)*
1.5. **점장 세 장(대장간부터)** — 화면 인상이 제일 크게 바뀐다. 한 가게씩 넣어가며 본다.
2. **손님 시트** — 확정된 규격으로 12종을 **한 장에** 그리게 하고 잘라 쓴다.
   한 번에 그리면 같은 붓으로 그려지므로 저절로 통일된다.
3. **물건 시트** — 같은 방식으로 17종. 가게별로 4~5장씩 나눠도 된다.
4. 여기서 멈춘다. 건물은 화면 구조가 굳은 뒤에.

1번을 건너뛰고 34장을 먼저 만들면, 크기가 틀렸을 때 34장을 다시 만들어야 한다.

## 넣는 쪽 준비

`scene.js`는 지금 이모지와 도형으로 그린다. 그림이 들어오면 그 자리만 바꾸면 된다.

- 손님 → `_walker()`의 `G.text(c, w.face, ...)`
- 나쁜 놈 → `_pest()`의 `G.text(c, t.face, ...)`
- 물건 → `_shop()`의 좌판 칸 안
- 엽전 → `_coin()`

그림 파일이 없으면 지금처럼 이모지로 그리도록 두면, 24장이 다 모이기 전에도
한 장씩 넣어가며 확인할 수 있다.
