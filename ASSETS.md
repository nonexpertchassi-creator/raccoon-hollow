# 그림 주문서

GPT/그림 도구에 통째로 넘기는 문서. **기획과 그림 사이의 계약서**다.
2026-08-21 기준 — Godot으로 옮기면서 **폴더가 바뀌었다**(아래 첫 칸).

## 어디에 넣나 — `godot/art/` 안이다

```
godot/art/hero/raccoon-make.png     ← 점장 너구리
godot/art/items/pick.png            ← 물건
godot/art/guests/rabbit.png         ← 손님
godot/art/pests/rat.png             ← 나쁜 놈
```

**저장소 뿌리의 `art/`가 아니다.** Godot은 제 프로젝트 폴더(`godot/`) 밖의
파일을 못 읽는다. 밖에 두면 주문서 목록에서는 사라지는데 화면에는 영영
안 나오는, 제일 나쁜 종류의 어긋남이 된다. 옛 `art/`는 통째로 옮겼다.

**넣은 뒤 한 번만:**

```sh
godot --headless --path godot --import
```

Godot이 그림을 제 방식으로 한 번 읽어 두는 과정이다(에디터를 켜 두었다면
저절로 한다). 이걸 안 하면 파일은 있는데 게임이 못 찾는다.

## 크기 — 표의 숫자대로 주면 된다

| 갈래 | 파일 크기 | 화면에 나오는 크기 |
|---|---|---|
| `hero/` 점장 | **144 × 144** | 72 × 72 |
| `items/` 물건 | **128 × 224** | 64 × 112 |
| `staff/` 직원 | **144 × 144** | 72 × 72 |
| `clerks/` 가게별 점장 (선택) | **144 × 144** | 72 × 72 |
| `guests/` 손님 | **128 × 128** | 64 × 64 |
| `pests/` 나쁜 놈 | **128 × 128** | 64 × 64 |

**파일은 화면 크기의 두 배다.** 요즘 폰은 화면이 촘촘해서(같은 1px 자리에
점을 2~3개 찍는다) 화면 크기 그대로 주면 뿌옇게 보인다. 두 배로 받아서
줄여 쓰면 또렷하다. 게임 안에서 크게·작게(0.45~1.8배)도 하므로 여유가 필요하다.

## 규격 (전부 공통)

- **PNG, 배경 투명.** 흰 배경으로 오면 못 쓴다
- **여백 없이 꽉 채우기.** 그림 주위 빈 공간은 배치를 어긋나게 만든다
- **발끝이 이미지 아래 변에 닿아야 한다** — 코드가 발끝을 그 자리에 놓고,
  발끝 높이로 앞뒤를 가린다. 발끝이 붕 떠 있으면 공중에 서게 된다
- **오른쪽을 보는 것 하나만.** 왼쪽은 코드가 뒤집는다 — 두 장씩 그리면
  장수가 두 배가 되고, 그 둘이 미묘하게 달라진다
- 파일명은 아래 목록의 이름 그대로. 폴더도 그대로

### 그림체 고정

따로 만들면 매번 다른 그림체가 된다. 아래를 고정한다.

- **시점** 살짝 위에서 내려다본 정면(3/4). 움직이는 방향과 상관없이 카메라를 본다
- **광원** 왼쪽 위. 그림자는 오른쪽 아래
- **테두리** 굵은 갈색 선. 검정은 쓰지 않는다
- **팔레트** 아래 색에서만 고른다

```
한지  #ece2cb    흙벽  #dccfb2    나무  #8a6a45 / #6d5236
기와  #4a4139    먹    #2b241b    금    #a8763e
풀    #8b9e74    길    #d9cba9    비취  #4a7c59
```

### 만드는 방법

**한 장에 몰아서 그린 뒤 잘라 쓴다.** 한 번에 그리면 같은 붓으로 그려져
저절로 통일된다. 따로 12번 시키면 12개 그림체가 나온다.

## 어느 순서로 그리나

| 차례 | 무엇 | 장수 | 왜 이 순서인가 |
|---|---|---|---|
| **1차** | 손님 30종 (걷는 모습) | **30** | 마을이 회색 덩어리를 벗는다. 지금 제일 어색한 자리 |
| **2차** | 손님 카드 1단 30장 | **30** | 뽑기에서 크게 뜨고 도감에 모인다 — **제일 오래 들여다보는 그림** |
| **3차** | 점장 너구리 | **5** | 화면에 오래 나온다. 두 장(만들기·팔기)만 들어와도 티가 난다 |
| **4차** | 직원 너구리 4등급 | **8** | 점장과 같은 몸에 **모자만** 다르다. 그래서 싸게 만든다 |
| **5차** | 물건 40종 | **40** | 제일 많다. 매대와 가게 창 두 군데에 쓰인다 |
| **6차** | 매대 5종 · 나쁜 놈 3종 | **8** | 가게마다 좌판이 달라진다 |
| 선택 | 손님 카드 2~4단 | 90 | 성이 오를 때 갈아입는다. 없으면 1단을 계속 쓴다 |
| 선택 | 가게별 점장 5가게 × 2포즈 | 10 | 없으면 공통 점장이 선다 |

### 누가 누구인지 — 이게 제일 중요하다

**점장과 직원은 같은 너구리다.** 몸도 크기도 같다(72×72).
크기로 가르면 직원이 덜 자란 너구리처럼 보인다 — 둘은 맡은 일만 다르다.

가르는 것은 **손에 든 것과 머리에 쓴 것**, 그 정도의 작은 차이다.

| | 누구 | 어디가 다른가 |
|---|---|---|
| **점장** | 이 가게 주인. 만들고 팔고 계산대를 오간다 | **손에 연장** — 대장간은 망치, 필방은 붓과 앞치마 |
| **직원** | 고용된 쪽. 작업대 옆에서 만들기만 한다 | 연장 없이 **머리에 쓴 것**만 — 머리띠 → 초립 → 패랭이 → 백립 |
| **손님** | 사러 온 짐승들. **서른 종** | 너구리가 아니다. 토끼·까치·해태… |

작은 차이로 충분한 이유가 있다. **둘은 서 있는 자리가 다르다** — 점장은
작업대와 계산대를 오가고, 직원은 마당 안쪽에 붙박이로 선다. 자리가 이미
말해 주므로 그림까지 크게 다를 필요가 없다.

직원 네 등급도 **모자만 바꾸면 된다.** 몸이 같으면 오히려 "같은 일을 하는데
격이 다르다"가 잘 읽힌다. 백립(흰 갓)이 제일 높다.

### 손님은 그림이 **두 벌**이다 — 헷갈리기 쉬운 자리

| | 어디에 쓰나 | 크기 | 어떻게 다른가 |
|---|---|---|---|
| `guests/rabbit.png` | 마을 길을 걸어온다 | 128×128 | **작다.** 옆에서 본 걷는 모습, 배경 없음 |
| `cards/rabbit-1.png` | 뽑기·도감 카드 | 512×768 | **크다.** 정면으로 선 초상, 배경까지 |

카드는 이 게임에서 **제일 오래 들여다보는 그림**이다. 뽑을 때 화면 가운데에
크게 뜨고, 도감에 모아 두고 다시 본다. 걷는 모습은 48px로 작게 지나가지만
카드는 250px로 멈춰 서 있으니 **공은 카드 쪽에 더 들여도 된다.**

카드 테두리와 등급 표시는 **코드가 그린다.** 짐승과 배경만 그리면 된다.

### 등급 여섯 — 귀할수록 귀티가 나야 한다

| | 등급 | 몇 종 | 어떤 짐승 |
|---|---|---|---|
| ⚪ | 흔함 | 8 | 토끼 · 까치 · 다람쥐 · 참새 · 개구리 · 두더지 · 고슴도치 · 오리 |
| 🟢 | 드묾 | 7 | 오소리 · 여우 · 수달 · 노루 · 족제비 · 살쾡이 · 산양 |
| 🔵 | 귀함 | 6 | 사슴 · 멧돼지 · 담비 · 원앙 · 늑대 · 백로 |
| 🟣 | 진귀 | 5 | 곰 · 거북 · 표범 · 사향노루 · 반달곰 |
| 🟠 | 영물 | 3 | 두루미 · 소 · **해태** |
| 🔴 | 신수 | 1 | **호랑이** |

**등급이 그림에서 읽혀야 한다.** 흔함은 수수하게, 신수는 한눈에 다르게.
같은 붓으로 그리되 공들인 정도가 달라야 뽑았을 때 "좋은 게 나왔다"가 온다.

**어디서 멈춰도 게임은 돈다.** 그림이 없는 자리는 여태처럼 도형과 이모지로
그린다. 한 장 넣을 때마다 그 자리만 바뀐다 — 전부 모을 때까지 기다릴 필요가 없다.
지금 세 장(점장 둘·토끼)이 이미 그렇게 화면에 나오고 있다.

**남은 장수와 파일 이름은 아래 목록에 있다** — 폴더를 훑어서 자동으로 세므로
절대 안 어긋난다. 갱신: `node tools/art.mjs --write`

---

## 남은 그림

<!-- 목록시작 -->
> 이 칸은 `node tools/art.mjs --write`가 다시 쓴다.
> **그림을 폴더에 넣으면 목록에서 저절로 빠진다** — 손으로 지울 필요 없다.

**170 / 170장** 들어왔다.

### 점장 너구리 — `godot/art/hero/` · 144×144 · **0장 남음**

전부 들어왔다. ✅

### 물건 — `godot/art/items/` · 128×224 · **0장 남음**

**두 군데에 쓰인다** — 매대 위(원래 크기)와 가게 창의 목록 썸네일(작게 줄여서).
그래서 작게 줄여도 뭔지 알아볼 수 있어야 한다 — 잔무늬보다 **실루엣**이 중요하다.

**등급 그림은 그리고 싶은 것만.** 가게를 승급하면 물건 이름이 바뀐다
(무쇠곡괭이 → 참쇠곡괭이 → 강철곡괭이). 그 등급 그림을 따로 주고 싶으면
`pick-1.png`(2등급) · `pick-2.png`(3등급)로 넣으면 그것부터 쓴다.
**없어도 된다** — 없으면 기본 그림에 등급 테(은·금)를 둘러 표시한다.
40종 × 3등급 = 120장을 다 그릴 이유는 없다.

전부 들어왔다. ✅

### 직원 너구리 — `godot/art/staff/` · 144×144 · **0장 남음**

**점장과 같은 몸, 같은 크기다.** 크기로 가르면 덜 자란 너구리처럼 보인다 —
둘은 같은 너구리고 맡은 일만 다르다. 가르는 것은 **머리에 쓴 것**이다.
점장은 손에 연장(망치 같은 것)을 들고, 직원은 그냥 모자만 쓴다.
등급도 모자로만 가른다 — 몸은 네 등급이 똑같아도 된다. 위로 갈수록 격이 오른다.
`work`는 일하는 자세, `sleep`은 조는 자세 — 진열대가 다 차면 존다.

전부 들어왔다. ✅

### 손님 — `godot/art/guests/` · 128×128 · **0장 남음**

마을에 걸어 들어오는 모습. **카드 그림과 다르다** — 이건 옆에서 본 걷는 모습이고,
카드는 정면으로 선 초상이다. 여기부터 있어야 마을이 회색 덩어리를 벗는다.
괄호 안이 카드 등급이다 — 귀한 짐승일수록 귀티가 나야 한다.

전부 들어왔다. ✅

### 손님 카드 1단 (1~5성) — `godot/art/cards/` · 512×768 · **0장 남음**

**세로로 긴 초상.** 정면을 보고 서 있는 모습, 배경까지 그린 한 장.
뽑기에서 크게 뜨고 도감에 모인다 — 이 게임에서 제일 오래 들여다보는 그림이다.
테두리·등급 표시는 코드가 그린다. **짐승과 배경만** 그리면 된다.
파일 이름은 `<손님id>-1.png`. 6~10성은 `-2`, 11~15성은 `-3`, 16~20성은 `-4`.

전부 들어왔다. ✅

### 손님 카드 2~4단 (6~20성) (선택) — `godot/art/cards/` · 512×768 · **20장 남음**

**나중에.** 없으면 1단 그림을 계속 쓴다.
같은 짐승이 성이 오를수록 차림이 좋아지는 식으로 — 옷·장신구·배경이 달라진다.

- [ ] `ox-4.png` — 소 — 16~20성
- [ ] `tiger-4.png` — 호랑이 — 16~20성
- [ ] `sparrow-4.png` — 참새 — 16~20성
- [ ] `frog-4.png` — 개구리 — 16~20성
- [ ] `mole-4.png` — 두더지 — 16~20성
- [ ] `hedgehog-4.png` — 고슴도치 — 16~20성
- [ ] `duck-4.png` — 오리 — 16~20성
- [ ] `otter-4.png` — 수달 — 16~20성
- [ ] `roe-4.png` — 노루 — 16~20성
- [ ] `weasel-4.png` — 족제비 — 16~20성
- [ ] `wildcat-4.png` — 살쾡이 — 16~20성
- [ ] `goral-4.png` — 산양 — 16~20성
- [ ] `marten-4.png` — 담비 — 16~20성
- [ ] `mandarin-4.png` — 원앙 — 16~20성
- [ ] `wolf-4.png` — 늑대 — 16~20성
- [ ] `egret-4.png` — 백로 — 16~20성
- [ ] `leopard-4.png` — 표범 — 16~20성
- [ ] `muskdeer-4.png` — 사향노루 — 16~20성
- [ ] `moonbear-4.png` — 반달곰 — 16~20성
- [ ] `haetae-4.png` — 해태 — 16~20성

### 나쁜 놈 — `godot/art/pests/` · 128×128 · **0장 남음**

전부 들어왔다. ✅

### 매대(좌판) — `godot/art/stalls/` · 192×176 · **0장 남음**

가게마다 **다르게 생겨야 한다.** 지금은 다섯 가게가 똑같은 나무 상자다.
그 가게에서 무엇을 파는지가 좌판에서 먼저 읽혀야 한다 —
대장간은 모루·쇠받침, 필방은 낮은 서안, 지물포는 종이 두루마리 선반,
옹기점은 흙바닥 좌판, 약재상은 약재 서랍장 같은 식으로.
**물건 그림이 이 위에 얹힌다** — 가운데 위쪽을 비워 둘 것.
승급 그림(`smith-1.png`)도 넣을 수 있다. 없으면 기본 것을 쓴다.

전부 들어왔다. ✅

### 뽑기와 룰렛 판 — `godot/art/ui/` · 아래 참고 · **0장 남음**

움직임은 `MOTION.md`에 적어 뒀다.
· `back.png` 512×768 — 카드 뒷면. 뒤집기 연출에 쓴다. 등급 빛은 코드가 얹는다
· `wheel.png` 512×512 — 룰렛 원판. **열두 칸**이 그려진 바퀴. 칸 안 글자는 코드가 얹는다
· `needle.png` 64×96 — 룰렛 바늘. 위에서 아래를 가리킨다

전부 들어왔다. ✅

### 가게별 점장 (선택) — `godot/art/clerks/` · 144×144 · **0장 남음**

가게마다 다른 너구리. **여기까지 오면 제일 좋다.**
대장간은 망치가 어울리고 필방은 앞치마가 어울린다 — 그 가게의 일이 보이게.
한 장이라도 있으면 그 가게만 이 그림을 쓰고, 없는 자리는 공통 점장이 선다.
`make`·`sell` 둘만 있어도 충분하다(걷기·조는 것은 공통 것을 쓴다).
**승급 그림도 된다** — `smith-make-1.png`(참쇠) · `smith-make-2.png`(강철).
없으면 그 가게의 기본 점장이 계속 선다.

전부 들어왔다. ✅

<!-- 목록끝 -->

## 넣는 법

1. `godot/art/<갈래>/<이름>.png` 로 넣는다 (위 목록의 이름 그대로)
2. `godot --headless --path godot --import` 한 번 (에디터를 켜 뒀으면 저절로 한다)
3. 끝. **하나만 넣어도 그 자리만 바뀐다** — 없는 자리는 여태처럼 도형과 이모지다
4. 목록 갱신: `node tools/art.mjs --write`
