# 그림 에셋 목록

그림을 언제·어떤 규격으로 만들지 정해두는 문서. **기획과 그림 사이의 계약서**다.

이게 없으면 기능이 바뀔 때마다 그린 그림이 버려진다. 반대로 이 목록에 있는 것만
그리면, 기능이 아무리 바뀌어도 그림은 안 버려진다.

## 원칙: 그림은 "기능 완성"을 기다리지 않는다

기다려야 하는 건 **기능**이 아니라 **안 변할 것**이다.
동물과 물건은 이름·개수가 이미 확정됐으므로 지금 그려도 안전하다.
가게 건물과 배경은 화면 구조가 아직 움직이므로 지금 그리면 버려진다.

## 1단계 — 지금 그려도 되는 것 (29장)

이 29장만 들어가도 화면이 완전히 달라진다.

### 손님 12종 · `64×64`

`content.js`의 `GUESTS`. 지금은 이모지로 대신하고 있다.

| id | 이름 | 임시 이모지 | 성격 (그림에 드러나야 하는 것) |
|---|---|---|---|
| `rabbit` | 토끼 | 🐰 | 작고 날쌔다 |
| `magpie` | 까치 | 🐦 | 제일 빠르다. 값을 깎는 얄미운 얼굴 |
| `squirrel` | 다람쥐 | 🐿️ | 작다. 두 개만 들고 간다 |
| `badger` | 오소리 | 🦡 | 느긋하고 둥글다 |
| `fox` | 여우 | 🦊 | 날렵하고 약삭빠르다 |
| `deer` | 사슴 | 🦌 | 기품 있다. 값을 후하게 쳐준다 |
| `boar` | 멧돼지 | 🐗 | 우악스럽다. 한 종류를 쓸어간다 |
| `bear` | 곰 | 🐻 | 제일 크고 육중하다 |
| `turtle` | 거북 | 🐢 | 제일 느리다. 부티가 난다 |
| `crane` | 두루미 | 🦢 | 길쭉하다 |
| `ox` | 소 | 🐂 | 크고 느리다. 수레와 어울려야 한다 |
| `tiger` | 호랑이 | 🐯 | 가장 귀하고 위압적이다 |

- 정면 또는 살짝 3/4. 전신. 서 있는 자세 하나면 충분하다(걷기 애니메이션은 코드가 위아래로 흔들어 준다)
- 발끝이 이미지 아래 변에 닿아야 한다 — 코드가 발끝 높이로 앞뒤를 가린다
- 조선 마을에 어울리는 차림. **크기 차이가 곧 씀씀이 차이다** — 다람쥐는 작고 소·곰은 커야
  한눈에 읽힌다. 걷는 속도도 손님마다 다르니(까치 1.7배, 소 0.4배) 몸집으로 짐작되게
- **짐을 들려서 그리지 말 것.** 봇짐·지게·달구지는 코드가 따로 그려 붙인다.
  동물이 이미 가방을 메고 있으면 짐이 두 개가 된다. 맨몸으로만 그린다.

### 물건 17종 · `64×112`

`content.js`의 `SHOPS[].items`. 좌판 한 칸에 세로로 들어간다.

| id | 이름 | 가게 |
|---|---|---|
| `pick` | 곡괭이 | 대장간 |
| `sickle` | 낫 | 대장간 |
| `hoe` | 호미 | 대장간 |
| `axe` | 도끼 | 대장간 |
| `brush` | 붓 | 필방 |
| `ink` | 먹 | 필방 |
| `inkstone` | 벼루 | 필방 |
| `hanji` | 한지 | 지물포 |
| `fan` | 부채 | 지물포 |
| `window` | 창호지 | 지물포 |
| `jar` | 옹기 | 옹기점 |
| `bowl` | 사발 | 옹기점 |
| `celad` | 청자 | 옹기점 |
| `root` | 도라지 | 약재상 |
| `ginseng` | 산삼 | 약재상 |
| `antler` | 녹용 | 약재상 |
| `bezoar` | 우황 | 약재상 |

- 같은 가게 물건끼리는 **한눈에 구분**돼야 한다. 곡괭이·낫·호미·도끼가 다 회색 쇳덩이면 실패다
- 물건 하나만 덩그러니. 배경·그림자·받침 없음

### 짐 4종 · `96×96` (선택)

손님이 지고 오는 짐. `qty`(한 번에 사가는 개수)를 눈에 보이게 하는 장치다.
지금은 코드로 그려 두었고, 그림으로 바꾸면 더 좋아진다.

| 짐 | 해당 손님 | 사가는 개수 |
|---|---|---|
| 맨손 (그림 없음) | 토끼·까치·다람쥐·오소리 | 2~5개 |
| 봇짐 | 여우·사슴 | 8~9개 |
| 지게 | 멧돼지·거북 | 12~14개 |
| 달구지 | 곰·두루미·소·호랑이 | 16개 이상 |

동물 왼쪽에 붙는다. 동물과 겹쳐도 되지만 동물 얼굴은 가리면 안 된다.

## 2단계 — 화면 구조가 굳은 뒤

아직 그리지 말 것. 지금 그리면 버려진다.

| 대상 | 크기 | 왜 기다리나 |
|---|---|---|
| 가게 건물 5종 | `384×304` | 스와이프가 들어가며 크기가 다시 바뀌었다. 한 번 더 굳는지 보고 |
| 무너진 집 | `384×304` | 위와 같다 |
| 바닥·길 타일 | 미정 | 길 모양이 방금 바뀌었다 |
| 창(패널) 장식 | 미정 | 형태가 아직 안 굳었다 |

지금 코드로 그린 것(기와지붕·흙벽·툇마루)으로 충분히 버틴다.

## 규격

### 크기는 왜 이 값인가

엔진(`core/engine.js`)이 **가로 480을 화면 폭에 맞추고 세로는 기기가 주는 대로**
받는다(`fit: 'width'`). 화면 폭이 최대 480px(`#app`의 max-width)이므로 가상 1px은
최대 1배로 그려지고, 해상도 배율 상한이 2라(`Math.min(devicePixelRatio, 2)`)
실제 픽셀로는 최대 **2배**다. 그래서 **가상 크기의 2배**면 어떤 기기에서도
안 뭉갠다. 그보다 크게 만들면 용량만 늘고 이득이 없다.

| 대상 | 화면에 보이는 크기 | 만들 크기 |
|---|---|---|
| 손님 | 27×27 | `64×64` |
| 물건 | 30×52 | `64×112` |
| 엽전 | 22×22 | `48×48` |
| 가게 건물 | 188×150 | `384×304` |
| 현판 | 118×25 | `240×64` |

### 형식

- **PNG, 배경 투명.** 흰 배경으로 오면 못 쓴다
- 여백 없이 꽉 채우기. 그림 주위 빈 공간은 코드가 배치할 때 어긋나게 만든다
- 파일명은 위 표의 `id` 그대로. `pick.png`, `rabbit.png`
- 넣을 자리: `art/guests/`, `art/items/`

### 그림체 통일 규칙

따로따로 만들면 24장이 다 다른 그림체가 된다. 아래를 고정한다.

- **시점** 살짝 위에서 내려다본 정면 (지금 장면이 그 시점이다)
- **광원** 왼쪽 위에서 온다. 그림자는 오른쪽 아래
- **테두리** 굵은 갈색 선. 검정은 쓰지 않는다
- **팔레트** 아래 색에서만 고른다 (`scene.js`의 색과 같다)

```
한지  #ece2cb    흙벽  #dccfb2    나무  #8a6a45 / #6d5236
기와  #4a4139    먹    #2b241b    금   #a8763e
풀    #8b9e74    길    #d9cba9    비취  #4a7c59
```

## 만드는 순서

한 장씩 따로 만들면 그림체가 매번 달라진다. **한 장에 몰아서 그린 뒤 잘라 쓴다.**

1. **앵커 한 장** — 토끼 하나만 만들어서 게임에 넣고 **폰에서 실제 크기로 본다.**
   십중팔구 "너무 작아서 뭔지 모르겠다"가 나온다. 여기서 크기·선 굵기를 고친다.
2. **손님 시트** — 확정된 규격으로 12종을 **한 장에** 그리게 하고 잘라 쓴다.
   한 번에 그리면 같은 붓으로 그려지므로 저절로 통일된다.
3. **물건 시트** — 같은 방식으로 17종. 가게별로 4~5장씩 나눠도 된다.
4. 여기서 멈춘다. 건물은 화면 구조가 굳은 뒤에.

1번을 건너뛰고 29장을 먼저 만들면, 크기가 틀렸을 때 29장을 다시 만들어야 한다.

## 넣는 쪽 준비

`scene.js`는 지금 이모지와 도형으로 그린다. 그림이 들어오면 그 자리만 바꾸면 된다.

- 손님 → `_walker()`의 `G.text(c, w.face, ...)`
- 물건 → `_shop()`의 좌판 칸 안
- 엽전 → `_coin()`

그림 파일이 없으면 지금처럼 이모지로 그리도록 두면, 24장이 다 모이기 전에도
한 장씩 넣어가며 확인할 수 있다.
