# 너구리 만물상 제작 노트

커밋 67개, 코드 4,850줄을 지나며 실제로 있었던 일. 잘한 것, 틀린 것,
다음에 만들 때의 순서. **감상이 아니라 기록이다** — 숫자는 전부 실제로 잰 값이다.

---

## 0. 먼저 정할 것 — 어디에 낼 것인가

이 결정이 나머지 전부를 규정한다. **그런데 우리는 커밋 67개를 지나고 나서야
이 얘기를 했다.** 이게 이 프로젝트의 가장 큰 절차상 실수다.

### '껍데기 씌우기'가 뭐였나

지금 게임은 웹 기술(HTML·자바스크립트·캔버스)로 만들어져 있다.
Capacitor 같은 도구로 이걸 감싸면 앱 스토어에 올라간다. 빠르고 확실하지만,
**엔진을 다루는 법은 하나도 안 배운다.** 앞으로 계속 게임을 만들 거라면
그 길은 지름길이지 배움이 아니다.

### 진짜 개발로 가는 길 셋

| 길 | 언어 | 배우는 것 | 현실 |
|---|---|---|---|
| **Godot 4** | GDScript (파이썬과 비슷) | 게임 엔진 전반 | 무료·로열티 0. 2D가 엔진의 본체. 안드로이드·iOS 진짜 네이티브 빌드. 에디터 150MB |
| Unity | C# | 업계 표준 | 자료와 일자리가 압도적. 무겁고, 2D는 3D 엔진 위에 얹은 구조. 2023년 요금 사태로 신뢰 문제가 있었다(철회됨) |
| 네이티브 (Kotlin + Swift) | 둘 다 | 플랫폼 밑바닥 | 코드를 두 벌 관리한다. 엔진이 해주는 걸 전부 직접 만든다 |

**추천은 Godot 4다.** 이유 셋:

1. **2D가 곁다리가 아니다.** 이 게임은 아이소메트릭 2D인데, Godot는 타일맵과
   Y축 정렬(앞뒤 가리기)이 엔진에 들어 있다. 우리가 iso.js에서 손으로 짠
   깊이 정렬 — 계산대가 너구리를 덮던 그 문제 — 를 엔진이 대신 해준다.
2. **돈이 안 든다.** 로열티도 매출 신고도 없다. 개인이 실험하기에 부담이 0이다.
3. **GDScript가 쉽다.** 파이썬을 닮아서, 지금 읽고 있는 자바스크립트와
   구조가 크게 다르지 않다. C#도 쓸 수 있어서 나중에 Unity로 옮기기도 쉽다.

### 옮길 때 무엇이 살아남나

이게 제일 중요하다. **지금 만든 것의 절반이 그대로 간다.**

| 지금 | 줄 | 옮길 때 |
|---|---|---|
| `content.js` | 450 | **그대로 산다.** 숫자와 이름은 언어를 안 탄다 |
| `sim.js` | 884 | **번역만 한다.** 순수한 규칙이라 1:1로 옮겨진다 |
| `tools/` | 598 | **방법이 산다.** Godot도 화면 없이 돌릴 수 있다 |
| `PLAN.md` | — | **제일 값지다.** 왜 그렇게 정했는지의 기록. 같은 함정을 두 번 안 밟는다 |
| `iso.js` | 1,550 | 버린다 — 그런데 이건 원래 **엔진의 몫**이다 |

버리는 쪽이 엔진이 더 잘하는 부분이라, 옮기는 건 손해가 아니라 승격이다.

### 언제 옮기나 — 지금은 아니다

**재미가 아직 확정되지 않았다.** 재미를 확인하는 데는 지금 도구가 훨씬 빠르다
(고치고 30초면 폰에서 확인). 엔진으로 옮기면 그 순환이 몇 분으로 늘어난다.

옮기는 시점은 **"이 게임은 만들 가치가 있다"가 확실해졌을 때**다.
그때까지 Godot를 깔고 튜토리얼로 손에 익히면 두 갈래가 병렬로 간다.

---

## 1. 잘한 것

립서비스가 아니라, 실제로 결과를 바꾼 것만 적는다.

### 눈이 정확했다 — 다섯 번 다 진짜 고장이었다

"왠지 이상한데"를 무시하지 않은 것이 이 프로젝트에서 제일 큰 기여다.
전부 코드에 실재하는 버그였고, 전부 먼저 발견했다.

| 말한 것 | 실제 원인 |
|---|---|
| "손님이 오기도 전에 돈이 오른다" | 경제는 즉시 계산하는데 화면만 늦었다. 표시를 손님 도착에 맞춤 |
| "점포가 공중에 떠 있는 것 같다" | 그림자 타원이 바닥 밖으로 나가고 지붕이 길바닥과 같은 색이었다 |
| "대장간을 뚫고 지나간다" | 세로 스크롤 시절의 직선 이동 규칙이 격자 마을에 남아 있었다 |
| "화난 채로 온다" | 못 사는 손님을 **도착 즉시** 💢로 그렸다 |
| "일꾼이 안 보인다" | 3×3 마당은 안쪽 칸이 하나뿐인데 작업대가 그걸 써서 **설 자리가 0개**였다 |

### 자기 아이디어를 빨리 뒤집었다

"돌부터 시작하는 컨셉은 내가 잘못 설정한 것 같다" — 대장장이가 돌도끼로
시작하는 건 말이 안 된다는 걸 스스로 알아채고 바로 무쇠·참쇠·강철로 바꿨다.
**자기가 낸 안을 버리는 건 생각보다 드물고, 프로젝트를 살린다.**

### 감이 아니라 숫자를 요구했다

"일꾼은 최대 몇인게 숫자상 제일 안전할까?" — 이 한 문장이
8시간 × 2운 번호 측정을 낳았고, 결과는 예상과 달랐다(2명 이상은 아무 효과가 없다).
**물어보지 않았으면 쓸모없는 업그레이드를 수십억에 파는 게임이 됐다.**

### 이탈을 계속 생각했다

"일일 던전처럼 숙제가 되면 탈주할 텐데", "들락날락하는 수고로 이탈이 생길 것" —
기능이 아니라 **사람이 왜 떠나는가**를 계속 봤다. '가게 안' 화면을 버리고
마당 방식으로 간 결정이 여기서 나왔다.

---

## 2. 틀린 것

### 플랫폼을 마지막에 물었다 — 가장 큰 것

기술 선택은 플랫폼이 정한다. 67커밋 뒤에 물으면, 이미 만든 것에 끌려가서
판단이 흐려진다("이만큼 만들었는데 버리기 아깝다"). **다음엔 첫 주에.**

### 재기 전에 정했다 — 두 번 만든 것들

| 만든 것 | 무슨 일 |
|---|---|
| 만·억 ↔ K·M 고르는 단추 | 만들고 나서 "그냥 K로" — 재는 게 아니라 **써 보고** 알 일이었다 |
| 줄 서기 시간을 화남에 포함 | 만들고 재보니 후반 97.5%가 성난 얼굴 → 도로 뺐다 |

둘 다 **먼저 재고 만들었으면 안 만들었을 것**이다.
다만 이건 절반은 내 책임이다 — 순서를 잡는 건 만드는 쪽 일이다.

### 작업 중에 새 요청을 끼워 넣었다

두 번 있었다. 진행 중인 작업이 흔들리고, 검증이 반쪽이 된다.
**한 덩어리가 끝나고 확인한 뒤에 다음 것**이 결과적으로 더 빠르다.

### 아직 안 정한 것 — 누가 왜 돈을 내는가

포졸 뽑기·인앱결제 얘기는 나왔지만, **"어떤 사람이 무엇 때문에 지갑을 여는가"**는
아직 없다. 이건 나중 문제가 아니라 게임 구조를 정하는 문제다.
지금 20성 단골, 카드 도감, 뽑기 같은 뼈대가 다 그 답에 딸려 있다.

---

## 3. 내가 틀린 것

공정하려면 이쪽도 적어야 한다.

- **재기 전에 만들었다.** 위의 두 번은 내가 "먼저 재보자"고 했어야 했다.
- **죽은 코드를 방치했다.** 화면 방식을 갈아탈 때 옛 파일 1,766줄을 안 지웠다.
  전체의 27%가 죽은 채로 남아, 저장소를 읽는 사람을 헷갈리게 했다.
- **화면을 안 봤다.** 표시 버그 다섯 개를 전부 놓쳤다 — 코드만 읽고 화면을
  안 봤기 때문이다. **스크린샷을 찍기 시작하고서야** 내가 먼저 잡기 시작했다.

---

## 4. 다음에 만들 때의 순서

순서가 중요하다. 뒤집으면 반드시 두 번 만든다.

### 0단계 — 어디에 낼지, 누가 왜 할지

플랫폼과 "이걸 누가 왜 하는가" 한 문단. **코드 한 줄 쓰기 전에.**
*왜:* 둘 다 나중에 바꾸면 만든 것을 버려야 한다.

### 1단계 — 규칙을 데이터로 분리

숫자·이름·설명을 파일 하나에 몰아둔다(`content.js`).
*왜:* 이 프로젝트에서 제일 잘한 구조 결정이다. 값을 만질 때 로직을 안 건드리니
밸런스를 수백 번 바꿔도 게임이 안 깨졌다. 기획자가 코드를 몰라도 만질 수 있다.

### 2단계 — 화면 없이 도는 코어 + 재는 도구

경제 규칙(`sim.js`)에 그리는 코드를 한 줄도 넣지 않는다.
그 다음 바로 **10시간 플레이를 30초에 돌리는 도구**(`balance.mjs`)를 만든다.
*왜:* 이게 없으면 밸런스를 감으로 정하게 된다. 이 도구 덕분에
죽은 품목 7개를 찾아 살렸고, 강화 클릭을 1,986번에서 0번으로 줄였고,
돈 단위를 1.31경에서 1,229억으로 눌렀다. **전부 재서 정한 값이다.**

### 3단계 — 그 다음에 화면

*왜:* 화면을 먼저 만들면 재미없는 게임을 예쁘게 만들게 된다.
2단계 도구로 "이 곡선은 재미있다"를 확인한 뒤에 그리는 게 순서다.

### 4단계 — 수치는 재서 정한다

새 수치를 넣기 전에 **먼저 잰다.** 예: 일꾼 상한은 0·1·2·3·4·6명을
8시간씩 두 운 번호로 돌려서 "1명"이라는 답을 얻었다.
*왜:* 감으로 정한 수치는 대부분 틀리고, 틀린 걸 되돌리는 비용이 재는 비용보다 크다.

### 5단계 — 눈으로 본다

고칠 때마다 화면을 찍어서 본다.
*왜:* 표시 버그는 코드를 아무리 읽어도 안 보인다. 이 프로젝트의 표시 버그 다섯 개가
그 증거다. 자동으로 스크린샷을 찍는 데 드는 시간은 1분이다.

### 6단계 — 죽은 코드는 그날 지운다

방식을 갈아탔으면 옛 파일을 **그날** 지운다.
*왜:* "혹시 나중에"는 오지 않는다. 대신 남은 파일이 읽는 사람을 헷갈리게 한다.

---

## 5. 자동화 — 이미 심어둔 것과 다음

"AX 자동화, 버그 확인"에 해당하는 것들. **엔진을 바꿔도 이 방법은 그대로 간다.**

| 도구 | 하는 일 |
|---|---|
| `tools/balance.mjs` | 10시간 플레이를 30초에. 죽은 품목·정지율·빈손률을 표로 |
| `tools/calibrate.mjs` | 관문 42개가 언제 열리고 그때 수입 몇 초치였는지 기록 |
| `tools/iterate.py` | 그 기록을 목표 삼아 값을 자동으로 수렴 (돈 단위 개편에 8판 사용) |
| `tools/art.mjs` | 폴더를 훑어 남은 그림만 목록에 남긴다 |
| Playwright 스크립트 | 화면을 자동으로 띄워 오류·겹침·길 이탈을 세고 스크린샷 |

### 다음에 붙일 것

- **회귀 검사** — 수치가 전보다 나빠지면 실패로 알리기.
  지금은 사람이 표를 읽고 판단한다. `빈손 > 3%`, `정지율 > 40%` 같은 선을 넘으면
  자동으로 빨간불이 켜지게.
- **크래시 자동 재현** — 오류가 나면 그 순간의 저장본을 남겨서 그대로 다시 돌리기.
- **운 번호 여러 개 동시 검사** — 지금은 운 번호 1·2·3을 손으로 돌린다. 한 번에.

---

# 네 갈래 — 기획 · 개발 · 디자인 · 운영

> 2026-08-29에 유저가 물었다. *"보통은 기획/개발/디자인/운영, 이렇게 큰
> 카테고리부터 어떻게 나뉘는지 좀 알려줄래?"* 그리고 *"우리가 가진 걸
> 우선 저렇게 쪼갤 수 있어?"*
>
> 여기가 그 답이다. **"지금 뭘 할 차례지?"를 물을 때 여기부터 본다.**

## 1. 기획 — 무엇을 만들지 정한다

| 세부 갈래 | 하는 일 | 우리 것 | 상태 |
|---|---|---|---|
| **시스템 기획** | 규칙과 숫자를 만든다 | `SYSTEMS.md` · `content.js` | 두껍다 |
| **밸런스 기획** | 그 숫자를 조율하고 잰다 | `tests/balance.gd` · `tools/calibrate.mjs` · `tools/guests.mjs` | 두껍다 |
| **콘텐츠 기획** | 가게 20 · 손님 30 · 의뢰를 채운다 | `content.js` · `BESTIARY.md` | 가게 15/20 |
| **시나리오 기획** | 이야기 · 대사 · 세계관 | `STORY.md` · `content.js`의 INTRO·BEATS | 뼈대만 |
| **UX 기획** | 화면 흐름 · 손맛 | `FLOW.md` · `MOTION.md` | 두껍다 |
| **사업(BM) 기획** | 유료화 · 광고 · 상품 | 장부의 나뭇잎·광고 칸 | **초안. 룰렛이 빠져 구멍** |
| **캐릭터 기획** | 누가 몇 마리, 무엇을 하나 | `RACCOON.md` · `BESTIARY.md` | 두껍다 |

## 2. 개발 — 그걸 돌아가게 만든다

| 세부 갈래 | 하는 일 | 우리 것 | 상태 |
|---|---|---|---|
| **클라이언트** | 화면에 보이는 것 | `godot/view/` · `godot/main.gd` | 돈다 |
| **규칙(시뮬레이션)** | 값·시간·확률 계산 | `godot/rules/sim.gd` | 돈다 |
| **서버** | 여러 사람의 자료 저장·검증 | **없다 — 없기로 정했다** | 해당 없음 |
| **툴/파이프라인** | 만드는 사람이 쓸 도구 | `tools/` 14개 | 두껍다 |
| **QA 자동화** | 고장을 기계가 잡는다 | `tools/crosscheck.sh` 11가지 | 두껍다 |
| **저장/이관** | 옛 저장본을 안 깨뜨린다 | `Sim.SAVE_VER` 15판 + 마이그레이션 | 돈다 |
| **최적화** | 폰에서 안 버벅이게 | **안 봤다** | **빈칸** |

## 3. 디자인(아트) — 보이고 들리는 것

| 세부 갈래 | 하는 일 | 우리 것 | 상태 |
|---|---|---|---|
| **컨셉/원화** | 이 게임이 어떻게 생겼나 | 결(동화·힐링)만 정해져 있다 | 말로만 |
| **2D 리소스** | 실제 쓰는 그림 | `godot/art/` | **45 / 487장** |
| **UI 디자인** | 창 · 단추 · 글꼴 | 코드가 그린다(`view/panel.gd` 등) | 임시 |
| **애니메이션** | 어떻게 움직이나 | `MOTION.md`(설계) | **그림이 없어 못 만듦** |
| **사운드/BGM** | 소리 | `view/sfx.gd` — **자리 7개, 파일 0개** | **빈칸** |

## 4. 운영 — 낸 뒤에 계속 하는 일

| 세부 갈래 | 하는 일 | 우리 것 | 상태 |
|---|---|---|---|
| **지표(데이터)** | 어디서 그만두나 | `METRICS.md` · `sim.bump()` · `tools/dash.mjs` | **자리는 심었다** |
| **QA(사람)** | 사람이 직접 만져 본다 | `tools/shots.sh`로 눈으로 보는 것뿐 | **빈칸** |
| **빌드/배포** | 스토어에 올린다 | **안 해 봤다** | **빈칸** |
| **업데이트/이벤트** | 기간제 이벤트 | `content.js`의 EVENT 뼈대 | 뼈대만 |
| **고객 대응(CS)** | 문의 · 버그 신고 | **없다** | **빈칸** |
| **마케팅/UA** | 사람을 데려온다 | **없다** | **빈칸** |

## 지금 어디쯤인가

```
기획   ████████████████████  아주 두껍다 (문서 8,000줄 남짓)
개발   ████████████████░░░░  튼튼하다 (규칙·화면·도구·시험)
디자인 ███░░░░░░░░░░░░░░░░░  그림 45/487 · 소리 0/7
운영   █░░░░░░░░░░░░░░░░░░░  지표 자리만
```

**세 가지를 적어 둔다.**

**1. 기획이 두꺼운 건 첫 게임이라 그런 게 아니다.** 1인 제작에서 기획은
*"적어 두지 않으면 잊어버리는 것"*이다. 회사면 옆자리에 물으면 되지만
우리는 물어볼 상대가 문서뿐이다. 그러니 **줄일 게 아니라 한 벌로 유지**한다.

**2. 제일 비어 있는 건 디자인이고, 코드로 못 메꾼다.** 그림 487장 중 45장.
`ASSETS.md`를 자동으로 뽑아 두는 게 그래서 중요했다.

**3. 운영은 아직 시작도 안 했지만 하나는 지금 해야 한다 — 지표다.**
`METRICS.md` 맨 위에 적힌 말: *"지표는 거슬러 올라가 모을 수 없다."*
사람이 들어온 뒤에 "첫날에 몇 명이 그만뒀지?"를 물으면 **답이 영영 없다.**
나머지 운영 일(스토어·CS·마케팅)은 낼 때 해도 된다.

---

## 한 줄로 줄이면

**재서 정하고, 눈으로 확인하고, 죽은 건 그날 지운다.**
이 셋을 지킨 구간은 결과가 좋았고, 어긴 구간은 두 번 만들었다.
