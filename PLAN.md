# 다음 작업 계획

2026-08-18 · 시뮬레이션(`tools/balance.mjs`) 결과를 근거로 작성.

## 진행 상황

| 단계 | 상태 |
|---|---|
| Phase 0 · 진단 도구 | ✅ 완료 |
| Phase 1 · 장바구니 | ✅ 완료 (무작위 방식으로) |
| Phase 2 · 콘텐츠 수명 | ✅ 완료 |
| Phase 3 · 조작 피로 | ✅ 완료 (자동 강화) |
| Phase 4 · `core/` 정리 | ⬜ 남음 |
| 엔드게임 (5시간 이후) | ⬜ 남음 — 아래 참조 |

### 지금까지의 결과 (8시간 · 씨앗 1·2·3)

| 항목 | 전 | 후 |
|---|---|---|
| 안 팔려 멈춘 품목 | 15개 중 7개 | **0개** |
| 콘텐츠 소진 | 4시간 | **5시간 15분** |
| 손님 7마리가 다 오는 시점 | 1시간 4분 | **4시간 44분** |
| 강화 클릭 | 1,986회 | **약 550회** (자동 강화 구입 뒤 0) |

### 재고 부족을 게임 규칙으로 쓸 수 있는가 — 못 쓴다 (측정 완료)

"손님이 원하는 물건이 없으면 기다리거나 평판이 깎이게 하자"를 검토했다.
**지금 구조로는 튜닝으로 도달할 수 없다.**

먼저 지금 상태:

| | 값 |
|---|---|
| 원하는 개수를 못 채운 거래 | 0.4% (전부 첫 1시간, 2시간째부터 0.0%) |
| 진열대가 텅 빈 시간 | 1.7% |
| 진열대가 꽉 찬 시간 | 16.5% |

병목이 반대쪽에 있다. 물건이 모자란 게 아니라 손님이 모자라 물건이 쌓인다.

손잡이를 다섯 개 돌려봤고 전부 실패했다.

| 손잡이 | 결과 |
|---|---|
| 손님 구매량 1.5~2배 | 과도하게 뒤집힘. 매출이 같이 늘어 콘텐츠 소진이 4시간대로 단축 |
| 생산 하한 0.4→0.45~0.8 | 앞 4시간 꽉 참 + 뒤 3시간 텅 빔. 출렁이지 않고 두 국면으로 갈릴 뿐 |
| 이정표를 생산 2배로 이전 | **더 나빠짐.** 8시간째 진열대 91% 꽉 참 |
| 수요가 품목·가게 수 따라 증가 | 2시간째 재고 0, 거래의 46~59%를 못 채움 |
| 요청 간격 14→60초 | **효과 없음.** 칸을 여는 관문은 타이머가 아니라 돈이다 |

원인은 구조적이다. **생산에는 천장이 있고(`timeFloor`) 수요에는 없다.**
수요가 그 천장을 넘으면 영구히 바닥, 안 넘으면 영구히 꽉 참이라
중간에 머무는 설정이 존재하지 않는다.

주의: 총합 평균은 이 문제를 숨긴다. 생산 하한 0.45의 평균 재고는 19.7/40으로
"딱 중간"처럼 보이지만 시간대별로는 36~39와 0.7의 평균일 뿐이다.
**밸런스는 반드시 시간대별로 볼 것.**

쓰려면 튜닝이 아니라 구조를 바꿔야 한다 — 진열대 확장을 업그레이드로 만들거나,
하위 품목을 상위 품목의 재료로 쓰거나(초기 검토안 (c)), 손님 대기열을 넣는 식이다.

**후속:** 튜닝 대신 구조를 하나 얹어서 이 문제를 부분적으로 풀었다 — '장'이다.
작은 건물이 가끔 북적이고, 그때 누르면 30초간 손님이 두 배로 온다.
수요 쪽에 붙였기 때문에 쌓인 재고가 실제로 빠진다. 8시간 측정:

| | 안 누름 | 볼 때마다 누름 |
|---|---|---|
| 진열대가 꽉 찬 시간 | 15.8% | **6.7%** |
| 평균 재고 | 31.3/40 | **13.3/40** |
| 누적 매출 | 288조 | 335조 (+16%) |
| 콘텐츠 소진 | 5시간15분 | 4시간40분 |

매출이 16%만 오르면서 꽉 찬 시간이 절반 이하로 떨어진다. 튜닝으로는
못 하던 일을 구조 하나로 한 셈이다. 대신 콘텐츠 소진이 35분 앞당겨진다.
231회는 매 기회를 다 누른 상한이므로 실제 플레이는 이보다 완만하다.

### 가게 승급 — 볼거리가 1시간 51분에 끝나던 문제

측정으로 드러난 것: **마을이 1시간 51분이면 다 채워진다.** 가게 다섯, 작은 건물
넷이 전부 서고 나면 지도 위에 새로 생길 게 없고, 그 뒤로는 이미 본 건물 안에서
숫자만 오른다. "1~2시간이면 끝"이라는 체감의 정체가 이것이다.

돌도끼 → 쇠도끼 → 강철도끼. 가게마다 3등급이고, 승급하면 레벨이 1로 돌아가는
대신 밑천이 통째로 좋아진다 — **그 가게만의 작은 환생**이다.

설계에서 지킨 것:

- **승급 직후 값이 직전 만렙보다 커야 한다.** 작으면 애써 만렙을 찍고 승급했는데
  수입이 떨어지는 꼴이 된다. 1급 만렙 ×31 → 2급 시작 ×40, 2급 만렙 ×4,880 →
  3급 시작 ×6,000으로 잡았다.
- **조건은 항목별로 보여준다.** 통과/미통과를 하나씩 나열해야 목표가 된다.
  boolean 하나만 넘기면 잠긴 버튼으로만 남는다.
- **속도 조절은 돈으로 한다.** 처음엔 업적 조건(만렙·손님 수·초당 수입)만 걸었더니
  손님 5종이 열리는 2시간 33분에 네 가게가 동시에 승급하고 3시간 26분에 다 끝났다.
  이 게임에서 실제로 속도를 정하는 건 돈이다(가게 값 9천→90만→1.2억→200억이
  그 증거다). 승급값을 붙이고 나서야 계단이 생겼다.

결과: 마지막 콘텐츠가 **3시간 26분 → 29시간 7분**.

| 승급 | 시점 |
|---|---|
| 대장간 쇠 · 필방 고운 | 2시간 33분 |
| 지물포 고운 | 2시간 41분 |
| 옹기점 오지 | 3시간 55분 |
| 약재상 묵은 | 4시간 49분 |
| 대장간 강철 | 5시간 10분 |
| 필방 명품 | 5시간 51분 |
| 지물포 진상 | 9시간 19분 |
| 옹기점 왕실 | 19시간 53분 |
| 약재상 천년 | 29시간 7분 |

건물 모양은 축을 둘로 나눴다 — 지붕은 등급(초가→기와→단청), 금테·화분은
칸을 다 열었는가. 등급 하나로만 하면 첫 승급까지 두 시간 반 동안 건물이
전혀 안 변한다.

### 단골 — 손님이 굳어 있던 문제

손님은 마을에 온 뒤로 아무 변화가 없었다. 마지막 손님이 4시간대에 오고 나면
일곱이 그대로 굳는다. 자주 오는 손님이 단골이 되게 했다.

    뜨내기 → 낯익은 → 단골 → 귀한 → 터줏대감
    (누적 0 · 500 · 2,500 · 10,000 · 30,000개)

문턱은 실측으로 잡았다 — 토끼가 10분에 515개, 8시간에 34,000개를 사간다.
결과: **단골 승급 23회가 10분부터 11시간 23분까지** 이어진다.

올리는 건 값이 아니라 주로 **개수**다(터줏대감 ×3). 이 게임은 물건이 남아도는
쪽이 병목이라, 수요를 늘려야 쌓인 재고도 같이 빠진다. 장(場) 보너스와 같은 이유다.

가게 승급 조건의 '손님 N종'을 '단골 등급 합계 N'으로 바꿨다. 종류 수는 4시간이면
7로 굳어 조건 구실을 못 하지만, 등급 합계는 계속 자란다(60분 6 → 6시간 20).
합계 문턱은 8·18로 잡았다 — 5·7로 두면 50분에 통과해 조건이 되지 못한다.

화면에서는 단골이 **갓을 쓴다**. 등급이 오를수록 갓이 커지고 색이 진해지며,
터줏대감은 금관자를 단다. 머리 위에 글자를 얹으면 손님이 몰릴 때 다시 겹치므로
모양으로 알린다 — 예전에 금액 글자가 겹쳤던 것과 같은 이유다.

### 손님 12종과 성격 — 그리고 병목이 뒤집힌 사건

손님이 일곱이었고, `every·qty·pay`가 뒤로 갈수록 나란히 커졌다. 그래서 나중 손님이
앞 손님의 **상위호환**이었고, 일곱이든 스물이든 체감이 같았다. 축을 다섯으로 쪼갰다.

| | 오는 간격 | 개수 | 값 | 훑는 종류 | 걸음 |
|---|---|---|---|---|---|
| 까치 | 3.1초 | 2 | ×0.85 | 2 | 1.7 |
| 다람쥐 | 4.2초 | 2 | ×1.15 | 2 | 1.5 |
| 멧돼지 | 80초 | 12 | ×2.2 | **1** | 1.1 |
| 거북 | 110초 | 14 | ×7 | 3 | 0.45 |
| 호랑이 | 400초 | 34 | ×13 | 2 | 0.95 |

`spread:1`(멧돼지·곰)은 한 종류를 통째로 쓸어간다 = 진열대 한 칸이 비는 사건이다.
`speed`는 화면에서만 쓴다 — 거북이 실제로 느리게 걸어야 성격이 읽힌다.

**여기서 병목이 반대로 뒤집혔다.** 일곱이 열둘이 되자 수요가 초당 7.5개 → 14.7개로
뛰었는데 공급은 7.8개가 상한이었다. 진열대 평균 재고 31.3 → **0.2/40**,
못 채우고 돌아간 손님 64%. 여태 "물건이 남아돈다"가 문제였는데 하루아침에
"물건이 없다"가 됐다.

간격에 **일괄 배수**를 거는 건 답이 아니었다. ×2.6을 걸면 앞 네 시간은 37~38로
그대로 꽉 차고 뒤 네 시간만 1로 비는 **두 국면**이 남는다 — 초반 손님까지 뜸해져
심심해지기만 한다. 손님마다 따로 잡았다: **앞은 자주, 뒤 큰손은 드물게.**

    4.6 · 3.1 · 4.2 · 17 · 32 · 50 · 80 · 120 · 110 · 145 · 260 · 400 (초)

|  | 1h | 2h | 3h | 4h | 5h | 6h | 7h | 8h | 못채움 | 꽉참 | 텅빔 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 손보기 전 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 64% | 0% | 87% |
| 일괄 ×2.6 | 37 | 38 | 38 | 36 | 5 | 1 | 1 | 1 | 15% | 11% | 23% |
| **지금** | 25 | 31 | 37 | 32 | 31 | 28 | 2 | 2 | **4%** | 6% | 10% |

씨앗 다섯 개에서 편차 ±0.3. 앞의 여섯 시간이 20~37 사이에 머문다.

**7~8시간대가 2로 주저앉는 건 남겨둔 문제다.** 단골 배수는 ×3까지 계속 자라는데
생산은 `timeFloor`에 막혀 천장이 있다. 콘텐츠 소진(7시간56분) 이후 구간이라
환생 루프가 들어갈 자리와 정확히 겹친다 — 조절이 아니라 구조로 풀 문제다.

**앞 세 마리는 손대면 안 된다.** 토끼를 4.6 → 4.0으로만 줄여도 평균 재고가
15.5 → 7.0으로 반토막 난다. 자주 오는 손님이 총수요를 지배하기 때문이다.

### 단골 20성 — 그리고 눈덩이 사고

5단계 × 12손님 = 작은 사건 60번이었다. 20단계면 240번이다. 호칭은 조선 벼슬로
올라가다 20성에서 **상감마마**가 된다(1 뜨내기 · 5 단골 · 10 터줏대감 · 16 어사또 ·
18 판서 · 19 정승 · 20 상감마마).

**세는 단위를 두 번 갈아엎었다.**

1. *누적 개수* — 손님마다 사가는 속도가 8배까지 벌어진 지금은 못 쓴다.
   까치는 순식간에 상감마마가 되고 호랑이는 영원히 뜨내기다.
2. *속도로 나눈 개수* — 여기서 **눈덩이**가 굴렀다. 등급이 오르면 한 번에
   사가는 개수가 늘고, 그러면 다음 등급이 더 빨리 온다. 두 시간 만에 12성이
   됐고 진열대가 텅 비었다(못채움 28%).
3. **방문 횟수** ← 지금. 등급이 올라도 안 늘어나므로 눈덩이가 안 생긴다.
   `at`(초)을 그 손님의 `every`로 나눠 횟수로 바꾼다. 그래서 **누구든 마을에
   온 뒤 같은 시간이 흐르면 같은 성**이 된다.

배수도 뒤집었다. 예전엔 개수를 ×3까지 밀고 값은 ×1.35만 올렸다 — 그때는
물건이 남아도는 게 병목이었으니까. 손님이 열둘이 된 지금은 정반대라
**개수와 값을 똑같이 ×2.4**로 간다. ×4.2로 두고 재보니 못 사고 간 손님이 32%였다.

| qty 꼭대기 | 평균 재고 | 못채움 | 텅빔 |
|---|---|---|---|
| ×4.2 | 0.9 | 32% | 53% |
| ×3.2 | 3.6 | 13% | 34% |
| **×2.4** | **24.0** | **1%** | **4%** |
| ×1.8 | 36.6 | 0% | 0% |

승급 조건의 '단골 등급 합계'도 8·18 → **32·135**로 다시 잡았다(20성 × 12명이라
최대가 228로 늘었다). 135를 150으로 올리면 대장간과 필방이 같은 분에 승급해
사건이 뭉치고, 175로 올리면 10시간 안에 아예 안 열린다.

콘텐츠 소진: 7시간56분 → **9시간24분**.

### 등장이 시계 같던 문제 — `wild`

`every`만 있으면 호랑이도 **400초마다 정확히** 온다. 시계처럼 오는 희귀 손님은
희귀하게 느껴지지 않는다. 손님마다 `wild`(0~1)를 줬다.

- `wild: 0` — every 그대로. 토끼·까치가 여기 있다. 길에 사람이 끊기면 안 되니까
- `wild: 1` — "초당 1/every 확률로 온다"와 같은 것(지수분포). 호랑이가 여기 있다
- 사이는 섞는다

**평균 간격은 어느 쪽이든 every 그대로**다. 그래서 위에서 잰 밸런스 표를 다시
잴 필요가 없다 — 흔들리기만 한다. 씨앗별 편차가 커진 것(평균 재고 21.7~25.5)이
그 흔들림이고, 이건 없애야 할 게 아니라 넣으려던 것이다.

### 나쁜 놈들 — 화면을 볼 이유

이 게임에는 **지켜볼 이유가 없었다.** 켜 두면 알아서 돌아가고 가끔 강화만 누르면
됐다. 방치형에서 제일 귀한 게 "지금 화면을 봐야 하는 이유"다.

쥐가 평균 75초마다, 까마귀가 평균 110초마다 나타난다(`wild:1`이라 언제일지
모른다). 정해진 시간 안에 누르면 잡고, 놓치면 조금 물고 달아난다.

**설계에서 딱 하나 지킨 것: 놓쳐도 손해가 크면 안 된다.**

방치형 유저는 폰을 꺼놓고 잔다. 아침에 "밤새 다 털렸다"가 뜨면 그 자리에서
지운다. 그래서 벌이 아니라 **상**으로 뒤집었다. 실측:

```
8시간 · 한 번도 안 잡았을 때 — 잃는 것이 누적매출의 0.62%
8시간 · 매번 잡았을 때       — 버는 것이 누적매출의 9.3%
```

**놓쳐서 잃는 것보다 잡아서 버는 것이 열다섯 배 크다.** 안 보고 있으면
잃는 게 아니라, 보고 있으면 버는 구조다. 폰을 꺼놓고 자도 게임은 멀쩡하다.

화면에서 두 가지를 챙겼다.

- **남은 시간 고리.** 몇 초 안에 눌러야 하는지 안 보이면 그냥 지나친다
- **마을 안쪽으로 달아난다.** 처음엔 가까운 마을 끝으로 보냈는데, 대장간처럼
  가장자리에 붙은 가게에서는 화면 밖으로 반쯤 잘린 채 서 있어 누를 수가 없었다
- 판정 반경 30~32 — 손가락은 뭉툭하고 놈들은 뛰거나 날고 있다

한 가지 알아둘 것: 4시간 무렵 곡선이 제일 가파른 구간에서는 "매번 잡음"이
누적매출을 열 배로 벌린다. 쥐가 센 게 아니라 그 시점의 곡선이 가파른 것이다 —
잡으면 **10분쯤 앞서간다**는 뜻이고, 활동 보상으로는 그 정도가 맞다.

#### 까마귀 — 같은 걸 두 번 만들지 않기

쥐를 색만 바꿔 내면 두 번째는 아무 느낌도 없다. 갈라놓은 축이 셋이다.

|  | 쥐 | 까마귀 |
|---|---|---|
| 훔치는 것 | 진열대 **물건** | 전대의 **엽전** |
| 어디에 | 그 가게 앞, 땅 | **보이는 화면을 가로질러**, 하늘 |
| 시간 | 5.5초 | 4.0초 |

엽전은 **가진 돈의 비율이 아니라 초당수입 2초치**로 잡았다. 비율로 하면
아껴 모은 사람만 크게 털린다 — 모으는 게 벌이 되면 안 된다.

까마귀를 화면 기준으로 날린 이유: 쥐처럼 가게에 묶어 두면 마을 다른 데를
보고 있을 때 아예 못 본다. 하늘에 있으니 무엇에도 안 가리고, 마을 어디를
보고 있든 눈앞을 지나간다.

한 가지 더 — **일정한 속도로 지나가면 시간의 절반을 화면 밖에서 쓴다.**
4초를 줘도 실제로 누를 수 있는 건 3.4초였다. 들어올 땐 빠르게, 가운데선
천천히 맴돌다, 나갈 때 다시 빠르게 나가도록 곡선을 넣었다.

#### 지금 수치 (8시간, 안 잡았을 때 대 매번 잡았을 때)

```
쥐      334마리   잃는 것 0.17%   버는 것 3.4%
까마귀  233마리   잃는 것 0.45%   버는 것 5.8%
─ 합계           잃는 것 0.62%   버는 것 9.3%  (15배)
```

**한 번에 한 마리만** 나온다. 둘이 겹치면 화면 위 알림 띠가 서로를 밀어내
어느 쪽도 안 읽힌다.

#### 삽살개 — 자리를 비운 사이를 맡는다

나쁜 놈은 "보고 있으면 이득"이지만 하루 종일 볼 수는 없다. 삽살개는 그 빈 곳을
메운다. 마을 어귀 오른쪽에 개집이 서고, 200만냥에 들인다.

**여기서 제일 조심한 것: 개가 더 잘 잡으면 안 된다.** 개가 사람보다 나으면
아무도 화면을 안 보게 되고, 그러면 나쁜 놈을 넣은 이유가 통째로 사라진다.
그래서 두 겹으로 깎았다 — 무는 확률 60%, 벌금은 직접 잡을 때의 1/6(3배 대 20·13배).

```
10시간 · 개 없이 자리 비움     -0.87%
10시간 · 삽살개만 믿고 비움    +1.2%
10시간 · 직접 매번 잡음       +13.9%   ← 개의 11배
```

개를 들여도 **직접 잡는 게 열한 배 낫다.** 개는 "안 보면 손해"를 지워줄 뿐,
"보면 이득"을 대신해 주지는 않는다.

화면에서는 개가 개집 앞에 앉아 졸다가, 나쁜 놈이 나오면 **일어나 들썩이며 '멍!'**
한다. 지금 지켜주고 있다는 걸 글자 없이 알리는 장치다.

지금은 돈으로 산다. 뽑기·인앱구매가 들어오면 그쪽으로 옮긴다 —
`sim.buyGuard()` 하나만 갈아 끼우면 된다.

### 주인공 너구리 — 세웠다가, 뺐다

게임 이름이 **너구리 만물상**인데 화면에 너구리가 없어서 '사장 너구리' 한
마리를 세웠다. 마을을 돌며 방금 판 가게로 가서 물건을 건네게 했다.

**그리고 뺐다.** 이유가 둘이다.

1. **사장은 플레이어다.** 화면 밖에서 마을을 보고 있는 사람이 곧 사장인데,
   그 사람의 분신이 필드를 종종거리면 내가 둘이 된다.
2. **하는 일이 없었다.** 초당 0.9건씩 팔리니 어느 가게에도 제때 못 닿고
   왔다 갔다만 했다.

대신 **가게마다 점장**을 두고, 그 점장이 만들고 팔고 존다. 사장은 화면에
안 나온다 — 방치형 타이쿤의 표준 구도이기도 하다(사장은 매니저를 고용한다).

이렇게 하면 나중에 **점장 고용·승급**이 그대로 축이 된다. 뽑기·인앱구매를
붙일 자리도 여기다.

그리고 그림 두 장(`raccoon-make` · `raccoon-sell`)은 안 버렸다 —
점장의 기본 그림으로 그대로 쓴다.

### 가게마다 점장 너구리 — 만들고 · 팔고 · 존다

주인공 하나가 마을을 왔다 갔다 하니 **가게에 사람이 없는 시간**이 생겼다.
가게마다 점원 너구리를 하나씩 붙였다.

점장은 자기 가게 안에서 **자리를 두 개** 오간다.

    작업대 자리   가게 안쪽 0.18/0.82    평소
    손님 자리     0.52/0.48              손님이 오면 몇 걸음 나온다
    (손님)        길 쪽 0.74/0.26        41px 떨어진 자리

64px를 130px/s로 가면 0.5초다. 손님이 서 있는 2.3초, 물건이 건네지는 건
1.1초 시점이니 **항상 제때 닿는다.** 예전 사장 너구리는 마을 반대편에서
출발하느라 한 번도 제때 못 닿았고, 그래서 '파는 중' 자세가 실제로는 화면에
나온 적이 없었다.

세 모습이다: **만듦**(작업대 옆) · **팖**(나가서 건넴) · **졺**(진열대가 꽉 참).

#### 계기판은 캐릭터에게 시키지 않는다 — 좌판의 고리

처음엔 점원이 **실제 제작 주기(1.2~3.6초)에 맞춰 두드리게** 했다. 정보는
정확했지만 **캐릭터에게 계기판을 시킨 셈**이었다. 다섯 마리가 각자 다른
박자로 쉬지 않고 움직이는데, 정작 "다음 게 언제 나오나"는 여전히 눈으로
세어야 했다.

정확한 진행은 **좌판 칸의 고리**가 맡는다. 칸 하나가 두 가지를 같이 보여준다.

    가운데 숫자 = 지금 쌓인 개수
    두른 고리   = 다음 한 개까지 얼마나 남았나 (prog / craftTime)

이게 강화를 처음으로 **눈에 보이게** 만든다 — 지금까지 강화는 숫자만 바뀌고
화면은 그대로였다. 이제 고리가 눈에 띄게 빨리 돈다.

너구리는 계기판에서 풀려나 **분위기만 맡는다.** 2.4초짜리 느긋한 한 박자로
움직이고, 제작 주기와는 아무 상관이 없다.

고리에서 두 번 손본 것:

- **막힌 칸은 빨간 고리를 한 바퀴 다 그린다.** 꽉 차면 생산이 멈추고 고리도
  멈추는데, 하필 거의 빈 상태로 멈추면 '막혔다'가 아니라 '방금 시작했다'로
  보인다. 조는 점원과 같은 신호를 색과 모양 둘 다로 준다
- **바탕 고리를 진하게(.18 → .32).** 좌판이 차면 배경이 주황으로 바뀌는데
  옅은 바탕 고리는 거기서 통째로 사라졌다

#### "생산을 늦춰야 하나"에 대한 답 — 안 늦춰도 된다

다섯이 동시에 두드리면 정신없을까 봐 실제 제작 시간을 재봤다.
(지금은 고리가 그 일을 맡지만, 이 숫자는 고리의 회전 속도라 그대로 유효하다.)

| | 1분 | 15분 | 60분 | 120분 | 240분 |
|---|---|---|---|---|---|
| 대장간 | 2.82초 | 1.94 | 1.94 | 1.23 | 1.23 |
| 필방 | — | 2.71 | 2.71 | 1.72 | 1.72 |
| 약재상 | — | — | 3.61 | 3.61 | 2.51 |

**제일 빠를 때가 1.2초에 한 번**이다. `LEVEL.timeFloor`(0.40)가 이미 천장을
막고 있어서 아무리 강화해도 이보다 빨라지지 않는다. 1~3초에 한 번 두드리는
건 느긋한 박자다 — **생산을 늦출 이유가 없다.**

가게마다 위상을 어긋나게 뒀다(`i * 0.37`). 다섯이 똑같이 맞춰 움직이면
기계처럼 보인다.

#### 조는 점원은 귀여움이 아니라 알림이다

진열대가 꽉 차면 `sim`이 실제로 생산을 멈춘다(`stock >= STOCK_CAP`).
**그 사실이 화면에 안 보여서 유저가 눈치를 못 챘다** — 어느 칸이 막혔는지
알려면 숫자를 하나씩 봐야 했다.

이제 그 가게 점원이 존다(💤). 조는 너구리가 보이면 "저기 물건이 남아돈다,
팔 곳을 늘리거나 다른 걸 강화해라"는 뜻이다. 8시간 중 진열대가 꽉 찬 시간이
5~6%라 자주 뜨지도 않는다.

#### 칸마다 한 마리(대장간 4마리)는 왜 안 했나

자리는 **넣을 수는 있다** — 188px에 4마리를 0.12/0.37/0.62/0.87로 놓으면
간격 47px, 28px짜리라 19px씩 남는다. 다만 그러면 손님과 주인이 설 자리가
없어져 줄을 둘로 나눠야 한다.

진짜 걸림돌은 자리가 아니라 **그림이다.** 칸마다 다른 점원을 그리면
17종 × (일함·졺) = **34장**이 더 든다. 지금 계획 전체가 34장인데 그게 두 배가 된다.

그래서 순서를 이렇게 잡았다:
1. **지금** 가게마다 1마리 (그림 5종 × 2 = 10장)
2. **나중** '점원 고용'으로 칸마다 늘리기 — 그때는 그림값이 비용이 아니라
   **콘텐츠**가 된다. 돈을 모아 두 번째 대장장이를 들이는 것이 곧 성장이다

#### "주문 받고 생산"은 다른 장르다

지금은 `만든다 → 쌓인다 → 팔린다`다. 주문부터 받으면 `주문 → 만든다 → 준다`가
되는데, 이건 방치형이 아니라 **주문 처리 게임**(디너대쉬 계열)이다.
자리를 비우면 주문이 밀리므로 방치가 성립하지 않고, 여태 맞춘 밸런스
(진열대 평균 22~25, 못채움 1%)가 통째로 무의미해진다.

넣는다면 기본 순환이 아니라 **특별 주문** 같은 사건으로 얹는 게 맞다 —
"산삼 20뿌리를 30분 안에" 같은 것. 그건 지금 구조 위에 그대로 올라간다.

### 손님이 자판기처럼 굴던 문제 — 주문 · 전달 · 퇴장

"명령 주입된 손님들이 가게 방문해서 호다닥 사가는 기분"이라는 말이 정확했다.
도착하는 **그 프레임에** 재고가 빠지고 엽전이 튀고 0.7초 뒤 떠났다.
장사가 아니라 자판기였다.

세 박자로 나눴다.

```
0.0초   도착. 머리 위에 "3개 · 2종" 말풍선 — 주문
1.1초   주인 너구리가 건넨다. 이때 재고가 빠지고 엽전이 튄다
2.3초   짐을 지고 떠난다
```

걸음도 늦췄다: `155 → 95` (곁걸음 175 → 105, 퇴장 150 → 92).
**손님 수가 아니라 한 명이 오래 머무는 것이 마을을 붐비게 만든다.**

#### 자리를 늘려야 했다

한 명이 화면에 남는 시간이 5~7초에서 10~12초로 늘었다. 자리가 여섯이던
시절 그대로 두면 거래가 줄줄이 잘린다. 실측으로 맞췄다.

| 자리 | 실제 판매 | 화면에 나온 손님 | 동시 최대 |
|---|---|---|---|
| 10 | 27건 | 21명 (78%) | 11 |
| **14** | 26건 | **25명 (96%)** | 15 |

초당 판매가 0.87~0.90건이니 14자리면 거의 다 나온다. 마을 세로가 1560인데
한 화면에는 절반쯤만 보이므로 **동시에 눈에 들어오는 건 예닐곱**이다.
넘치면 그 거래는 화면에 안 나온다 — 계산은 이미 끝나 있으니 손해는 없다.

#### 말풍선을 두 번 옮겼다

- 글자는 **개수만.** 품목 이름까지 넣으면 손님이 몰릴 때 서로 겹친다 —
  예전에 금액 글자로 똑같은 실수를 했다
- **깊이 정렬 밖(z=9e8)에 그린다.** 줄에 세우면 앞에 선 것에 가려 못 읽는다
- **가게 반대쪽(길 쪽)으로 20px 비껴 띄운다.** 머리 바로 위에 두면
  좌판 칸을 덮어 재고 숫자가 안 보인다

### 뒤뚱뒤뚱은 그림이 아니라 코드로

"이동할 때 뒤뚱뒤뚱 모션이 있으면 좋겠다, 꼬리가 흔들리면 그렇게 보이려나"
— 꼬리보다 **몸통을 기울이는 게 훨씬 세게 읽힌다.** 뒤뚱거림의 정체가
무게중심이 좌우로 넘어가는 것이라, 흔들려야 하는 건 꼬리가 아니라 몸이다.

걷는 동안 스프라이트를 **±6도 기울인다.** 위아래 들썩임과 반박자 어긋나게
둬서 두 발로 걷는 것처럼 만든다. **그림은 한 장도 안 든다.**

걷는 그림을 준다면 **두 장이면 충분하다**(왼발·오른발). 30px짜리에서
4장은 2장과 구별이 안 된다. 코드가 `-walk1`/`-walk2`를 찾고, 없으면
`-idle`, 그것도 없으면 `-work`로 내려간다. 기울기는 어느 쪽이든 그대로 얹힌다.

#### 상하좌우 — 좌우만 있으면 된다

점장이 오가는 두 자리는 **가로 64px, 세로 3px** 떨어져 있다. 사실상 좌우
운동이라 정면·뒷면은 화면에 나올 일이 없다.

손님은 길을 따라 위아래로 걷지만, 위에서 살짝 내려다본 3/4 시점에서는
**움직이는 방향과 무관하게 카메라를 보는 것이 표준**이다. 뒷모습을 넣으면
그림이 두 배가 되고 얻는 건 거의 없다. **오른쪽 보는 것 하나만** 그리고
왼쪽은 코드가 뒤집는다.

#### 자세 목록이 넷이 됐다

`-work`(작업대에서 숙임) · `-idle`(차렷, 걸어갈 때) · `-sell`(앞발 내밈) ·
`-sleep`(앉아서 졺). 지금 쓰는 임시 그림은 앞발을 가슴께에 모으고 있어
**돈을 들고 있는 것처럼** 보인다 — 걸어갈 때 쓸 차렷 자세가 따로 필요하다.

### 폰에서 얼마나 작은가 — 재고, 높이로 풀었다

"생각보다 작을 것 같다"는 걱정이 맞았다. 캔버스는 가로 480을 화면 폭에
맞추는데 폰이 375~393이라 **배율이 0.78~0.82**다. 코드에 적은 값의 8할이
화면에 나온다. 좌판 물건 이름이 **7.1 CSS px — 1.9mm**였다.

#### 넓히기로는 못 푼다

가게 폭을 재봤더니 이미 한계였다. 길이 가운데를 지나므로 양옆에 179px씩밖에
안 남는다 — 대장간 188, 넓힐 수 있는 최대 **190**. 2px이다.

#### 높이로 풀었다 — 좌판을 두 줄로

한 줄에 네 칸을 두 줄에 두 칸씩으로 바꿨다. **칸 폭이 37 → 71.**

| | 전 | 후 |
|---|---|---|
| 칸 폭 | 37px | **71px** |
| 물건 이름 | 7.1 CSS px | **11.1 CSS px** |
| 재고 숫자 | 10.3 | **15.0** |

폰에서 글씨는 11 CSS px부터 편하게 읽힌다 — **둘 다 넘겼다.**

값은 마을 길이다. 가게 높이 150 → 192, 마을 1560 → **1750**(1.12배).
스와이프가 조금 길어지는 건 감수하기로 했다.

곁들여 바꾼 것:
- 길 휨 ±24 → ±18, 폭 74 → 66 (가게 자리를 벌기 위해)
- 가게 위치를 길의 곡선에 맞춰 다시 잡았다. 길이 오른쪽으로 부풀면 왼쪽에
  자리가 남는다 — 다섯 곳 다 길과 25~48px 여유를 두고 배치했다
- 작은 건물·개집은 **가게 맞은편**으로. 가게 사이(80px)에 끼웠더니 위 가게
  점장이 서는 자리를 깔고 앉았다
- 등급을 좌판에서 현판으로(`⚒ 대장간 · 쇠`). 칸마다 '쇠곡괭이'를 넣을 이유가 없다

#### ★ 캔버스가 화면보다 46px 컸다 (오래된 고장)

이걸 재다가 발견했다. 캔버스 상자가 **위로 23px, 아래로 23px 삐져나와**
있었다. 위는 버튼 줄 뒤로 숨고 아래는 화면 밖이었다 — **마을이 계속 잘려
있었고 아무도 몰랐다.**

원인: 캔버스 크기를 처음 맞추는 시점이 머리말·버튼 줄이 채워지기 **전**이라,
그 뒤 상자가 줄어들어도 창 크기(resize)는 안 바뀌니 다시 맞출 일이 없었다.

고침: 창이 아니라 **담는 상자를 ResizeObserver로 지켜본다.** 여섯 기기에서
캔버스와 상자가 픽셀 단위로 일치하는 것을 확인했다(아이폰SE·15·갤S24·
폴드 접음/폄·아이패드).

덤으로 카메라도 고쳤다. 창이 바뀔 때 **끝에 붙어 있었으면 끝에 그대로**
붙여 둔다. 가운데만 붙잡으면 마을 어귀를 보다가 어귀에서 밀려나는데,
처음 켤 때가 정확히 그 경우였다.

#### 다음 단계는 물건 그림

이름 글자가 11.1px까지 왔지만, **물건 그림 17장이 들어오면 이름은 뺀다.**
칸이 71px이니 30×52 그림이 넉넉히 들어가고, 그림이 글자보다 훨씬 잘 읽힌다.

### 화면이 둘이 됐다 — 마을과 가게 안

"지금 상태를 지도라 칠 때, 대장간을 누르면 그 안을 보게 할 수 없나"

할 수 있고, **작다는 문제를 구조로 푸는 방법**이기도 하다.

마을 한 화면에 가게 다섯을 다 담으니 가게 하나가 화면 폭의 35%였다. 그래서
좌판 글자가 1.9mm가 됐고, 두 줄로 바꿔 겨우 11px을 만들었다. **한 화면에
하나만 보여주면 같은 물건이 저절로 2.5배가 된다.**

| | 마을 | 가게 안 |
|---|---|---|
| 재고 숫자 | 15 CSS px | **23** |
| 진행 고리 지름 | 32 | **50** |
| 점장 | 33 | **52** |

역할도 갈렸다.

    마을     어디에 무엇이 있나 · 어느 폐허를 되살릴까 · 나쁜 놈 잡기
    가게 안  이 가게에서 무슨 일이 벌어지나 · 강화 · 칸 열기 · 승급

#### 시점 — 아이소메트릭 바닥 + 정면 캐릭터

바닥을 마름모로 깔면 평면이 아니라 공간으로 읽힌다. 사람은 **지금 그림
그대로 정면**을 본다. 흔한 조합이고, 그래야 이미 그린 너구리·손님을
다시 안 그린다. 새로 그려야 하는 건 바닥·벽·작업대뿐인데 그건 지금 코드로
그린다.

#### 배치를 네 번 고쳤다

1. **방이 화면 가운데 작게 뜨고 위아래가 텅 빔** — 마름모를 화면 폭 안에
   다 넣으려 했다. 실제 이런 게임은 카메라가 방 안에 있어 벽이 잘린다.
   **화면보다 크게** 잡아 좌우가 잘리게 했다
2. **작업대 넷을 마름모 네 꼭짓점에 놓았더니** 앞쪽 것이 문·손님·점장과
   자리를 다퉜다 → **뒤쪽 두 벽에 붙이고 앞 절반을 통째로 비웠다**
3. **2:1 정통 비율로 했더니 벽에 붙인 작업대 둘이 화면 밖으로** 나갔다.
   벽을 따라 움직이면 가로가 세로의 두 배로 벌어지기 때문이다 →
   폭에 맞추고 깊이만 늘린 **눌린 아이소메트릭**(1.13:1)으로
4. **작업대 하나가 세로 108px을 써서** 앞 작업대가 뒤 작업대의 이름표를
   덮었다 → 단추 층을 없애고 이름·레벨을 한 줄에 합쳐 93px로

지금 자리는 벽을 따라 0.30과 0.70이다. 더 붙이면 뒤쪽 두 이름표가 겹치고,
더 벌리면 앞쪽 둘이 화면 밖으로 나간다.

#### 번들러에 이름 충돌 검사기를 붙였다

`scene.js`와 `interior.js`가 둘 다 `W`와 `C`를 쓰다가, 한 파일로 묶는
순간 터졌다. 파일로 나눠 쓸 때는 각자 자기 안에서만 사는 이름이지만
이어 붙이면 한 덩어리가 된다. **브라우저를 켜야만 보이는 종류의 고장**이라
`tools/bundle.mjs`가 묶기 전에 잡는다.

#### 두 화면을 잇는 것 — 0.3초 확대

화면이 툭 갈아치워지면 '다른 메뉴로 갔다'가 된다. 눌린 가게 자리에서 방이
부풀어 오르면 '그 안으로 들어갔다'가 된다. 같은 두 화면인데 이어져 있다는
느낌이 여기서 나온다.

한 건물을 이어 붙인 도면(고양이 게임들)이 우리보다 나은 점이 정확히 이
'끊기지 않음'인데, **그 값의 대부분을 0.3초짜리 확대로 산다.**

확대되는 동안은 손가락을 안 받는다 — 움직이는 자리를 누르면 엉뚱한 게 눌린다.

#### 왜 '한 건물 도면'으로 안 갔나

고양이 게임처럼 방을 여러 개 이어 붙여 하나로 만들 수도 있었다. 안 한 이유:

1. **바깥이 우리 정체성이다.** 무너진 마을, 구불구불한 길, 장승, 길을 걷는
   손님, 쥐와 까마귀, 삽살개 — 저쪽 게임들은 전부 실내다. 한 건물로 바꾸면
   이야기가 '마을을 되살린다'에서 '내 건물을 넓힌다'로 바뀌고, 동시에
   똑같이 생긴 게임이 된다
2. **크기 문제는 어느 쪽이든 똑같이 풀린다.** 둘 다 "한 방이 화면을 채우게
   당긴다"이고, 차이는 이어져 있느냐뿐이다 — 그건 확대로 샀다
3. **값이 그림에 붙는다.** 한 건물로 이으면 방마다 바닥·벽·가구가 한 결로
   있어야 한다. 저 게임들이 돈을 쓰는 곳이 정확히 거기다(수백 장)

**'도면 느낌'은 가게 안에서 낼 수 있다.** 승급하면 방이 넓어지고 칸이
늘어나게 하면 된다(돌급 한 칸 → 쇠급 작업방+창고 → 강철급 손님방까지).
이미 있는 승급 축을 그대로 쓰고, 방금 만든 가게 안 화면 위에 얹힌다.

#### 남은 것

- 방이 아직 휑하다. 화덕·물통·선반 같은 소품이 필요하다(코드로 그릴 수 있다)
- 물건 그림이 들어오면 작업대 위에 올라간다. 여기선 32×48로 그려진다
- 가게마다 방을 다르게 할지(대장간은 화덕, 옹기점은 가마) — 그림값이 붙는다

### 걱정 두 가지 — 들락날락하는 수고와 그림값

#### 1. 들어갈 이유가 없으면 안 들어가면 된다

**"일일 던전처럼 숙제가 되면 떠난다"** — 맞는 걱정이고, 처음 문구가 정확히
그 실수였다. `2가지 할 일`이라고 적었는데 그건 숙제다.

일일 던전이 의무가 되는 진짜 이유는 **만료**다. 하루 놓치면 손해가 나니까
안 하면 안 되는 게 된다. **여기선 아무것도 사라지지 않는다** — 언제 들어가도
그대로 있다. 그러니 남은 건 문구뿐이었고, 개수 대신 **뭐가 좋은지**를 적었다:
`승급!` · `새 칸!`. 초대장에 가깝게.

두 화면이 되면 오가는 수고 때문에 유저가 떠날까 하는 걱정. 대책은 하나다 —
**"혹시 뭐 생겼나" 하고 들여다볼 일을 없애는 것.**

지도의 가게 위에 **할 일 표**를 띄운다. 표가 없으면 안 들어가도 된다.

세는 것: 열 수 있는 칸 · 승급.
**안 세는 것: 강화** — 늘 할 수 있어서 표가 항상 켜져 있게 되고,
항상 켜진 표는 없는 것과 같다. 게다가 자동 강화를 사면 아예 할 필요가 없다.

10시간을 돌려 실제로 셌다.

```
가게에 들어갈 이유가 생긴 횟수: 20번
시간대별: 9 · 4 · 1 · 3 · 0 · 1 · 1 · 0 · 1 · 0
자동 강화 구입: 18분 (이후로는 강화하러 들어갈 일이 없다)
```

첫 시간에 9번은 배우는 구간이라 오히려 좋다. **그 뒤로는 한 시간에 한 번꼴**
이다. 오가는 수고가 문제가 될 빈도가 아니다.

#### 2. 가게 안 때문에 늘어난 그림은 0장이다

바깥과 안이 **같은 그림을 크기만 다르게** 쓴다. 코드에서 확인된다 —
`interior.js`가 요청하는 그림 종류가 `scene.js`와 완전히 같다
(`clerks` · `guests` · `items` · `pests` · `hero`).

| | 마을에서 | 가게 안에서 | 만드는 크기 |
|---|---|---|---|
| 점장 | 33 | 52 | `72×72` 한 장 |
| 손님 | 33 | 46 | `64×64` 한 장 |
| 물건 | 44×50 | 32×48 | `64×112` 한 장 |

가게 안의 바닥·벽·작업대는 전부 코드다.

**그리고 목록 자체를 줄였다.** 점장을 가게마다 4포즈씩 20장으로 잡아 두었던
것을 **공용 4장(필수) + 가게별 16장(선택)**으로 갈랐다.

```
필수 36장 = 점장 4 + 물건 17 + 손님 12 + 나쁜 놈 3
선택 16장 = 가게별 점장
```

네 차례로 나눠 **어디서 멈춰도 게임이 돈다.** 안 그린 것은 이모지로 나온다.

### 마을을 격자로 갈아탔다 — scene.js에서 iso.js로

"이미지만 갈아끼우면 사실상 바둑판형 마을도 될 수 있다"는 지적이 결정타였다.
고양이 게임의 본질은 실내가 아니라 **격자**였고, 격자 위에 우리 바깥 마을을
그대로 올릴 수 있다.

세로 두루마리(scene.js)의 진짜 약점은 **가게가 늘 때마다 아래로만 길어지는
것**이었다(1560 → 1750, 가게 열이면 3000). 격자는 가로로도 퍼지고, 빈 칸이
눈에 보여서 "폐허를 하나씩 채운다"가 지도 자체로 읽힌다.

- 바닥 한 칸 96×48(가로:세로 2:1 — 이 시점의 기본 비율), 마을 10×14칸
- 가게 2×2칸, 곁가게·개집 1×1칸, 건물은 전부 코드(상자+지붕) — **새 그림 0장**
- 카메라가 2차원이 됐다. 위아래만 밀던 것을 어느 방향으로든 민다
- 경제(sim.js)는 한 줄도 안 바뀌었다. 좌판 두 줄·진행 고리·주문 박자·
  단골 갓·나쁜 놈·삽살개·점장, 전부 그대로 옮겨 탔다

#### 자리는 (tx−ty)가 정한다 — 밟은 함정

격자에서 화면의 가로 위치는 tx가 아니라 **tx−ty**다. '왼쪽/오른쪽'을 tx로
갈랐다가 대장간(5,11)과 필방(1,7)이 화면에서 **정확히 같은 세로줄**(둘 다
tx−ty=−6)에 포개졌다 — 필방 좌판이 대장간 지붕 뒤로 사라졌다.

지금은 세 세로줄(−240 / 0 / +240px)에 번갈아 놓고, 같은 줄 안에서는 세로로
240px 이상 벌린다. 건물+현판+표가 230px쯤을 쓰기 때문이다. 곁가게도 같은
이유로 대장간 세로줄을 통째로 피해서 놓았다.

#### 손님은 직선으로 걷는다

세로 두루마리에선 큰길을 따라 내려오다 가게 앞에서 꺾었다. 격자에서 그
규칙을 그대로 쓰면 마을 반대편까지 도는 우회가 생긴다. 직선으로 바꿨다 —
아이소메트릭에서 직선은 어차피 비스듬한 길로 보인다.

#### scene.js는 은퇴

파일은 남겨 두되 게임은 iso.js를 쓴다. 묶음(bundle)에서도 뺐다.
가게 안(interior.js)과 0.3초 확대는 그대로 산다.

### 가게를 상자에서 마당으로 — '가게 안' 화면 은퇴

"이제 건물 안으로 들어가지 말자. 내부 간판대를 밖으로 빼자"는 결정.
지붕 덮인 2×2 상자를 **3×3 마당**으로 열어젖혔다. 안이 지도에서 다 보이니
들어갈 이유가 없고, interior.js(가게 안 화면)는 은퇴했다.

마당 아홉 칸의 쓰임:

```
가마(1) · 작업대(1) · 매대(품목마다 1, 최대 4) · 계산대(1) · 빈 바닥(2)
```

- **매대 한 칸 = 좌대 + 물건 + 재고·진행 계기 + 이름패.** "책상과 학생이
  한 칸"인 참고 게임의 문법 그대로다
- **계산대가 보인다** — 손님이 그 앞에 서고, 점장이 나와서 건넨다.
  엽전 접시까지 올려놨다
- 매대를 **직접 누르면 강화**, 잠긴 매대는 누르면 열린다. 마당의 나머지를
  누르면 가게 창(승급·자세한 숫자)이 열린다
- 1×1칸 = 96×48 가상 px (폰에서 약 20×10mm). 소수점 배치도 자유다 —
  가마는 0.56칸, 계산대는 0.68×0.4칸을 쓴다

품목이 늘어나는 날엔 **대장간2가 아니라 승급으로 마당을 넓힌다**
(3×3 → 3×4 → 4×4). 이미 있는 승급 축이 '땅이 넓어지는 것'으로 보이게 된다.
지금은 안 만든다 — 품목 4개에 3×3이면 맞는다.

#### 밟은 함정 둘

1. **매대 2×2 뭉치기.** 마당 가운데 2×2로 놓았더니 화면에서 마름모로
   포개져 앞 매대의 계기가 뒤 매대를 덮었다. **뒤 두 담벼락을 따라**
   두 개씩(ㄱ자 진열)으로 바꿨다.
2. **터치 판정 겹침.** 이웃 칸은 화면에서 48px밖에 안 떨어진다.
   '먼저 맞은 것'을 고르면 점포가 도끼 매대의 터치를 삼켰다(실제로 그랬다).
   매대는 **제일 가까운 것**을 고르고, 점포는 한 칸 옆으로 옮겼다.

### 마당이 등급 따라 자란다 — 그리고 콘셉트·가림·주문 셋 손봄

**① "대장간이 물건 4개만 팔면 섭섭하다"** — 대장간에 가위·부엌칼을 더해
품목 6개가 됐고, 매대 수를 마당 크기에 묶었다.

    무쇠급 3×3 마당 = 매대 4칸 → 참쇠급 4×4 = 6칸 → 강철급 5×5 = 8칸

대장간2가 아니라 **같은 마당이 넓어진다.** 격자라서 승급이 "땅이 커지는
것"으로 공짜로 보인다. 격자도 4분면 배치(13×18, 길이 십자로 가름)로 넓혀
마당이 5×5까지 자라도 길·이웃을 안 밟는다.

밟은 함정: 승급 조건이 "모든 칸을 만렙"인데 5번째 칸은 승급해야 생긴다 —
**서로를 영원히 기다렸다.** 실제로 10시간 내내 무쇠급에 잠겼다. 조건을
"지금 있는 매대"로 고쳤다. 그리고 손님이 매대 없는 물건을 조르면 약만
오르므로, 물어보기도 매대가 있는 물건만 하게 했다.

**② "돌부터 시작하는 콘셉트를 잘못 잡았다"** — 맞는 지적이었다. 대장장이가
돌도끼부터 시작하는 건 말이 안 된다. 등급을 **무쇠 → 참쇠 → 강철**로 바꿨다.
무쇠(부어 만든 거친 쇠), 참쇠(두드려 단련한 쇠) — 실제 조선의 쇠 등급이라
시대 콘셉트에도 맞고, 첫날부터 '무쇠곡괭이'라 대장간답다.

**③ "매장이 동물을 가린다"** — 마당을 한 덩어리로 그려서 생긴 버그였다.
마당의 z가 아래 꼭짓점이라, 마당 **안에 서 있는** 손님·점장보다 항상 앞이었다.
바닥은 맨 뒤로 보내고 **가구(가마·매대·계산대) 하나하나를 자기 발끝 z로**
깊이 정렬에 태웠다. 이제 손님이 계산대 앞에 서면 계산대를 가리고, 매대
뒤로 걸으면 매대에 가려진다 — 당연한 일이 당연해졌다.

**④ "주문 받고 생산하는 게 낫지 않나"** — 경제는 안 바꾸고 **연출만** 바꿨다.
손님이 주문하면 점장이 **그 물건의 매대에 들러 집어서** 계산대로 가져와
건넨다. '주문 받고 내온다'가 눈에 보인다. 경제까지 주문식으로 바꾸면
방치가 성립하지 않고(자리를 비우면 주문이 밀린다) 여태 잰 밸런스가 통째로
무의미해진다 — 진짜 주문제작은 나중에 '특별 주문'(산삼 20뿌리를 30분 안에)
같은 **사건**으로 얹는 것이 맞다.

### 직원 고용 — "주문 후 생산"의 진짜 정체

"주문 받고 생산"을 다시 짚어보니 원하던 건 주문식 경제가 아니라 **직원**이었다.
점장 혼자 만들고 팔다가 벅차지면 일손을 들이고, 한 마리는 생산 · 한 마리는
판매, 손이 비면 같이 만드는 것.

넣었다. 자리 수는 등급이 정한다 — **무쇠급 0명 · 참쇠급 1명 · 강철급 2명.**
무쇠급에 자리가 없는 이유: 매대 4칸은 점장 혼자로 충분하고, 마당이 6칸으로
커지는 순간이 정확히 '혼자서는 벅차지는' 순간이다. 값은 승급값÷4라 승급
직후마다 모을 목표가 하나 더 생긴다.

**효과를 정하는 데 두 번 실패했다.**

1. *생산 +22%* — 진열대의 25%가 꽉 찬 채 생산이 멈췄다. 공급·수요를 몇
   시간 재서 맞춰 놨는데 공급만 22%씩 올리면 균형이 통째로 무너진다.
2. *진열대 +10칸* ← 지금. 초반을 안 건드리고, 쌓아둔 것이 후반 큰손
   (곰·호랑이)의 싹쓸이를 받아낸다.

| | 직원 전 | 직원 후 |
|---|---|---|
| 못 사고 간 손님 | 4% | **0%** |
| 텅 빈 칸 | 5~10% | **0%** |
| 꽉 찬 칸 | 6~9% | 12% |
| 콘텐츠 소진 | 9시간43분 | **9시간49분** |

측정 함정도 하나: 상한이 40이 아니게 됐는데 도구들이 40을 '꽉참'으로 세서
꽉참 58%라는 유령 수치가 나왔다. 도구를 `capOf`로 고치니 12%였다.
**지표를 만들면 상한이 변수라는 것도 같이 기억해야 한다.**

화면: 직원은 자리를 안 옮긴다. 자기 모루만 지키고, 점장이 계산대로 불려가도
계속 만든다 — '한 마리는 생산, 한 마리는 판매'가 그림으로 보인다.
고용은 가게 창에서 하고, 살 수 있으면 지도에 '일손!' 표가 뜬다.

### 남은 문제 두 가지

1. **5시간 15분 이후 할 게 없다.** 마지막 품목(우황)이 열린 뒤로는 자동 강화가
   숫자만 올린다. 환생/이사 같은 반복 루프를 넣을지 결정해야 한다.
2. **4시간~5시간 15분 사이에 79분짜리 빈 구간이 있다.** 우황(12조)을 모으는
   시간이다. 방치형에서 '앱을 끄고 나중에 오는' 구간이라 나쁘다고만 볼 수는
   없지만, 그 앞뒤보다 유독 길다.

---

## 아래는 최초 계획 원문 (기록용)

---

## 지금 상태 진단

6시간 시뮬을 돌려 나온 숫자 세 개가 이 계획의 전부다.

| 관측 | 숫자 | 의미 |
|---|---|---|
| 재고 상한에 박혀 생산이 멈춘 품목 | 12개 중 7개 (2시간 시점) | 콘텐츠의 절반이 죽어 있다 |
| 마지막 해금 | 3시간 33분 (약재상) | 6시간이면 할 게 없다 |
| 손님 전원 등장 | 1시간 4분 | 제일 센 카드를 첫 1시간에 다 쓴다 |
| 레벨업 : 의미 있는 선택 | 1945 : 14 | 조작의 99.3%가 버튼 연타 |

### 근본 원인 — `sim.js:_buy()`

```js
open.sort((a, b) => this.price(b) - this.price(a));
const id = open[0];        // 손님이 "제일 비싼 것 한 종류"만 사간다
```

손님 한 명이 품목 **한 종류**만, 그것도 항상 최고가부터 사간다.
그래서 하위 품목은 재고가 `STOCK_CAP`(40)에 도달하고, `tick()`의

```js
if (st.stock >= STOCK_CAP) continue;   // 진열대가 꽉 차면 생산 정지
```

에 걸려 **생산까지 멈춘다.** 한 번 멈추면 영영 안 팔리므로 영구히 죽는다.

2시간 시점 실측:

```
곡괭이 lv160  재고 40/40  ✗ 죽음      부채   lv 89  재고  2/40  ✓
낫     lv150  재고 40/40  ✗ 죽음      창호지 lv 80  재고  1/40  ✓
호미   lv141  재고 40/40  ✗ 죽음      옹기   lv 71  재고  1/40  ✓
붓     lv124  재고 40/40  ✗ 죽음      사발   lv 62  재고  0/40  ✓
도끼   lv132  재고 40/40  ✗ 죽음      한지   lv 97  재고 25/40  △
먹     lv115  재고 40/40  ✗ 죽음
벼루   lv106  재고 40/40  ✗ 죽음
```

곡괭이를 160레벨까지 올렸는데 초당 수입 기여가 **정확히 0**이다.

---

## Phase 0 — 진단 도구 (선행 필수)

다른 어떤 것보다 먼저다. **지금 `balance.mjs`는 위 문제를 못 잡고 "문제 없음"이라고 보고한다.**
도구가 거짓말하는 상태로 밸런스를 고치면 고쳤는지 알 수가 없다.

### 0-1. 정지 판정 수정 — `tools/balance.mjs`

```js
if (Object.values(s.items).every((x) => x.stock >= 40)) idle++;
```

`every`라서 **전부** 꽉 차야 센다. 상위 품목은 계속 팔리므로 조건이 영원히 거짓 →
7개가 죽어 있는데도 `생산 정지 0.0%`로 나온다.

→ 전역 플래그를 버리고 **품목별로** 정지 시간을 누적한다.

### 0-2. 품목별 매출 기여도 표 추가

죽은 품목이 `0.0%`로 한눈에 보이게 한다. 이게 Phase 1의 합격/불합격 판정 기준이 된다.

```
■ 품목별 매출 기여
   곡괭이    0.0%   정지 94%    ← 죽음
   낫        0.0%   정지 91%    ← 죽음
   ...
```

### 0-3. 콘텐츠 소진 시점 출력

마지막 해금(가게·품목·손님)이 일어난 시각 = **더 할 게 없어지는 시각**.
Phase 2의 목표 수치가 여기서 나온다.

### 0-4. 진단 규칙 추가

```
if (죽은 품목이 하나라도 있음)  ⚠ 품목 N개가 재고 상한에 박혀 생산 정지
if (마지막 해금 < 플레이 시간)  ⚠ N시간 시점에 콘텐츠 소진
```

**완료 기준:** 고친 도구를 지금 코드에 돌렸을 때 위 문제 3건이 전부 ⚠로 잡혀야 한다.
안 잡히면 도구가 아직 틀린 것이다.

---

## Phase 1 — 장바구니 (핵심)

**결정: 손님 한 명이 여러 품목을 섞어서 산다.**

검토했던 대안:
- *(a) 손님별 취향* — 손님마다 가격대가 다름. 손님이 캐릭터가 되지만 `content.js` 확장 필요.
- *(c) 재료 체인* — 하위 품목이 상위 품목 재료로 소모됨. 가장 깊지만 `sim.js` 재설계 수준.

(b)를 고른 이유: **`_buy()` 하나만 고치면 되고, 죽은 품목 7개가 즉시 되살아난다.**
가장 큰 버그를 가장 작은 변경으로 잡는다. 취향·재료 체인은 이게 안정된 뒤에 얹어도 늦지 않다.

### 1-1. 분배 규칙

재고가 **많은** 품목부터 사간다. 상한에 박힌 품목이 먼저 빠져나가므로 정지가 자동으로 풀린다.

```js
_buy(g) {
  const have = Object.keys(this.items).filter((id) => this.items[id].stock > 0);
  if (!have.length) return null;

  // 재고 내림차순 — 캡에 박힌 품목을 우선 비워 생산을 재개시킨다
  have.sort((a, b) => this.items[b].stock - this.items[a].stock);

  const perItem = Math.max(1, Math.ceil(g.qty / 3));   // 손님 하나가 최소 3종류를 건드린다
  let left = g.qty, gain = 0;
  const lines = [];

  for (const id of have) {
    if (left <= 0) break;
    const n = Math.min(left, perItem, this.items[id].stock);
    if (n <= 0) continue;
    const g2 = Math.floor(this.price(id) * g.pay * n);
    this.items[id].stock -= n;
    left -= n; gain += g2; this.sold += n;
    lines.push({ item: itemById(id), n, gain: g2 });
  }

  this.money += gain;
  this.revenue += gain;
  return { guest: g, lines, gain, n: g.qty - left };
}
```

`perItem`의 분모 `3`은 시작값일 뿐이다. **Phase 0 도구로 튜닝해서 결정한다.**

### 1-2. 파급 — `index.html` 판매 메시지

반환 모양이 `{ item, n }` → `{ lines[], gain }`으로 바뀐다. 이걸 쓰는 곳:

```js
// index.html:265 부근 — 지금은 단일 품목 가정
`${s.guest.name}가 <b>${s.item.name}</b> ${s.n}개를 사갔다 · <b>${fmt(s.gain)}냥</b>`
```

→ `곰이 옹기 외 2종 12개를 사갔다 · 3.4억냥` 같은 형태로 바꾼다.
**여기를 안 고치면 화면에 `undefined`가 뜬다.** Phase 1에서 같이 처리한다.

### 1-3. 확인해야 할 부작용

- **매출 폭주.** 죽어 있던 7개가 되살아나면 초당 수입이 뛴다. 6시간 곡선을 다시 재고,
  필요하면 `STOCK_CAP`이나 손님 `qty`로 조인다.
- **최고가 우선의 상실.** "비싼 걸 사간다"는 손맛이 약해질 수 있다.
  재고 우선이 지나치면 재고 순 + 가격 순 가중 혼합으로 조정한다.

**완료 기준:** Phase 0 도구에서 **매출 기여 0.0%인 품목이 하나도 없어야** 한다.

---

## Phase 2 — 콘텐츠 수명

Phase 1이 끝난 뒤 곡선을 다시 재고 착수한다.

- **손님 등장을 6시간에 배분.** 지금은 `GUESTS`의 `at` 문턱이 낮아 1시간 4분에 7명이 다 온다.
  방치형에서 제일 강한 해금 이벤트를 초반에 다 소모하는 셈이다.
- **약재상 품목 보강.** 지금 2개뿐 (다른 가게는 3~4개). 마지막 가게가 제일 빈약하다.
- **엔드게임 판단.** 마지막 해금 이후 남는 게 레벨업뿐이다.
  환생/이사 루프가 필요한지 여기서 결정한다.

---

## Phase 3 — 조작 피로

6시간에 레벨업 **1945회**. 손가락 노동이다.

- 일괄 레벨업 버튼(×10 / 최대), 또는
- 자동 레벨업 해금 → 플레이어는 "무엇을 열지"만 결정하게 한다

Phase 1·2로 곡선이 바뀌면 레벨업 횟수도 같이 바뀌므로 **순서상 마지막이어야** 한다.

---

## Phase 4 — `core/` 정리

| 파일 | 상태 | 처리 |
|---|---|---|
| `store.js` | ✅ 사용 중 | 유지 |
| `juice.js` | ✅ 사용 중 | 유지 |
| `telemetry.js` | ⬜ 미연결 | **연결** — 해금·이탈 지점 계측 |
| `engine.js` | ⬜ 미연결 | **삭제** — 캔버스 엔진, 이 게임은 DOM 렌더 |
| `ui.js` | ⬜ 미연결 | **삭제** — 광고 오버레이, 붙일 단계 아님 |

---

## 순서 요약

```
Phase 0  진단 도구        ← 선행 필수. 도구가 거짓말하면 나머지가 무의미
   ↓
Phase 1  장바구니         ← 가장 큰 버그. 죽은 품목 7개 부활
   ↓
Phase 2  콘텐츠 수명      ← 곡선이 바뀐 뒤에 재측정
   ↓
Phase 3  조작 피로        ← 곡선 확정 후에야 횟수가 의미 있음
   ↓
Phase 4  core/ 정리       ← 독립적. 아무 때나 가능
```

---

## 2026-08-19 · 계산대 규칙 — 돈·장부의 타이밍, 계산 멈춤, 기다리는 손님

유저 보고 세 가지를 한 번에 고쳤다.

**1) 돈과 장부가 손님보다 먼저 움직였다.**
sim은 계산을 즉시 끝내는데 화면의 손님은 아직 걸어오는 중이라,
"오기도 전에 사갔다고 나온다"가 됐다 — 유저 눈엔 그냥 버그다.
경제는 그대로 두고 **보여주는 쪽만 늦췄다**:
- 위쪽 엽전 표시는 `sim.money − village.inTransit()` — 계산 연출이 아직
  안 끝난 몫을 빼고 보여준다 (`inTransit()`은 걷는 중인 손님들의 미지급 합).
- 아래 장부(로그)는 sim이 아니라 **화면이 쓴다** — 손님이 계산대에서
  물건을 받는 순간 `village.onDelivered` 콜백이 울린다.
  로그 품목명도 등급 붙은 이름(무쇠곡괭이)으로 바뀌었다.

**2) 점장 혼자인데 생산과 판매를 동시에 했다.**
`content.js`의 `SERVICE.servePause`(1.2초): 계산이 한 번 끝날 때마다
직원 0명인 가게는 그 시간만큼 생산이 멈춘다(`sim._hold`).
직원을 들이면 계산 중에도 직원이 계속 만든다 — 고용의 이유가 하나 더 생겼다.
(손님이 주문을 기다리는 동안은 안 멈춘다 — 그때 점장은 그 물건을 만드는 중이다.)

**3) 물건이 없으면 손님이 그냥 사라졌다.**
이제 **기다리는 주문**이 있다 (`sim.orders`):
- 모자란 개수 × 생산 시간(+앞 주문 몫)이 참을성(`SERVICE.patience` 6초)
  이하면 손님이 기다린다. 갓 만든 물건은 진열대보다 주문 먼저 받는다.
- 넘으면 💢 남기고 그 매대만 포기한다 — 재고는 안 건드린다.
  전부 포기하면 가게 앞까지 왔다가 빈손으로 돌아간다(💢 연출).
- 돈·매출·단골 방문은 전부 물건이 다 나온 순간(`_settle`)에 움직인다.
  참을성×2를 넘기면 나온 만큼만 계산하고 보낸다.

**측정 (10시간, 씨앗 1):**

| 항목 | 값 |
|---|---|
| 기다렸다 받아간 방문 | 10.9% (평균 6.1초) |
| 빈손 💢 | 0.6% |
| 생산 정지(꽉 참) | 16~37% — 전엔 곡괭이 53%가 최악, 지금 최악 37% |
| 콘텐츠 소진 | 7시간46분 (전 9시간49분) |

재고 0에서 돌아가던 손님이 이제 주문으로 사가므로 **후반이 빨라졌다**.
소진이 2시간 당겨진 건 나쁜 신호가 아니라 페이즈2 품목(다른 가게들 이름만
지으면 됨)을 넣을 이유가 하나 더 생긴 것.

진단에 빈손 8% 초과 경고를 추가했다 — 참을성·생산 수치를 만질 때 이 표를 볼 것.

### 2026-08-19 · 직원 최대 4명 (유저 제안)

참쇠 1·강철 2였던 직원 자리를 **참쇠 2 · 강철 4**(등급×2, `STAFF.max`)로 늘렸다.
3·4번째 직원 값은 한 명마다 30배씩 뛴다 — 10시간 측정에서 아무도 3번째까지
못 갔고, 그게 의도다. 콘텐츠가 다 열린 뒤에도 살 게 남는 장기 목표.

측정(10시간, 씨앗 1): 콘텐츠 소진 7:46 → **8:42** (2번째 직원들이 돈을 빨아들여
승급이 알맞게 늦어짐). 계산대·빈손·생산정지 수치는 그대로 — 초반은 안 건드렸다.

---

## 2026-08-19 · 돈 단위 개편 — 경(京)에서 억(億)으로 (유저 지적)

"몇 시간 만에 조 단위가 나오는 건 현실감이 없다"는 유저 지적. 맞는 말이다 —
10시간 누적매출이 1.31경이었다. 폭주의 범인은 곱셈 네 겹:
강철 승급 ×6,000 · 레벨당 +50% · 25레벨마다 ×2 · 품목 사다리 ×1,000만(곡괭이→우황).

**네 겹을 전부 눌렀다**: 승급 [1,40,6000]→[1,4,16] · 레벨당 +12% ·
25레벨마다 ×1.25 · 우황 기본가 1.15억→6.5만. 대신 **모든 관문의
'수입 몇 초치' 무게는 기준판에서 옮겨와** 페이스를 지켰다.

방법: 기준판 10시간을 돌려 관문 42개의 (시각, 수입 초치)를 기록하고
(`calibrate.mjs`), 새 곡선 위에서 `새값 = √(지금값 × 목표값)` 감쇠 반복
(`iterate.py`)으로 8판을 수렴시켰다. 진자처럼 흔들리다 평균 어긋남 17%에 정착.

| | 개편 전 | 개편 후 |
|---|---|---|
| 10시간 누적매출 | 1.31경 | **1,229억** |
| 끝 초당 수입 | 2,687억 | **391만** |
| 최대 표시 단위 | 경 | **억** (최고가 목표 = 약재상 천년급 1,000억) |
| 콘텐츠 소진 | 8:42 (+1:17 공백) | **9:22 (+0:37 공백)** |
| 계산대 (기다림/빈손) | 10.9% / 0.6% | 10.8% / 1.1% |

직원값도 같이 손봤다(유저 지적 둘째): 첫 직원이 수입 36초치였다 →
**첫째 = 승급값 ×3 (≈7분치), 둘째 = 2차 승급값 절반 (≈20~40분치)**,
셋째·넷째는 ×3·×9로 10시간 밖의 목표. `STAFF.costMul = [3, 0.5, 3, 9]`.

옛 저장과 물가가 안 맞아 저장 열쇠를 `rs:save2`로 바꿨다 — 전원 새 출발.
씨앗 1·2·3 모두 진단 통과(소진 경고뿐, 9:10~9:22).

---

## 2026-08-20 · 머리 위 표시 정리 — 주문표·매상 표·손해 표

**1) 주문표: '5개 · 2종' → ⛏️3 🪴3 🌾1**
'2종'은 무엇을 사가는지 안 알려준다. 알려면 아래 장부를 봐야 하는데,
그러면 머리 위에 띄운 뜻이 없다. 품목마다 `icon`(이모지)을 붙이고
그림과 개수를 그대로 적는다. 세 가지까지 적고 나머지는 `+N`으로 줄인다 —
넷을 넘기면 말풍선이 손님보다 커져 길을 덮는다.
그림(`art/items/*.png`)이 준비되면 이모지 자리에 그대로 들어간다.

**2) 점장이 가게 밖에 서 있었다 · 계산대가 점장을 덮었다**
둘은 한 뿌리였다. 계산대만 옛 z(마당 끝 `S.y`)로 세워져 있어서
마당 **안**의 점장·손님보다 항상 앞이었다(가구를 하나씩 세운 이유가 바로
이건데 계산대만 빠져 있었다). 게다가 점장의 계산 자리가 `cc+(-34,+12)` —
마당 밖 길가였다. 계산대를 제 발끝 z로 세우고, 점장을 손님 자리의
정반대(`cc+(-32,-16)`, 마당 안)로 옮겼다. 3·4·5칸 마당 전부에서
**점장 마당 안 · 손님 길가 · 깊이 순서 점장 < 계산대 < 손님**을 계산으로 확인했다.
이제 점장은 어떤 상황에서도 제 마당을 안 벗어난다(작업대·매대·계산대 전부 안쪽).

**3) 지붕 위 층 규칙 — 화면이 복잡해지는 걸 막는 방법**
매상 표가 '새 칸!' 배지와 **똑같은 높이**(N.y−100)를 노려 서로 덮고 있었다.
가게가 다섯이 되면 그게 다섯 배가 된다. 자리를 옮기는 대신 **층을 못 박았다**:

```
현판(이름) → 배지(할 일) → 매상 표(방금 번 돈) → 손님 질문
계속 있는 것이 아래, 잠깐 있는 것이 위. 한 층이 비면 위층이 내려온다.
```

지붕 위 빈 하늘은 매대도 동물도 안 가리는 유일한 자리다. 계산대 위에도
띄워 봤지만 매대 이름과 엽전 연출을 덮어서 되돌렸다(`_roofBase`).
**표가 늘지 않는 진짜 장치는 합산이다** — 같은 가게의 연속 판매는 한 장에
숫자만 커진다. 가게가 몇이든 가게당 최대 한 장.

**4) 엽전 → 부호 → 숫자, 그리고 손해(−)**
`+🪙1,387` → `🪙 +1,387`. 도둑이 털어가면 붉은 `🪙 −290`이 그 가게 지붕에
뜬다(엽전을 물어간 까마귀는 날아간 자리에). 삽살개가 물어오면 `🪙 +N`.
`sim.tick()`이 `pests[]`로 넘긴다 — 판매(`sales`)·주문완료(`done`)와 같은 방식.
손해 표는 2.1초로 조금 더 오래 남긴다: 드물게 일어나고, 놓치면
도둑을 잡을 이유 자체가 안 생긴다.

17분 소크(도둑 9회·주문 최대 8건·손님 최대 15마리) 오류 0.

---

## 2026-08-20 · 마을 다시 그리기 — 길로만 다니고, 계산대는 길가에

두 가지 보고가 사실은 **한 뿌리**였다.
"손님이 길 아닌 곳으로 마구잡이로 온다" + "대장간 갔다가 필방 가면 대장간을 뚫고 지나간다".

**뿌리 1 — 계산대가 길에서 제일 먼 자리에 있었다.**
`SHOP_T`는 마당의 **뒷** 모서리였고 마당은 앞으로 자랐다. 계산대는 늘 앞
모서리라, 승급할 때마다 계산대가 길에서 한 칸씩 멀어졌다. 3칸 마당에서는
길에 닿지도 않았다. **뒤집었다** — `SHOP_T`가 이제 마당의 **앞(길 쪽)
모서리**이고, 마당은 길 반대쪽으로 자란다. 가게를 넓힌다고 가게 앞이
옮겨 다니면 안 된다. 넓어지는 건 뒷마당이다.
(`_org(i)`가 뒷 모서리를 계산해 준다. SHOP_T를 쓰던 12곳을 전부 이걸로 바꿨다.)

**뿌리 2 — 손님이 목적지까지 직선으로 걸었다.**
세로 두루마리 시절의 규칙이 격자에 그대로 남아 있었다. 이제 **길칸만 밟는다**:
너비우선탐색으로 길을 찾고(길칸 70여 개, A*까지 갈 일이 아니다), 꺾이는
칸만 남겨 걸음이 잘게 끊기지 않게 하고, 길 모양은 안 변하니 한 번 찾은
길은 넣어 두고 다시 쓴다(`_route`/`_points`/`_send`/`_follow`).

**새 지도 (15×18)**
```
큰길 두 줄(세로 5·6열, 12·13열) · 골목 두 줄(가로 5행, 11행)
→ 여섯 구획. 가게 다섯 + 가운데 장터 하나.
가게는 모두 제 구획의 길 쪽 모서리에 앞을 두고 뒤로 자란다.
```
| | |
|---|---|
| 손님 서는 칸이 길인가 | 다섯 가게 전부 ✓ |
| 마당이 길을 밟나 | 없음 ✓ |
| 정문·가게끼리 다 이어지나 | 전부 ✓ |
| 화면에서 겹칠 뻔한 가게 짝 | 2쌍 → **1쌍**(필방·지물포, 세로 312px) |
| 강철급 전원 6분 30초 실측 | 손님 370명 · **길 이탈 0%** · 갇힘 0 |

작은 건물 넷은 가운데 장터로 모았다 — 예전 자리는 화면에서 대장간 마당
모서리에 얹혀 보였다. 스물다섯 칸을 전부 재서 서로 136px 이상 벌어지는
네 칸을 골랐고, 삽살개는 마을 오른쪽 풀밭(마당에서 193px)으로 옮겼다.

---

## 2026-08-20 · 마당 생김새 넷 · 줄 서기 · 고용 잠금 해제

**1) 가게가 다 똑같이 생겼다 → 마당 생김새 A/B/C/D**
계산대는 늘 마름모 아래 꼭짓점, 매대는 늘 뒤 두 담벼락이었다. 길이 둘로
늘어도 가게는 한 종류였다. 네 가지로 나눴다:

| | 계산대 | 매대 | 가마 |
|---|---|---|---|
| **A 마주보기** (대장간) | 앞 꼭짓점 | 뒤 두 변(Λ자) | 뒤 꼭짓점 |
| **B 왼쪽살림** (필방) | 오른 변 가운데 | 왼쪽 두 변 | 오른 꼭짓점 |
| **C 길가진열** (옹기점·약재상) | 오른 꼭짓점 | 오른쪽 두 변 — 길에서 물건이 보인다 | 왼 꼭짓점 |
| **D 옆문** (지물포) | 왼 변 가운데(골목 쪽) | 오른쪽 두 변 | 뒤 꼭짓점 |

**담은 넷 다 뒤 두 변에만 세운다.** 앞쪽에 담을 세우면 마당 안이 가려지고
깊이 순서까지 뒤집어야 해서 얻는 것보다 잃는 게 크다 — 생김새는 담이 아니라
**계산대·매대·가마의 자리**로 가른다. 계산대는 어느 생김새든 반드시
길에 닿는 변에 둔다(BR은 항상 큰길, BL은 가로 골목이 있는 가게만).
4종 × 3등급 = 12가지를 계산으로 확인: 매대 칸수 정확 · 계산대/가마/직원 겹침 0 ·
전부 마당 안 · 계산대는 늘 길가.

**2) 손님이 겹쳐 서서 말풍선이 뭉갰다 → 줄 서기**
가게마다 줄(`this.line`)을 만들었다. 먼저 온 손님이 계산대 앞 0번,
나머지는 길을 따라 47px 간격으로 뒤에 선다. 앞사람이 빠지면 한 칸씩 당겨 선다.
**주문표는 맨 앞사람만** 띄운다 — 줄을 세운 이유의 절반이 이것이다.
뒤에 줄이 섰으면 계산이 빨라진다(×0.62). 실측: 겹침 5.4%, 최대 줄 6명, 갇힘 0.

**3) 화난 이모지가 너무 늦었다 → 4초부터**
참을성 6초를 다 쓰고 포기할 때만 💢가 떴다. 이제 **기다림이 4초(참을성의 2/3)를
넘으면 😠**가 먼저 뜬다. ★ 줄 서서 기다리는 시간은 안 센다 — 줄은 눈에 보이라고
만든 연출이지 손해가 아니다(계산은 이미 끝나 있다). 그것까지 화를 내게 했더니
가게 앞 손님의 **28.9%**가 성난 얼굴이 됐다. 지금은 **3.4%** — 뜨면 진짜 신호다.

**4) 고용이 승급을 기다렸다 → 돈만 있으면 (유저 지적)**
승급 조건이 다섯 가지라 두 시간 가까이 걸리는데, 일손이 필요한 건 바로
그 전 구간이다. 이제 승급과 무관하게 뽑는다. 자리 수만 등급이 늘린다
(무쇠 2 · 참쇠 3 · 강철 4). 첫 직원 값도 첫 승급값의 3배 → **12%**로 내렸다.

| | 전 | 후 |
|---|---|---|
| 첫 직원 | 84분 (승급 뒤) | **37분** (승급과 무관) |
| 빈손 💢 | 1.1% | **0.5%** |
| 기다렸다 받아감 | 10.8% | **4.9%** |
| 콘텐츠 소진 | 9시간22분 | 8시간26분 (씨앗 2·3: 8:15·8:22) |

**5) 너구리가 매대에 가려졌다 (유저 지적)**
물건을 집을 때 **매대 칸 안**에 서게 해 놨었다(+26,+14). 매대 상자가 낮아도
점장이 그 칸 뒤쪽에 서는 셈이라 머리끝까지 덮였다. 이제 매대 **옆 칸**
(마당 안쪽, 다른 매대·계산대·가마를 피해서)으로 물러선다 — 깊이가 같거나
한 칸 뒤라, 가려도 발끝뿐이다.

---

## 2026-08-20 · 화난 채로 오던 손님 · 숫자 단위 고르기

**1) 손님이 마을에 화난 채로 걸어 들어왔다 (유저 보고) — 진짜 버그였다**
살 게 하나도 없는 손님(`sold`가 빈 손님)을 **그것만으로** 💢로 그렸다.
그래서 그 손님은 아직 묻지도 않았는데 화난 얼굴로 들어와, 줄까지 성난 채
서 있었다. 이제 💢는 **계산이 끝난 뒤에만**(`w.paid`) 뜬다 —
'볼일이 끝났는데 못 샀다'는 뜻이니 그게 맞다.
실측: 도착 즉시 화남 **0 / 248명**.

**2) 화남을 언제 세는가 — 줄 시간은 안 센다 (재보고 내린 결론)**
유저 제안대로 '줄에 선 뒤 4초'로 만들어 재 봤더니:

| | 줄 시간까지 셀 때 | 물건 기다림만 셀 때 |
|---|---|---|
| 초반 | 4.0% | 0.8% |
| 중반 | **79.3%** | 23.4% |
| 후반 | **97.5%** | 16.2% |

이유는 단순하다 — **잘되는 가게는 화면에 세울 수 있는 것보다 훨씬 빨리
팔린다.** 줄은 늘 꽉 차 있으니 '줄 시간'은 가게 사정이 아니라 연출의 한계를
재는 셈이 되고, 그렇게 뜬 화남은 신호가 아니라 소음이다.
지금 😠는 하나만 뜻한다: **물건을 제때 못 만들고 있다.**
문턱은 참을성 6초의 3/4 = **4.5초**('4초대', 유저가 말한 범위 안).

곁들여 고친 것:
- 줄이 꽉 찬 가게(4명)에 도착하면 그 가게는 조용히 지나친다. 예전엔 도착
  시점에 안 막아서 줄이 여섯까지 밀렸다. 지나칠 때 **도로 더해 둔 재고를
  반드시 풀어 준다** — 안 풀면 그 몫이 진열대 숫자에 영원히 얹혀,
  빈 매대가 꽉 찬 것처럼 보인다.
- 붐빌수록 계산이 빨라진다(2명 ×0.7 · 3명 이상 ×0.5).

**3) 숫자 단위 — 만·억 ↔ K·M 단추 (유저 질문)**
머리띠 오른쪽에 단추를 달았다. 기본은 **한국식**이다: 이 게임은 조선
만물상이고 한국말은 네 자리마다 단위가 바뀐다(만 10⁴ · 억 10⁸) —
'1.2억'은 한국 사람이 소리 내어 읽는 그대로다. 세 자리로 끊는 K·M이
편한 사람도 있어서 한 번 누르면 바뀐다(1.23억 ↔ 123M). 소리 설정처럼
따로 기억하고, 누르면 열려 있는 창까지 즉시 다시 그린다.

---

## 2026-08-20 · 직원이 안 보이던 버그 · 직원 상한을 재서 정함 · K 단위

**1) 직원을 뽑았는데 한 마리도 안 보였다 (유저 보고) — 진짜 버그**
직원은 담벼락에 안 붙은 **안쪽 칸**에만 세우게 해 놨는데, 무쇠급 3×3 마당은
안쪽 칸이 딱 하나이고 그걸 작업대가 쓴다 → **설 자리 0개.**
돈만 나가고 화면에는 아무 일도 안 일어나는, 제일 나쁜 종류의 고장이었다.
이제 매대·계산대·가마가 안 쓰는 담벼락 칸을 뒷자리로 내준다.
4종 마당 × 3등급 12가지에서 자리 수 충분 · 겹침 0 확인.

**2) 직원 최대 몇 명이 안전한가 (유저 질문) → 재보니 답은 "한 명"**
고용 비용을 빼고 직원 수만 박아 8시간씩 두 씨앗으로 돌렸다:

| 가게당 직원 | 생산정지(꽉참) | 빈손 💢 | 기다렸다 받음 | 8시간 매출 |
|---|---|---|---|---|
| 0명 | 0.2% | 1.86% | **79.1%** | 486억 |
| **1명** | 27.2% | 0.00% | **0.7%** | **961억** |
| 2명 | 26.6% | 0.00% | 0.6% | 933억 |
| 3명 | 26.1% | 0.00% | 0.8% | 996억 |
| 4명 | 26.2% | 0.00% | 0.8% | 1008억 |
| 6명 | 26.2% | 0.00% | 1.0% | 960억 |

**둘째부터는 아무것도 안 바뀐다**(전부 오차 범위). 이유가 분명하다 —
계산 중 생산 멈춤은 한 명이면 이미 완전히 사라지고, 진열대는 이미 26%가
꽉 차 있어 칸을 더 줘도 쌓을 데가 남아돌 뿐이다. **병목이 생산이 아니라 수요다.**
아무 일도 안 하는 걸 수십억에 파는 건 함정이므로 **가게당 1명**으로 못 박았다.
(상한을 2·4·6·8로 바꿔 10시간을 돌려도 결과가 똑같았다 — 10시간 안에는
상한이 아니라 **돈**이 병목이라 애초에 둘째를 못 뽑는다.)
더 뽑게 하려면 직원에게 **새 일거리**를 줘야 한다(호객 → 손님이 더 자주 옴 같은).

**3) 사자마자 화난 손님 (유저 보고)**
두 가지가 겹쳐 있었다. ① 😠가 기다림이 끝난 뒤에도 안 꺼졌다 — 물건을 받아
들고 돌아서는 내내 성난 얼굴이었다. 이제 **기다리는 동안만** 뜬다.
② 몇 개 사고 한 종류를 포기한 손님에게도 💢를 띄웠다. 이제 💢는
**빈손으로 돌아갈 때만**. 실측: 계산 끝난 손님 94명 중 화남 0명.

**4) 숫자는 K·M 하나로 (유저 결정)**
만·억과 고르는 단추를 달아 봤지만 고르는 게 일이지 얻는 게 없었다.
세 자리로 끊는 K·M·B로 통일하고 단추를 뺐다.

---

## 2026-08-20 · 기다림 시계를 둘로 — 계산대 6초 · 줄 9초

유저 질문: "줄서기부터 6초야?" → **아니었다.** 그때까지 줄 시간은 아예 안 셌다.
6초는 계산대 앞에서 **물건이 만들어지길** 기다리는 시간뿐이었다.
이제 유저 제안대로 시계를 둘로 나눴다:

| 시계 | 시간 | 성격 |
|---|---|---|
| `patience` | **6초** | 계산대 앞, 물건 기다림. **장사에 진짜 영향** — 6초 안에 안 나올 것 같으면 손님이 그 물건을 포기한다(`sim._buy`가 여기서 갈린다). 😠는 3/4인 4.5초부터. |
| `linePatience` | **9초** | 앞사람 기다림. **연출** — 계산은 이미 끝나 있다. 넘겨도 얼굴만 굳는다. 남 때문에 기다리는 건 더 참아 주라고 넉넉히 잡았다. |

실측(실제로 플레이하며 구간별 3분씩):

| | 줄 선 시간(중앙) | 줄 선 시간(최장) | 줄 때문에 화남 | 최대 줄 |
|---|---|---|---|---|
| 초반 | 0.1초 | 2.7초 | 0% | 2명 |
| 중반 | 2.0초 | 7.7초 | 0% | 4명 |
| 후반 | 1.3초 | 4.0초 | 0% | 4명 |

**9초는 잘 고른 숫자다** — 제일 오래 선 손님도 7.7초라, 평소에는 안 뜨고
정말 밀렸을 때만 뜨는 안전망이 된다.

**곁들여 잡은 것 — 줄이 영영 막힐 수 있었다.**
계산대 맨 앞 손님은 '주문한 물건이 나왔다'는 신호(`onOrderDone`)를 기다린다.
그 신호를 어떤 이유로든 놓치면 그 손님이 계산대를 영원히 막고 뒤도 다 갇힌다
(측정 도구에서 그 상황을 우연히 만들어 353초짜리 대기를 봤다).
sim은 아무리 늦어도 12초 안에 주문을 끝내므로, 그보다 넉넉히 지나면
신호가 없었어도 진행시키는 **안전 빗장**을 넣었다. 줄은 무슨 일이 있어도 흘러야 한다.
(실제 플레이 5시간 반 연속 측정에서는 30초 넘게 갇힌 손님 0명.)

---

## 2026-08-20 · 설계 결정 — 성취를 무엇으로 만들 것인가

유저 진단: **"터치>업글>터치>업글이 끝이야. 이 게임의 끝은 뭔데?
성취가 없어. 업글은 그냥 과정인 거고."** — 방치형의 고전적 병이고, 정확한 진단이다.

### 정한 것

| | 결정 |
|---|---|
| 성취의 축 | **관계 + 의뢰.** 돈은 축이 아니다 — 돈으로 걸면 업글의 연장일 뿐이다 |
| 도둑 잡기 | **성취 조건으로 안 쓴다.** "게임 내 마이크로 행위지 의미가 없다"(유저). 대신 **젬을 확률로 주는 자리**로 쓴다 |
| 도깨비 시장 · 모험가 길드 | **2장·3장(영구 진행)이 아니라 기간제 이벤트.** "48시간 안에 깨면 스킨" |
| 도깨비 아이템·음식 | 기획이 아직 없으니 **지금 안 만든다** |
| 판매자 | 어느 테마든 **너구리 그대로.** 바뀌는 건 손님과 물건뿐 |

### 왜 '기간제 이벤트'가 '2장·3장'보다 나은가

영구 진행(2장·3장)으로 만들면 콘텐츠를 **한 번 쓰고 버린다.** 기간제로 만들면
같은 콘텐츠를 몇 달마다 다시 돌릴 수 있고, 돌 때마다 "이번엔 깨야지"가 생긴다.
그리고 **끝나는 시각이 있다는 것 자체가 이유**가 된다 — 방치형에서 제일 구하기
어려운 "지금 켜야 할 이유"가 공짜로 딸려온다.

주의: 기간제는 **숙제가 되면 즉시 이탈**한다(유저가 앞서 걱정한 그것).
그래서 이벤트는 **놓쳐도 손해가 없어야** 한다 — 못 깨면 스킨을 못 받을 뿐,
본편 진행이 뒤처지면 안 된다.

### 남겨 둔 생각

- **까치 = 길조.** 지금은 손님이라 못 쓰지만, '행운'을 어딘가에 붙일 재료로 남긴다.

### 다음에 만들 것 — 의뢰와 젬

이벤트는 결국 **'의뢰 + 시간제한 + 특별 보상'**이다. 그러니 순서는 이렇다:

1. **의뢰** — 손님이 준다. **단골 등급이 의뢰의 크기를 정한다**(관계 + 의뢰 결합).
   지금 손님은 자판기인데 의뢰를 주면 인물이 되고, 20성이 숫자가 아니라 뜻이 된다.
   "왜 강화해?"가 "곰 의뢰 깨려고"로 바뀐다.
2. **젬** — 도둑 잡기(확률)와 의뢰 보상으로 들어온다. 나중에 인앱결제와 이벤트가
   여기에 붙는다.
3. **이벤트** — 위 둘이 있으면 이벤트는 '기간이 붙은 의뢰 묶음'이라 싸게 만든다.

셋 다 **규칙·데이터**라서 Godot으로 그대로 따라간다.

★ 정해야 할 것 하나: **젬을 당장 어디에 쓰는가.** 쓸 곳 없는 화폐는 아무도 안 모은다.

---

## 2026-08-20 · 마을 의뢰와 젬 — 만들고, 재고, 두 번 갈아엎었다

유저가 앞의 설계를 한 번 더 깎았다:
**"의뢰도 가볍게 생각하면 됨. 지금 손님 1마리에 집중하고 있지만
단위를 마을로 정하면 됨. 토끼마을 > 호미 30개 > 젬2."**

그 한 줄이 설계를 통째로 바꿨다. 손님 한 마리에 걸면 **그 손님이 올 때까지
아무 일도 안 일어난다** — 호랑이는 400초에 한 번 오니 의뢰 하나가 하루짜리가 된다.
마을로 묶으면 그 마을 손님이 올 때마다 조금씩 차오른다. 방치형에서 다시 켤 이유는
"가만히 뒀는데 뭔가 올라가 있다"에서 나온다.

### 만든 것

| | 규칙 |
|---|---|
| 단위 | **마을**(= 손님 종족). 토끼마을·까치마을 … 호랑이마을 |
| 동시 | 3건. 한 마을에 하나씩. 한 자리 비면 90초 뒤 새 의뢰 |
| 개수 | **손으로 안 정한다.** 300초어치를 그 마을의 실제 구매 속도로 나눈다 |
| 보상 | 엽전(청한 개수 × 지금 값 × 1.5) + 젬(단골 등급 6성마다 한 알, 최대 5) |
| 실패 | **없다.** 시간 제한도 벌칙도 없다 — 있으면 지켜봐야 하는 숙제가 된다 |
| 젬 버는 곳 | 의뢰 · 품목 만렙(승급 전 한 알) · 나쁜 놈 잡기(10%) |
| 젬 쓰는 곳 | 벼린 연장 · 잰 손놀림 · 흥정 솜씨(영구) + 삯꾼 부르기(소모) |

### 함정 1 — 개수를 손으로 정하면 마을마다 8배가 벌어진다

마을마다 사가는 속도가 8배까지 다르다(까치 초당 0.65개, 호랑이 0.085개).
같은 30개를 걸면 까치마을은 1분, 호랑이마을은 한 시간이다.
그래서 **개수가 아니라 시간으로 정한다** — 단골 20성 표(REGULARS)에서 쓴 것과
똑같은 방법이다.

그런데 그것만으로는 안 됐다. 첫 측정에서 토끼마을 6분, 곰마을 25분, 소마을 37분.
곰은 한 종류를 통째로 쓸어가는 손님(spread 1)이라, 열린 칸이 15개면
**원하는 물건을 집을 확률이 15분의 1**이다. 개수를 아무리 줄여도
'언제 그 물건을 집느냐'가 시간을 정해버려서 안 고쳐진다.

고친 방법: **의뢰를 건 마을은 그 물건을 먼저 집는다.** 무엇보다 말이 된다 —
토끼마을이 호미를 청했으면 토끼들이 호미를 사가야 한다.
고친 뒤 열두 마을 전부 4분45초 ~ 5분31초로 모였다(소마을만 11분).

### 함정 2 — 도구가 안 눌러본 기능은 도구에게 없는 것이다

젬 강화를 처음엔 **장(場)**에 붙였다(장 시간 +6초, 간격 −8초, 값 +4%).
재보니 8시간 누적매출이 71B → 206B, **세 배**. 손님 열둘이 다 오는 시각도
5시간31분 → 4시간9분으로 당겨졌다 — 콘텐츠가 25% 빨리 바닥났다.

절반으로 낮추고 하나씩 꺼가며 다시 쟀더니 이상한 줄이 나왔다:

| | 8시간 누적매출 |
|---|---|
| 전부 켬 | 190B |
| **장 강화만 끔** | **240B** ← 끄는 쪽이 더 벌었다 |

**장 강화를 사면 손해였다.** 손님이 몰릴수록 줄이 길어지고(기다린 손님
15.5% → 44.6%), 점장 혼자인 가게는 계산하는 동안 망치를 놓는다.
손님을 두 배로 부르면 계산 멈춤도 두 배가 된다. 만드는 양은 그대로인데
줄만 길어진 것이다. 끝낸 의뢰도 113건 → 42건으로 주저앉았다.

그러니까 **이 게임의 병목은 수요가 아니라 생산이다.**
FAIR 주석에는 오랫동안 정반대로 적혀 있었는데, 그건
**도구가 장을 한 번도 안 눌러봤을 때**의 이야기였다.
`tools/balance.mjs`의 가상 플레이어에 "북적이면 누른다" 한 줄이 없어서,
장에 붙은 수치는 아무리 키우고 줄여도 결과가 안 변했다.

두 가지를 고쳤다.
1. 도구가 장을 누르게 했다(8시간에 232번). 기준선 자체가 71B → 148B로 올랐다.
2. 젬 강화를 **전부 생산 쪽으로** 옮겼다 — 만드는 시간, 계산 멈춤, 판매가.

### 최종 측정 (8시간 × 씨앗 4개, 누적매출 중앙값)

| | 매출 | |
|---|---|---|
| 의뢰조차 없음 | 148B | 기준 |
| 의뢰만 (보상 0 · 강화 0) | 155B | +5% |
| + 벼린 연장만 | 168B | +14% |
| + 잰 손놀림만 | 163B | +5% |
| + 흥정 솜씨만 | 204B | +32% |
| 전부 켬 | 264B | **+78%** |

'의뢰만'이 +5%인 게 재미있다. 보상을 한 푼도 안 줘도 매출이 오른다 —
의뢰를 건 마을이 그 물건을 먼저 집으니 진열대가 덜 막히기 때문이다. 공짜로 얻은 몫이다.

그리고 **매출 1.78배가 시간으로는 11%밖에 안 된다**
(손님 열둘이 다 오는 데 4시간40분 → 4시간8분).
매출도 지수로 자라고 등장 문턱도 지수라서 그렇다.
**밸런스는 돈 배수가 아니라 시간으로 봐야 한다.**

### 함정 3 — 싼 소모품이 영구 강화를 굶긴다

삯꾼 부르기(3알)가 제일 싸서, 다음 강화값(5·7·11알)을 모으기 전에 젬이 계속
빠져나갔다. 8시간 내내 삯꾼만 부르고 영구 강화는 2단계에서 멈췄다.
재보니 **모아 쓰는 쪽이 유리**했다(246B vs 201B) — 그래서 함정은 아니지만,
값을 정할 때 계속 봐야 할 관계다.

### 아직 안 한 것

- **뽑기 · 룰렛 · 스킨 상점** — 전부 새 화면이 필요하다. 지금 만들면 엔진을 옮길 때
  두 번 만들게 된다. 규칙과 숫자는 넘어가지만 화면은 안 넘어간다.
- **광고 보고 젬** — 앱으로 나가야 되는 일이라 아직.
- **기간제 이벤트** — 이제 '의뢰에 기한과 특별 보상을 붙인 것'이라 싸게 만들 수 있다.

---

## 2026-08-20 · 콘텐츠 수명 두 배 · 기간제 이벤트 (JS에서 할 마지막 두 가지)

유저 걱정: **"이러다 godot 넘어가는 거 안 되는 거 아녀?"** — 맞는 걱정이라 숫자로 답했다.
지금 코드는 규칙 1,648줄 / 화면·엔진 3,105줄이고, 버려지는 쪽이 두 배다.
다만 그 3,105줄은 대부분 이 약속(규칙만 쌓기) 이전에 쓴 것이다.
그래서 **화면이 필요 없는 것 둘만 하고 멈추기로** 정했다.

### 1. 콘텐츠 수명 — 매대 19칸 → 40칸

가게마다 매대는 이미 8칸까지 들어가게 돼 있었다(무쇠급 4 · 참쇠급 6 · 강철급 8).
그런데 19칸만 쓰고 있었다 — **21칸이 비어 있었다.** 새 시스템 없이 그냥 채웠다.

| 가게 | 늘린 품목 |
|---|---|
| 대장간 | 자물쇠 · 가마솥 |
| 필방 | 연적 · 필통 · 서산 · 붓걸이 · 화첩 |
| 지물포 | 장지 · 연 · 지우산 · 지등 · 병풍 |
| 옹기점 | 시루 · 술병 · 다기 · 향로 · 달항아리 |
| 약재상 | 당귀 · 영지 · 침향 · 경옥고 |

값은 각 가게의 기존 비율(2.4~2.7배)을 이어 붙였고, 여는 값은 **재고 고쳤다.**
결과: 콘텐츠 소진 **8시간25분 → 14시간49분** (+76%).

### 함정 — 품목을 늘리면 손님이 앞당겨진다

손님 등장 문턱(GUESTS[].at)은 **누적매출**이다. 매대가 두 배가 되니 매출이
세 배로 뛰었고, 손님 열둘이 다 오는 시각이 4시간8분 → **3시간15분**으로 당겨졌다.
품목 축은 늘렸는데 손님 축은 오히려 줄어든 것이다.

손으로 고칠 수 없다 — at을 하나 고치면 매출 곡선이 움직여서 나머지 열한 개가
어긋난다. 그래서 `tools/guests.mjs`를 새로 썼다. 목표 시각을 정해 두고
재고 → 고치고 → 다시 재기를 10번 반복해 수렴시킨다(감쇠 √(지금×제안)).
10회차에 전부 목표의 8% 안으로 들어왔다. 호랑이 목표 8시간 → 실측 7시간38분.

> 이 도구를 처음 돌렸을 때 마지막 둘이 NaN·undefined가 됐다. 파일 전체에서
> `at: 숫자,`를 찾아 바꿨는데 PESTS에도 at이 있어 14군데가 걸렸고, `++k`로
> 세는 바람에 값이 한 칸씩 밀렸다. **소스를 자동으로 고치는 도구는 범위를
> 좁히는 것이 첫 번째 안전장치다.** 지금은 GUESTS 배열 안으로 못 박고,
> 고친 개수가 12가 아니면 던진다.

### 함정 — 죽음을 막던 보험이 이제는 해가 됐다

`_buy`에는 '진열대가 꽉 찬 품목을 맨 앞으로 당기는' 정렬이 있었다.
품목이 15개이고 손님이 한 종류만 사가던 시절에 죽은 품목을 되살리던 장치다.

매대가 40칸이 되자 정반대로 굴렀다 — 싼 품목 아홉 개가 늘 꽉 차 있으니
**모든 손님이 매번 싸구려부터 집어갔다.** 끄고 재보니:

| | 12시간 누적매출 |
|---|---|
| 보험 켬 | 2.7T |
| 보험 끔 | **4.2T (+60%)** |

그러고도 안 팔린 품목은 0개였다. 죽음을 막는 진짜 장치는 그 정렬이 아니라
손님의 `spread`(여러 종류를 집는 것)였다. 그래서 뺐다.

### 함정 — 도구가 늑대소년이 됐다

품목이 40개가 되자 '△ 정체(재고 상한에 절반 넘게 박힘)' 경고가 열두 개씩
떴다. 그런데 그중 곡괭이는 **26,003개나 팔린** 품목이다. 싸고 빨리 만들어지는
물건은 늘 진열대가 차 있는 게 정상이다.

판정을 고쳤다: 박혀 있고 **게다가** 제일 많이 팔린 품목의 10분의 1도 못 판
경우만 정체로 잡는다. 지금은 경고가 0개다.
**열두 개씩 뱉는 경고는 아무도 안 읽는다.**

### 2. 기간제 이벤트

"48시간 안에 깨면 스킨" — 유저가 정한 모양 그대로.

| | 규칙 |
|---|---|
| 마감 | **실제 시간** 48시간. 껐다 켜도 흐른다 |
| 간격 | 하나 끝나고 12시간 뒤 다음 것. 늘 켜져 있으면 특별하지 않다 |
| 시작 | 가게를 둘 연 뒤부터 |
| 실패 | **아무것도 안 잃는다.** 스킨을 못 받을 뿐 |
| 목표 | 마을 의뢰 35건 / 나쁜 놈 45마리 / 장 40번 — **셋 다 이미 도는 것** |
| 보상 | 젬 25~30 + 스킨(이름만. 그림은 그림 작업 때) |

도깨비 시장·모험가 길드는 기획이 없으니 목표를 새로 만들지 않았다.
나중에 도깨비 물건이 생기면 `goal` 하나만 늘리면 된다 — 틀은 그대로다.

**실제 시간을 '남은 시간 깎기'가 아니라 '시각'으로 저장한다.** 깎는 방식이면
게임을 꺼둔 동안 시계가 멈춘다 — 마감은 실제로 다가오는데 다음 이벤트는
안 오는, 앞뒤가 안 맞는 상태가 된다. `offline()`이 wall을 그대로 밀어준다.

40시간 연속 측정: 달빛 장터 3시간8분에 깸 → 12시간 쉼 → 쥐잡이 대회 15시간44분에 깸.

### 여기서 멈춘다

JS에서 할 수 있는(=Godot으로 그대로 따라가는) 일은 사실상 끝났다.
남은 것 — 뽑기 · 룰렛 · 스킨 상점 · 도깨비 시장 화면 — 은 전부 **새 화면**이라
지금 만들면 두 번 만든다. 다음은 Godot 이관이다.

---

## 2026-08-20 · Godot 4.7을 깔았다 · 답안지 방식이 첫 조각에서 바로 버그를 잡았다

유저: **"godot4는 어디서 깔어… 너 스스로 깔어봐"**

### 깔았다

이 작업방에서 `godotengine.org`와 tuxfamily 미러는 막혀 있다. 그런데 GitHub
릴리스 파일이 나가는 `objects.githubusercontent.com`은 열려 있어서, 릴리스
주소로 직접 받으면 된다. 버전은 태그를 하나씩 찔러 찾았다 — 4.8은 404,
4.7은 zip이 나온다. **4.7이 최신 stable.**

```
tools/godot-install.sh     # 50MB 받아 /opt/godot에 풀고 심볼릭 링크
godot --headless --version → 4.7.stable.official.5b4e0cb0f
```

이 상자는 일회용이라 세션이 끝나면 지워진다. 그래서 손으로 깔지 않고
스크립트로 남겼다 — 다음 세션에서 "어디서 받더라"를 다시 찾지 않으려고.

### 답안지 방식 — 되는지 확인했다

`sim.js`는 몇 달 돌아갔고 수백 시간을 재본 코드다. 그러니 GDScript로 옮길 때
**정답으로 쓸 수 있다.** 같은 입력을 양쪽에 넣고 대조하면, 옮기다 조용히
달라진 곳이 그 자리에서 드러난다.

첫 조각으로 `fmt()`(큰 숫자 줄여 쓰기) 하나를 옮겼다. 한 줄 한 줄 그대로
베껴 썼다. 그런데:

```
입력 5625 · JS 5.63K · Godot 5.62K
```

JS의 `toFixed()`는 절반을 올리고(5.625→5.63) C의 `%.2f`는 짝수 쪽으로
붙인다(→5.62). **함수 하나에서 벌써 갈렸다.** 눈으로는 절대 못 찾는다.

`+0.5 하고 내림`으로 JS를 흉내 내는 방법도 있는데 그것도 안 맞는다 —
1.005는 이진수로 정확히 안 담겨서(1.00499…) JS는 1.00을 주는데 그 방법은
1.01을 준다. 그래서 한쪽을 흉내 내지 않고 **두 언어가 똑같이 할 수 있는
규칙으로 양쪽 다 바꿨다**: 자릿수만큼 곱해 내리고 나눈다. 찍는 단계에
반올림할 것이 안 남는다. 게임에도 맞다 — 가진 것보다 많이 보여주지 않는다.

### 시험 문제도 고쳤다

처음엔 난수 4천 개만 뿌렸는데, **한 번은 통과하고 다음엔 실패했다.**
반올림이 갈리는 건 값이 딱 절반에 떨어질 때뿐이라, 그런 값이 우연히
걸려야만 드러났기 때문이다. 어쩌다 잡히는 시험은 시험이 아니다.
지금은 씨앗을 고정하고 절반에 떨어지는 값을 일부러 전부 넣는다 — 5,004개,
세 번 돌려 세 번 다 같은 결과.

### 남는 것

| | |
|---|---|
| `tools/godot-install.sh` | 이 작업방에 Godot 4.7 깔기 |
| `tools/crosscheck.sh` | JS판과 GDScript를 대조 (1.3초) |
| `godot/rules/num.gd` | 옮긴 첫 조각 |

이관은 이제 **"옮기고 → 대조하고 → 초록불 확인"의 되풀이**다.
방법이 되는지는 확인했고, 그 방법이 실제로 일한다는 것도 확인했다.

---

## 2026-08-20 · 경제 규칙 전체를 Godot으로 옮겼다 · 4시간 돌려 한 글자도 안 갈렸다

유저: **"godot 설치했어"** → 이관 시작.

### 손으로 옮기지 않은 것

`content.js`의 숫자 700줄은 `tools/gen-content.mjs`가 뽑아낸다.
손으로 베끼면 반드시 어딘가 틀리고, **틀려도 안 보인다** — 도끼 값이 260이어야
하는데 250으로 적혀 있으면 게임은 멀쩡히 돌고 몇 시간 뒤 곡선만 이상해진다.
게다가 이관하는 동안 숫자를 더 만질 텐데, 그때마다 다시 돌리면 양쪽이 저절로 같아진다.

### 뽑아낸 숫자를 **전부 실수**로 둔 이유

GDScript는 정수끼리 나누면 정수가 나온다. **`7 / 2`가 3이다.** JS는 3.5다.
`qty / every` 같은 줄을 그대로 옮기면 조용히 다른 값이 된다.
우리 숫자는 제일 큰 것이 2조인데 실수(double)는 9천조까지 정수를 정확히
담으므로, 전부 실수로 둬도 오차가 없다. 위험만 없앤 것이다.

### 옮기면서 조심한 넷

| | |
|---|---|
| 정수 나누기 | 위 그대로. 재고·레벨까지 전부 실수로 뒀다 |
| 딕셔너리 순서 | 손님이 물건 고를 때 items 순서를 섞어 쓴다. JS·Godot 둘 다 넣은 순서를 지켜 같다 |
| 정렬의 안정성 | **Godot의 sort_custom은 같은 값끼리의 순서를 보장하지 않는다**(JS는 한다). 그래서 '제일 싼 것 고르기'를 정렬 대신 훑기로 짰다 |
| 지우기 | Array.erase는 값으로 찾는데 딕셔너리 비교는 못 믿는다. 주문·의뢰는 고유 번호로 찾아 지운다 |

### 그리고 실제로 하나 더 걸렸다

`get_script_constant_map()`의 이름을 정렬했는데 **글자순으로 안 됐다.**
Godot은 StringName의 대소를 내부 주소로 비교한다(빠르라고). 에러도 경고도 없이
만들어진 순서가 나온다. String으로 바꾼 뒤 정렬해야 한다.

### 진짜 시험 — 같은 판을 양쪽에서 돌린다

조각 하나하나가 맞는 것과 **몇 시간을 돌려도 안 갈라지는 것**은 다른 문제다.
규칙이 서로를 부르고 결과가 다음 틱의 입력이 되니까, 한 곳만 어긋나면
시간이 갈수록 벌어진다.

그래서 씨앗을 고정하고 같은 가상 플레이어로 양쪽을 돌린 뒤,
**1분마다 32가지 값을 통째로 적어** 대조한다 — 돈·매출·젬·재고·진행도·레벨합·
등급합·직원수·장 타이머·의뢰 목록·주문 목록까지. 갈라지면 몇 분째에 어느 값부터
달라졌는지가 바로 나온다.

적는 값은 전부 정수로 만든다. 소수를 글자로 찍으면 표기가 갈려서 **규칙이
맞는데도 빨간불**이 뜬다 — 그러면 시험 자체를 못 믿게 된다.

```
✅ fmt      5004개 전부 일치
✅ rng       160개 전부 일치
✅ content   678개 전부 일치
✅ sim       240개 전부 일치   (4시간 · 씨앗 1·2·3·7 전부)
```

### 못생긴 화면 하나

`main.tscn`을 열고 Play를 누르면 돈이 올라간다. 일부러 못생기게 뒀다 —
이건 게임 화면이 아니라 "규칙이 Godot 안에서 실제로 돈다"를 눈으로 보는 자리다.
지금 예쁘게 만들면 그림이 나왔을 때 또 뜯어고친다.

### 남은 것

- **그리는 코드**(iso.js 1,550줄) — 그림이 나온 뒤에. Godot이 대부분 공짜로 해준다
- **화면**(index.html) — 새로 만든다. 이건 원래 안 넘어가는 부분이다
- 뽑기 · 룰렛 · 스킨 상점 — 이제 Godot에서 만들면 된다. 두 번 만들 일이 없다

---

## 2026-08-20 · 마을 화면을 Godot으로 — 손님이 길로 걸어온다

경제 규칙에 이어 **보이는 부분**을 옮겼다. 다만 한 줄씩 베끼지 않았다.

### 옮긴 것과 다시 만든 것을 갈랐다

| | |
|---|---|
| **옮겼다** (`view/iso.gd`) | 격자 좌표, 길 위치, 마당 생김새 넷(A·B·C·D), 매대·계산대·가마 자리, 길찾기(너비우선탐색) |
| **다시 만들었다** (`view/village.gd`) | 그리는 부분. Godot의 `_draw()`로 도형을 직접 찍는다 |

배치는 **규칙**이라 그대로 옮겼다 — "계산대는 길에 닿는 변에", "마당은 뒤로만
자란다", "화면의 가로 위치는 tx가 아니라 (tx − ty)다" 같은 것들은 몇 번씩
고쳐서 얻은 답이라 다시 발견할 이유가 없다.

그리는 부분은 캔버스 명령을 한 줄씩 옮겨봤자 얻는 게 없다. Godot이 훨씬
편하게 해준다 — 카메라, 깊이 순서, 입력이 전부 엔진에 있다.

### 지금 되는 것

- 격자 마을 · 길 · 풀밭 · 나무와 바위
- 가게 다섯(무너진 집 포함) · 마당 생김새 넷 · 담과 현판 · 매대와 재고 계기
- 작은 건물 넷 · 개집 · '장 서다!' 표
- **손님이 길로만 걸어온다** — 가게 문 앞에서 줄을 서고, 사고, 나간다
- 점장 너구리가 평소엔 작업대, 팔릴 땐 계산대 뒤로 간다
- 끌어서 둘러보기 (마을 밖으로는 안 나간다)

### 스크린샷 도구도 고쳤다

처음에 빨리 감기가 `sim.tick`만 돌렸다. 그랬더니 판매가 화면으로 안 넘어가
**손님이 한 마리도 안 나왔다.** 빨리 감기는 게임을 그대로 감아야지 반만
감으면 딴것이 된다 — 지금은 `main._process`가 하는 일을 그대로 한다.

### 아직 안 된 것

- **누르는 조작.** 가게를 눌러 강화하거나 나쁜 놈을 잡는 것. 지금은 가상
  플레이어(`RunSim.act`)가 대신 누른다 — 다음 차례가 이것이다
- 창(가게·의뢰·손님 도감) — 새로 만든다
- 주문 말풍선 · 매상 표 · 화난 이모지 같은 잔재미
- 그림(스프라이트) — 지금은 도형이다. 그림이 나오면 그 자리에 끼운다

---

## 2026-08-20 · 누르기 — 이제 손으로 논다

가상 플레이어(`RunSim.act`)를 게임에서 뺐다. 이제 유저가 누른다.

### 되는 것

| 누르는 곳 | 하는 일 |
|---|---|
| 매대 | 그 품목을 **살 수 있는 만큼** 강화 (닫힌 칸이면 칸 열기) |
| 현판 · 마당 앞쪽 | 가게 창 — 자세한 숫자 · 직원 · 승급 |
| 무너진 집 | 가게 되살리기 |
| 작은 건물 자리 | 세우기 / 북적일 때 눌러 장 열기 |
| 삽살개 자리 | 삽살개 들이기 |
| 나쁜 놈 | 잡기 (남은 시간이 테두리로 줄어든다) |
| 빈 곳 | 창 닫기 |

### 끌기와 누르기를 가른다

이걸 안 하면 마을을 둘러보려고 끌 때마다 손가락 뗀 자리가 눌려서 엉뚱한 게
강화된다. 8px 넘게 움직이면 '끈 것'으로 본다.

### 나쁜 놈은 늘 맨 앞에

지붕 뒤에 숨으면 누를 수가 없다. 깊이 순서를 무시하고 마지막에 그린다.
까마귀는 **카메라 한가운데를 기준으로** 가로지른다 — 마을 어디를 보고 있든
눈에 들어와야 누를 수 있다. 쥐는 반대로 훔치는 가게 앞에 나온다.

### 누르기도 시험을 만들었다 (`tools/crosscheck.sh`에 포함)

눈으로 보면 "눌리는 것 같다"까지밖에 못 간다. 좌표를 계산해 두드려 보고
sim이 실제로 변했는지 본다 — **매대를 눌렀는데 옆 매대가 오르는** 종류의
고장은 그림으로는 안 보인다.

> 처음 돌렸을 때 '마당 한가운데를 눌렀는데 창이 안 열린다'로 실패했다.
> 그런데 고장이 아니라 **시험이 틀린 것**이었다 — 3×3 마당은 가운데가 매대
> 판정 안에 들어간다(이웃 칸이 화면에서 48px밖에 안 떨어진다). 매대가 먼저인
> 건 일부러 그렇게 만든 것이고(강화를 지도에서 바로 하려고), 마당으로 들어가는
> 정식 통로는 현판과 앞 꼭짓점이다. 시험을 그쪽으로 고쳤다.

### 창 만들 때 걸린 것

- **배경을 안 깔면** 지도가 글자 사이로 비쳐서 아무것도 못 읽는다
- **글자 줄바꿈을 기본으로 켜면 안 된다.** 가로줄(HBox)에 넣었더니 제목이
  "대/장/간"으로 세로로 쪼개졌다 — 옆 것이 자리를 다 먹으면 글자 폭까지
  줄어드는데, 줄바꿈이 켜져 있으면 그걸 "한 줄에 한 글자"로 받아들인다

### 아직 안 된 것

- 손님 도감 · 의뢰 창 · 젬 쓰는 곳 (창 틀은 만들었으니 이제 싸다)
- 주문 말풍선 · 매상 표 · 화난 이모지 같은 잔재미
- 그림(스프라이트)

---

## 2026-08-20 · 의뢰 창 · 손님 도감 — 창 하나에 셋을 담았다

창을 세 개 만들지 않았다. **한 틀에 세 가지를 담는다**(`kind`로 가른다).
창을 세 개 만들면 여닫는 규칙도 세 벌이 되고, 그중 하나는 반드시 어긋난다.
같은 단추를 다시 누르면 닫힌다 — 닫기 단추를 찾아 누르는 수고를 던다.

- **마을 의뢰** — 기간제 이벤트(남은 시간·눈금·보상) · 가진 젬 · 걸린 의뢰 셋 ·
  젬 쓰는 곳(삯꾼·벼린 연장·잰 손놀림·흥정 솜씨)
- **손님 도감** — 단골 등급, 다음 등급까지 남은 방문, 아직 안 온 손님의 문턱
- **가게** — 강화·칸 열기·직원·승급

### 깃을 Godot 안에서 — 알아본 결과

유저가 에셋 라이브러리 1581(**Godot Git Plugin 3.2.1**, MIT, 4.1+)을 찾아왔다.
위키를 읽고 내가 앞서 걱정한 것 두 가지를 확인했는데 **둘 다 틀렸다**:

| 내 걱정 | 실제 |
|---|---|
| 저장소가 프로젝트 폴더보다 위에 있으면 못 찾는다 | **찾는다.** "부모 폴더에서 가장 가까운 저장소를 찾는다" |
| 4.7용 빌드가 없을 수 있다 | GDExtension이라 4.1+면 된다 |

대신 진짜 걸림돌은 다른 데 있었다. **이 저장소가 비공개**라(로그인 없이 404)
받으려면 GitHub 개인 토큰이 필요하고, 위키에 따르면 플러그인은 그 토큰을
디스크에 저장하지 않는다 — 켤 때마다 다시 넣어야 한다. 게다가 주고받는 동안
에디터가 멈춘다(단일 스레드).

**모르는 것을 아는 척하지 않고 찾아본 것이 답을 바꿨다.** 걱정 두 개는 틀렸고,
진짜 문제는 예상 못 한 쪽(저장소가 비공개)에 있었다.

---

## 2026-08-20 · 저장 — 빠뜨렸던 뼈대, 그리고 JSON이 판 두 개의 함정

Godot판을 여기까지 옮겨놓고 보니 **저장이 통째로 없었다.** 껐다 켜면 처음부터다.
방치형에서 저장은 잔재미가 아니라 뼈대다 — "잠깐 두고 나중에 본다"가 아예
성립하지 않고, 오프라인 수익도 여기 얹혀 있다.

### 함정 1 — 담을 칸을 손으로 적어야 한다

자바스크립트는 "이 셋만 빼고 전부"라고 쓸 수 있지만 GDScript는 안 된다.
42개를 손으로 적었다. **하나 빠뜨리면 껐다 켤 때마다 그것만 조용히 0이 된다.**

그래서 시험을 먼저 만들었다: 반쯤 돌린 뒤 저장하고, 그 저장본으로 새 판을
띄우고, **안 껐던 판과 같은 주사위로 30분을 더 나란히 돌려** 1분마다 대조한다.
저장 직후가 아니라 한참 뒤에 대조하는 게 요령이다 — 칸 하나를 빠뜨리면
그 순간엔 티가 안 나고 몇 분 뒤에 곡선이 갈라진다.

### 함정 2 — `JSON.stringify`가 열쇠를 제멋대로 정렬한다

첫 시험이 바로 빨간불이었다. 30분 만에 매출이 갈라졌다.

원인: Godot의 `JSON.stringify`는 **`sort_keys`가 기본으로 켜져 있다.**
그러면 `items`의 순서가 넣은 차례가 아니라 가나다순으로 바뀐다.
손님은 items의 순서를 섞어서 물건을 고르므로(`_buy`), 순서가 달라지면
**같은 주사위로도 다른 물건을 집는다.** 저장했다 켰을 뿐인데 경제가 갈라진 것이다.

### 함정 3 — 그걸 고쳐도 비트 하나가 어긋났다

`sort_keys`를 끄고 `full_precision`을 켜도 값 하나가 달랐다:

```
1.43984302427218668896  (안 껐을 때)
1.43984302427218691101  (저장했다 켰을 때)
```

**글자로 비교했으면 못 잡았다** — 표기 차이인 줄 알았을 것이다. 값을 직접
견주는 시험(`tests/precision.gd`)을 따로 만들어서 나왔다.

지금은 티가 안 나지만, 만드는 진행도가 문턱을 넘느냐 마느냐가 한 틱 갈리면
거기서부터 판이 달라진다.

**고친 방법: 글자를 버리고 이진 저장(`var_to_bytes`)으로 갔다.** 소수를 8바이트
그대로 담고 그대로 꺼내므로 어긋날 자리가 없다. 정수와 소수도 구분해 보존한다.
사람이 열어볼 수 없게 되지만 게임 저장본은 원래 그래도 된다. 저장본 4.7KB.

### 붙인 것

- 5초마다 저장 · 창을 닫거나 화면을 벗어날 때 한 번 더
- **오프라인 수익** — 껐던 실제 시간만큼(최대 4시간) 벌어 둔다
- "다녀오신 동안" 창 — 방치형에서 다시 켤 이유는 이 창 하나에 걸려 있다
  (실측: 16.0M에서 껐다가 두 시간 뒤 켜니 250M이 쌓여 있었다)

### 대조 시험이 여덟 개가 됐다

```
✅ tap · file · bits · fmt · rng · content · sim · save
```

---

## 2026-08-20 · 장날 소식 — 텍스트 로그 말고 그래프

유저가 내 제안을 고쳐 줬다. 내가 말한 '잔재미'를 글자 로그로 알아듣고
**"텍스트로 하면 굳이 아냐? 그래픽 데이터는 재밌을 거 같은데"** —
그리고 예를 들었다: *막대로 "토끼들의 핫템 호미!", 원형으로 "낫을 제일
많이 사간 동물은 다람쥐!"*

내가 말한 잔재미는 로그가 아니라 화면 안의 반응(말풍선·튀는 엽전)이었지만,
유저가 낸 쪽이 **더 나은 기능**이라 그걸 먼저 만들었다.

### 이 기능의 함정은 하나뿐이다 — 누적으로 세면 굳는다

처음 연 품목이 판매 개수에서 영원히 1등이라, 두어 시간이면 순위가 다시는
안 바뀐다. **안 바뀌는 화면은 두 번째부터 아무도 안 본다** — 넣은 이유가
통째로 사라진다. 유저도 "이번주 핫템"이라고 말하며 이미 이걸 짚고 있었다.

### '주' 대신 **오일장**

조선에는 닷새마다 장이 섰다. 게임에 이미 장(場)이 있으니 말이 저절로 맞는다 —
"이번 장", "지난 장"이 그대로 쓰인다. 일주일은 이 세계에 없는 말이다.

게임내 하루 = 실제 8분, 닷새가 한 장(40분). 한 번 앉으면 장이 한 번은 서고,
하루 이틀 지나면 순위가 뒤집힌다.

### 그래프보다 **한 줄 소식**이 주인공

```
이번 장 핫템 — 고운붓!
제일 많이 사간 건 🐦 까치!
```

이게 먼저 읽히고, 그래프는 그 말의 근거다. 그래프를 먼저 놓으면 숫자를 읽어야
뜻을 알게 되는데 그건 재미가 아니다.

### 색을 이름표로 쓰지 않는다

보통 그래프는 "파란 건 토끼, 빨간 건 까치"처럼 **색이 이름표**다.
그러면 색을 잘 구분 못 하는 사람은 못 읽는다(남자 스무 명 중 한 명쯤).

여기서는 조각마다 🐰·🐦를 직접 붙였다. 색은 이름표가 아니므로
**진하기만** 달리하면 된다 — 그건 누구에게나 구분된다.
막대도 한 색으로만 그린다. 막대마다 색이 다르면 색에 뜻이 있는 줄 알고 읽는다.

원형은 **다섯 조각을 안 넘긴다**(넷 + '그 밖에'). 여섯이 넘어가면 사람 눈이
각도를 못 견준다. 손님 열둘을 다 그리면 아무것도 안 읽힌다.

### 등수를 정렬로 뽑지 않는다

Godot의 `sort_custom`은 같은 값끼리의 순서를 보장하지 않는다. 개수가 같은 두
품목의 등수가 **화면을 볼 때마다 뒤바뀐다.** 그래서 훑어서 제일 큰 것을
뽑는 방식으로 짰다(먼저 나온 쪽이 이긴다).

### 여기서 갈림길을 하나 지났다

장부는 **Godot에만** 넣었다. 자바스크립트판에는 안 넣는다.
이제부터 **자바스크립트판은 답안지로 굳는다** — 이미 옮긴 것이 맞는지 대조하는
데만 쓰고, 새 규칙은 Godot에서만 자란다. 대조 시험 여덟 개는 그대로 초록불이다
(장부는 경제를 건드리지 않으므로 대조가 흔들리지 않는다).

---

## 2026-08-20 · 폰 준비 — 지금 안 하면 화면을 다시 짜야 하는 것

유저 질문: **"Godot으로 하면 아이폰에서도 작동하는가?"**

된다. 다만 애플이 요구하는 게 있고(맥·Xcode·개발자 계정), 그건 **낼 때**의 일이다.
지금 해야 하는 건 따로 있었다 — 프로젝트에 **화면 설정이 하나도 없었다.**

### 왜 이건 미루면 안 되나

폰마다 가로세로 비율이 다르다. 기준 크기와 늘리는 방식을 안 정해두면
어떤 폰에서는 글자가 잘리고 어떤 폰에서는 여백만 크다. 그걸 나중에 고치려면
**모든 자리를 다시 잰다** — 지금까지 잡아 둔 단추·창·현판 위치가 전부 헛일이 된다.

| | |
|---|---|
| 기준 크기 | 430×932 (요즘 아이폰) |
| 늘리는 방식 | `canvas_items` · `expand` — 남는 자리를 잘라내지 않고 더 보여준다 |
| 방향 | 세로 고정 |
| 손가락 | 누름을 마우스처럼 다룬다 — 지금 짜 둔 누르기·끌기가 폰에서 그대로 돈다 |

`expand`를 고른 이유: 잘라내는 방식이면 화면이 짧은 폰에서 아래 단추가 밖으로
나간다. 늘려 보여주는 쪽이면 마을이 조금 더 보일 뿐 단추는 늘 제자리다.

길쭉한 폰(430×932)과 뭉툭한 폰(360×640) 두 가지로 찍어 확인했다.

### 아이폰·안드로이드 정리

- **아이폰**: 무료 계정으로 **내 폰에 넣어보는 것**까지는 공짜다(7일마다 다시 넣어야 한다).
  남한테 주거나 앱스토어에 올리려면 연 $99.
- **안드로이드**: 파일 하나 만들어 폰에 넣으면 끝. 계정도 심사도 없다.
  **그래서 폰에서 만져보는 시험은 안드로이드가 편하다.**

---

## 2026-08-20 · 가게마다 다른 강화 다섯 — 그리고 재보고 둘을 갈아엎은 이야기

가게가 다섯인데 하는 일이 다 똑같았다. 대장간을 키우는 재미와 약재상을 키우는
재미가 구별이 안 됐다. 그래서 **그 가게에서만 살 수 있는 강화**를 하나씩 붙였다.

| 가게 | 강화 | 하는 일 |
|---|---|---|
| 대장간 | 🔥 풀무 | 이 가게가 만드는 시간 −6% (5단계) |
| 필방 | ⚫ 먹 갈기 | 이 가게 물건값 +8% (5단계) |
| 지물포 | 📜 의뢰방 | 마을 의뢰가 한 자리 더 걸린다 (3단계) |
| 옹기점 | 🏺 질그릇 한 벌 | 이 가게 물건을 한 번에 15% 더 사간다 (5단계) |
| 약재상 | 🌾 약재 말리기 | 5% 확률로 판 값을 두 배 (5단계) |

값은 **그 가게 첫 승급값 × 0.05 / 0.12 / 0.25 / 0.5 / 1.0**이다. 가게마다
승급값이 다르니 강화값도 저절로 그 가게 크기에 맞는다.

### 먼저 만든 것이 잴 도구였다

자바스크립트판은 이제 답안지라 새 규칙이 없다. 그러니 `tools/balance.mjs`로는
**이 강화를 아예 잴 수가 없다.** 그래서 `godot/tests/balance.gd`를 새로 만들었다.

이 도구의 가상 플레이어는 대조 시험 쪽(`RunSim.act`)과 **일부러 다른 사람**이다.
저쪽은 답안지와 한 줄씩 같아야 해서 새 강화를 못 산다. 이쪽은 산다 — 안 사면
그 강화가 경제에 뭘 하는지 볼 방법이 없다.
(*"도구가 안 써 보는 기능은 도구에게 없는 것이다"* — 이 프로젝트에서 네 번 당한 규칙.)

### 재보니 두 개가 헛것이었다 (8시간 · 씨앗 1 · 누적매출)

| 강화 | 8시간 매출 | |
|---|---|---|
| 끔 | 855B | 기준 |
| 먹 갈기 | 1.37T | +60% |
| 풀무 | 1.16T | +36% |
| 약재 말리기 | 1.13T | +32% |
| **건조장**(진열대 +10칸) | 894B | **+5% — 사나 마나** |
| **큰 가마**(8% 확률로 두 개) | 727B | **−15% — 사면 손해** |

둘 다 *"더 만들고 더 쌓는"* 쪽이었다. 그런데 이 게임의 문제는 못 만드는 게
아니라 **그 가게 물건이 안 팔려서 쌓이는 것**이다. 그러니 더 만들어봤자
진열대만 차고, 그 돈으로 레벨을 올렸으면 벌었을 것을 못 번다.

젬 강화 **'너른 매대'**를 뺐을 때와 **똑같은 함정을 두 번째로 밟았다.**
이 게임에서 "공급을 늘리는 강화"는 거의 다 헛것이라고 봐도 된다.

### 그래서 둘을 수요 쪽으로 갈아엎었다

- 지물포 `건조장` → **`의뢰방`**: 의뢰 자리가 늘어난다. 의뢰를 건 마을은
  그 물건을 먼저 집으니, **쌓인 것이 나가는 길**이 하나 더 생긴다.
- 옹기점 `큰 가마` → **`질그릇 한 벌`**: 이 가게 물건은 한 번에 더 집어간다.
  만드는 쪽이 아니라 **나가는 쪽**을 넓혔다.

| 고친 뒤 | 8시간 매출 | |
|---|---|---|
| 끔 | 855B | 기준 |
| 먹 갈기 | 1.37T | +60% |
| **의뢰방** | **1.22T** | **+43%** (전 +5%) |
| 풀무 | 1.16T | +36% |
| 약재 말리기 | 1.13T | +32% |
| **질그릇 한 벌** | **1.06T** | **+24%** (전 −15%) |
| 전부 켬 | 1.16T | +36% |

**'전부 켬'이 '먹 갈기 하나'보다 낮은 건 버그가 아니다.** 강화값도 돈이라
다섯을 다 사면 그만큼 레벨을 못 올린다.

### ★★ 그리고 다음 날, 이 표가 통째로 틀렸다는 걸 알았다 (2026-08-21)

위 표는 **씨앗 하나로 잰 것**이다. 씨앗을 바꿔 보니 이야기가 뒤집혔다.

| 8시간 매출 | 씨앗1 | 씨앗2 | 씨앗3 | 씨앗4 | 씨앗5 |
|---|---|---|---|---|---|
| 끔 | 0.85T | 1.25T | 1.15T | 1.04T | 1.03T |

**아무것도 안 바꾸고 씨앗만 바꿔도 0.85T ~ 1.25T로 흔들린다.**
재려던 효과(±30%)보다 판마다의 운(+46%)이 더 컸다. 그러니 위의 "+43%",
"+24%"는 운을 실력으로 착각한 값이다. *`CLAUDE.md` 첫 줄에 내가 적어 둔
"짐작하지 말고 잰다 · 씨앗을 바꿔 두세 번 더 돌린다"를 내가 어겼다.*

씨앗 다섯으로 다시 쟀다. 아래는 **안 산 판 대비 배수의 가운뎃값**과
다섯 판 중 이긴 횟수다.

| 강화 | 배수 | 이김 | 판정 |
|---|---|---|---|
| ⚫ 먹 갈기 | **1.21배** | 4/5 | 좋다 |
| 🔥 풀무 | **1.09배** | 4/5 | 괜찮다 |
| 📜 의뢰방 | 0.99배 | 2/5 | 사나 마나 |
| 🌾 약재 말리기 | 0.88배 | 2/5 | **손해** (씨앗 1에선 +32%로 보였다) |
| 🏺 질그릇 한 벌 | 0.82배 | 1/5 | **손해** (씨앗 1에선 +24%로 보였다) |

**규칙이 하나 보인다.** 이긴 둘은 **먼저 여는 가게**(대장간·필방)이고,
진 셋은 **나중에 여는 가게**다. 강화값을 그 가게 승급값에 맞춰 놨는데
뒷가게는 승급값이 어마어마해서 다섯 단계가 승급 두 번 값이다. 게다가
늦게 열리니 그 돈이 일해 줄 시간도 짧다. 그 돈으로 물건 레벨을 올렸으면
벌었을 것을 못 버는 것이다.

**아직 안 고쳤다.** 다음에 손볼 때는 값을 짐작으로 만지지 말고
**씨앗 다섯으로 재는 것부터** 한다. 한 판은 판단의 근거가 못 된다.

### 난수 순서를 밀지 않게 막은 것

확률을 쓰는 강화(약재 말리기)는 **안 샀으면 주사위를 아예 안 굴린다.**
0% 확률이라도 굴리면 난수 순서가 한 칸씩 밀려서, **기능을 넣기만 해도**
옛 판과 다른 게임이 된다. 저장본도 대조 시험도 그 자리에서 어긋난다.

강화를 안 산 채로 대조 여덟 개가 전부 초록불인 것으로 확인했다.

---

## 2026-08-21 · 잔재미 — 손님 머리 위에 뜨는 것 셋

화면에 **말이 없었다.** 손님이 오고 돈이 오르는데, 왜 그 물건을 여는지,
왜 저 손님이 빈손으로 가는지가 아무 데도 안 적혀 있었다. 셋을 넣었다.

| | 언제 뜨나 |
|---|---|
| 말풍선 `"혹시 도끼 파시오?"` | 손님이 아직 안 연 물건을 물어볼 때. 그 가게 지붕 위 |
| 💢 | 하나도 못 사고 돌아갈 때 |
| 😠 | 줄에서 9초 넘게 기다릴 때. 제 차례가 오면 바로 푼다 |

### 💢를 띄우려면 빈손 손님을 **들여보내야** 했다

여태 `on_sale`은 산 게 없으면 문 앞에서 되돌려보냈다. 그러니 화날 손님이
마을에 아예 안 들어왔다 — 없는 손님 머리 위에는 아무것도 못 띄운다.
못 산 물건의 가게 앞까지는 걸어가게 두고, **계산대 앞에 서 본 뒤에** 💢를 띄운다.
걸어 들어올 때부터 화가 나 있으면 '묻지도 않았는데 왜 화났지'가 된다 —
자바스크립트판에서 한 번 겪은 실수라 이번엔 처음부터 그렇게 짰다.

### 또 걸렸다 — 도구가 안 보는 줄에 시계를 뒀다

말풍선 시계를 `_process`에 뒀는데, 빨리 감기(`shot.gd`)는 `_process`를
안 거치고 `_advance`만 부른다. **도구에게는 말풍선이 영영 안 사라지는 물건**이
될 뻔했다. 시계를 `_advance`로 옮겼다. CLAUDE.md 세 번째 규칙에 다섯 번째로
걸린 셈인데, 이번엔 넣자마자 찍어봐서 그 자리에서 잡았다.

### 드물게 나오는 장면은 그냥 찍으면 안 걸린다

말풍선은 여덟 시간에 네 번쯤 뜬다(4분간 재보니 물어보기 4번, 4.5초씩 —
전체의 3.5%). 30분 감아서 찍으면 열 번에 아홉 번은 빈 화면이라,
"안 되는 건지 이번에만 없는 건지"를 못 가른다. 찍는 도구에 둘을 붙였다:

```sh
SHOT_UNTIL=bubble godot --path godot tests/shot.tscn   # 나올 때까지 감았다가 찍는다
# 찍을 때마다 한 줄 남긴다: SHOT 손님 4 · 빈손💢 0 · 말풍선 도끼?
```

**그림에 없는 것을 글로도 남긴다** — 이게 없으면 스크린샷을 눈으로 뒤지게 된다.

### 눈으로 봐서 잡은 것

첫 판은 말풍선이 **현판을 덮어서** 가게 이름이 안 보였다. 현판이 N.y−72~−40에
있는데 말풍선을 −86에 뒀으니, 꼬리까지 재면 겹치는 게 당연했다. −116으로 올렸다.
코드만 읽었으면 절대 못 봤을 종류다.

### 대조

여덟 개 전부 초록불. 화면만 만졌으니 당연하지만, **당연한 것도 돌려서 본다** —
`sim`을 안 건드렸다는 걸 증명하는 게 이 시험의 값이다.

---

## 2026-08-21 · 소리는 자리만 잡아 뒀다 · 돈 소리는 **일부러 안 넣는다**

유저 판단: *"돈 올라가는 소리는 나중에 너무 많아져서 노이즈처럼 들릴 수 있다."*
맞다. 후반에는 초당 수십 번이 팔린다. 그때 파는 순간마다 울리면 그건 소리가
아니라 소음이고, **사람은 소음을 끄지 소리만 골라 끄지 않는다.** 소리 설정을
꺼 버리면 나머지 소리도 같이 죽는다 — 하나 때문에 전부를 잃는 셈이다.
돈이 오르는 건 이미 숫자와 🪙 표로 보인다. 귀까지 쓸 이유가 없다.

### 소리 파일은 한 장도 없는데 왜 지금 넣나

넣은 것은 소리가 아니라 **부르는 자리**다(`view/sfx.gd`). 나중에 파일이 왔을 때
"어디서 울려야 하지"를 다시 찾으면 화면 코드를 통째로 다시 읽어야 한다.
그림을 도형 자리에 끼우기로 한 것과 같은 수법이다.

| 소리 | 언제 |
|---|---|
| tap | 매대를 눌러 강화했다 |
| open | 칸·가게·건물·삽살개를 새로 열었다 |
| catch | 나쁜 놈을 잡았다 |
| fair | 장을 열었다 |
| quest | 의뢰를 마쳤다 |
| guest | 새 손님이 왔다 |
| ~~sell~~ | **없다.** 위의 이유 |

**된 것만 울린다.** 돈이 모자라 실패한 누름은 소리를 안 낸다 — 안 된 일에
소리가 나면 됐다고 착각한다.

### 재 봤다 — 이 소리들은 소음이 안 되나

두 시간을 감아서 셌다: **손님 5번 · 의뢰 10번**. 시간당 일곱 번쯤이다.
나머지(tap·open·catch·fair)는 손가락이 눌러야 나는 것이라 저절로 늘지 않는다.
*짐작이 아니라 센 값이다.*

### 또 걸렸다 — 이번엔 뿌리를 뽑았다

소리를 붙이고 30분을 감아 찍었더니 **"소리 없음"**이 나왔다. 게임에서는
울리는데 도구에게만 안 울린 것이다. 원인은 `shot.gd`가 '게임 한 걸음'을
**베껴 적고 있던 것** — `main._process`에 뭘 하나 붙일 때마다 도구는 그걸 못 본다.

이건 CLAUDE.md 세 번째 규칙에 여섯 번째로 걸린 것이고, 앞의 다섯 번과 원인이
같다. 그래서 이번엔 그 자리만 고치지 않고 **한 걸음을 `main.step(delta)` 하나로
모았다.** `_process`도 도구도 같은 것을 부른다. 베낀 것은 반드시 뒤처지니,
베낄 것을 없앴다.

확인: 같은 30분에 `소리 guest 3`. 두 시간에 `guest 5 · quest 10`.

### 남은 것

소리 파일. 그때 할 일은 `KINDS`의 값 자리에 파일 이름을 넣는 것뿐이다.

---

## 2026-08-21 · 유저가 폰에서 만져 보고 여섯 가지 · 그중 넷을 고쳤다

### 1·2. 크게·작게가 아예 없었다 · 처음 크기가 너무 멀었다

없는 게 맞았다. 넣었다 — **두 손가락**(폰) · 트랙패드 오므리기 · 휠(책상).
누른 자리를 붙잡고 커진다. 화면 한가운데를 기준으로 하면 보고 있던 가게가
손 밑에서 도망간다.

칸을 정해 뒀다. 마음대로 줄이면 마을이 개미만 해지고, 키우면 손님이 화면을 덮는다.

| | 왜 |
|---|---|
| **1.00 기본** | 가게 하나(3×3 마당 = 288px)가 폭 430 안에 꽉 찬다. 찍어 보고 정했다 |
| 0.45 최소 | 가게 셋이 한눈에. 마을을 훑는 크기 |
| 1.80 최대 | 매대 하나를 정확히 누를 만큼 |

전에는 0.62였다. 마을이 작아 보이는 대신 무엇 하나 또렷하지 않았다.
찍는 도구에 `SHOT_ZOOM`을 붙여서 눈으로 견줘 정했다.

### 5. "계산대를 눌러야만 창이 열리는 것 같다" — 맞았다

매대 판정이 **104×92짜리 네모**였다. 칸은 96×48이니 두 칸 몫을 먹은 셈이고,
마당 어디를 눌러도 매대가 먼저 잡혔다. 창으로 가는 길은 현판뿐이었다.

더 아픈 건 **우리가 이걸 알고도 시험을 그쪽에 맞춘 것**이다. 8월 20일 기록에
"마당 한가운데는 매대 판정에 먹히니 현판으로 시험한다"고 적어 뒀다.
고장을 재현하는 대신 고장을 시험에 새겨 넣은 것이다.

판정을 **제 칸 모양(마름모) 그대로**로 좁혔다. 매대 몸통 높이만큼만 위로 올린다.
시험도 되돌렸다 — 마당의 빈 칸을 눌러 창이 열리는지 본다.

### 3. 손님이 시계처럼 온다 — 맞았다

토끼·까치는 `wild: 0`이었다. 4.6초·3.1초마다 **정확히** 온다.
8월에 이렇게 정한 이유가 기록에 있다("길에 사람이 끊기면 안 되니까").
이유는 맞지만 정도가 셌다 — 끊기지 않게 하려다 메트로놈이 됐다.

`0.25 / 0.25 / 0.3`으로 조금만 줬다(평균 간격은 그대로다).

**재 봤다 — 8시간 × 씨앗 셋, 누적매출:**

| 씨앗 | 전 | 후 |
|---|---|---|
| 1 | 1.16T | 1.11T |
| 2 | 1.14T | 1.16T |
| 3 | 1.33T | 1.13T |

중앙값 1.16T → 1.13T (−2.6%). **씨앗끼리의 차이(1.14~1.33T)보다 작다** —
경제는 그대로고 느낌만 바뀌었다는 뜻이다. 이게 원하던 것이다.

### 6. 20성 승급은 뽑기가 아니라 **방문 횟수**다 (유저 물음)

유저가 "뽑기로 승급되는 줄 알았는데 왜 저절로 올랐나"라고 물었다.
지금 만들어진 것은 **온 횟수**로 오른다(`visits`). 뽑기는 아직 없다.

- 20등급 전부 있다 — 뜨내기 → 스치는 손 → … → 정승 → 상감마마
- 필요한 방문 수는 손님마다 다르다(`REGULARS.at / every`) — 자주 오는
  토끼는 많이 와야 하고, 드문 호랑이는 적게 와도 오른다
- 오르면 사는 양과 값이 같이 오른다(20성이면 ×2 남짓)

**뽑기로 바꿀지는 아직 안 정했다.** 정하려면 "누가 왜 돈을 내는가"부터
답해야 한다 — PROCESS.md가 비워 둔 그 칸이다.

### 4. 장부 정리 삭제는 **아직 안 했다** — 되물었다

지우면 후반 강화가 전부 손가락이 된다. 옛 측정에 그 숫자가 있다:
자동 강화가 없을 때 **1,986번**을 눌러야 했다. 그래서 그냥 지우기 전에
"단추가 싫은 건지, 기능이 싫은 건지"를 물었다.

---

## 2026-08-21 · 가게 창을 다시 짰다 — 유저가 캡처 한 장으로 여섯 가지

유저가 다른 게임 화면을 캡처해서 보내며 여섯 가지를 짚었다. 전부 반영했다.

### 1. 닫기 단추를 없앴다

창 밖을 누르면 닫히고, 아래 단추를 다시 누르면 닫힌다. **닫는 길이 셋이면
그중 둘은 자리만 먹는다.** 제목 줄이 통째로 비어서 화살표가 들어갈 자리가 생겼다.

### 2·3. 화살표로 옆 가게 · 열면 그 가게로 지도가 간다

`‹ ⚒ 대장간 무쇠 ›` — 화살표를 누르면 **연 가게들**만 차례로 돈다.
안 연 가게로 넘어가면 빈 창이 나오니 건너뛴다.

그리고 창을 열 때 지도가 그 가게로 간다(줌 1.25). 여기서 하나 걸렸다 —
**화면 한가운데에 놓으면 방금 연 그 가게가 창 뒤로 숨는다.** 창이 아래
540px을 덮기 때문이다. 남는 위쪽 자리(392px)의 한가운데에 놓는다.

### 4. 단추 셋을 하나로 — 그리고 꾹 누르기

예전엔 `[레벨업][×10][최대]` 셋이었다. **셋은 고민거리가 아니라 산수 문제다** —
"지금 돈으로 몇 번이 되지?"를 사람이 대신 풀어야 했다.

- 평소엔 `레벨업 🪙6.35K` 하나
- **꾹 누르면** 계속 오른다. 첫 0.35초는 쉬고, 그다음 초당 열두 번.
  안 쉬면 톡 누른 것도 서너 단계가 올라가서 "한 단계만"을 할 수가 없다.
  너무 빠르면 멈추고 싶은 데서 못 멈춘다
- 지금 돈으로 만렙까지 갈 수 있으면 단추가 **스스로 `최대 Lv.30`으로 바뀐다**.
  그때는 한 번 누르는 게 맞고, 그걸 사람이 알아챌 이유가 없다

> **함정 — 0.3초마다 다시 그리는 창에서 꾹 누르기.**
> 이 창은 돈이 오르면 단추가 켜지도록 0.3초마다 통째로 다시 그린다.
> 그러면 누르고 있던 단추가 사라지고 `button_up`이 영영 안 온다 —
> **손을 뗐는데 계속 올라가는** 고장이다. 그래서 뗀 것은 단추가 아니라
> **창이 직접 듣는다**(`_input`). 다시 그려져도 상관이 없다.

### 5. 물건마다 그림 자리

지금은 이모지가 그 자리에 선다. `art/items/<id>.png`가 오면 거기만 갈아 끼운다.
그림 주문서(ASSETS.md)에도 적었다 — **같은 그림이 매대 위와 목록 썸네일
두 군데에 쓰인다.** 작게 줄여도 알아볼 수 있어야 하니 잔무늬보다 실루엣이 중요하다.

### 6. 갈피 셋 — 제품 · 일손 · 승급

한 줄로 다 쏟았더니 매대가 여덟 칸까지 늘면 승급 조건은 스크롤 저 아래였다.
**자주 만지는 것과 가끔 보는 것은 같은 화면에 있을 이유가 없다.**

| 갈피 | 담은 것 |
|---|---|
| 제품 | 매대 목록 · 레벨업 · 칸 열기 |
| 일손 | 직원 · 이 가게만의 강화(풀무·먹 갈기…) — 둘 다 "한 번 사면 계속 도는 것" |
| 승급 | 조건 체크리스트(✅/⬜)와 눈금 · 승급 단추 |

찍는 도구에 `SHOT_TAB`을 붙였다. 갈피를 손으로 눌러 봐야만 보이면
그 화면은 도구에게 없는 것이나 마찬가지다.

---

## 2026-08-21 · 유저가 다시 일곱 · 그리고 **시험이 저장본을 물려받고 있었다**

### 먼저, 오늘 제일 큰 것 — 시험이 빈손으로 시작하지 않았다

화살표를 고치고 누르기 시험을 돌렸더니 **"매대를 눌렀는데 레벨이 안 올랐다"**가
넷이나 나왔다. 방금 만진 것과 아무 상관 없는 자리였다.

되돌려 봐도 그대로였다. 그래서 상태를 찍어 봤더니:

```
0 곡괭이 open=true lv=30.0 max=30.0 afford=0     ← 이미 만렙이었다
smalls=2                                          ← 건물도 이미 서 있었다
```

**시험이 내 저장본을 물려받고 있었다.** 저장이 들어온 뒤로 `main._ready`가
켤 때마다 `user://save.dat`을 읽는데, 시험 장면도 그 main을 통째로 얹어 쓴다.
스크린샷을 찍느라 30분짜리를 몇 번 돌렸더니 그게 저장됐고, 그다음부터
누르기 시험은 **만렙인 가게를 눌러 놓고 "안 오른다"고 실패**하고 있었던 것이다.

고쳤다 — main은 **자기가 이 판의 주인공일 때만** 저장본을 읽고 쓴다
(`get_parent() == get_tree().root`). 시험 장면 안에 얹혀 있으면 손대지 않는다.

씨앗도 못 박았다. main은 시계로 씨앗을 잡는데(사람이 놀 때는 그게 맞다)
시험이 그걸 그대로 쓰면 **돌릴 때마다 다른 판**이 된다. 실제로 쥐가 마침
매대 앞에 서 있으면 그 누름이 쥐잡기로 먹혀서, 어떤 날은 통과하고
어떤 날은 실패했다. 시험 안에서는 씨앗 7로 고정하고 나쁜 놈을 치운 뒤 시작한다.

> 이 프로젝트의 세 번째 규칙("도구가 안 써 보는 기능은 도구에게 없는 것")의
> 반대편이 여기 있었다 — **도구가 쓰지 말아야 할 것을 쓰고 있었다.**

### 3·4·5. 화살표가 "어쩔 땐 되고 어쩔 땐 안 된다"

원인은 하나였다. 이 창은 0.3초마다 통째로 다시 그린다(돈이 오르면 단추가
켜져야 하니까). 그런데 **누른 순간과 뗀 순간 사이에 그 일이 벌어지면
단추가 사라진다.** Godot은 누른 단추와 뗀 단추가 같을 때만 '눌렸다'로 치므로,
사라진 단추는 아무 일도 안 한다. 0.3초와 손가락 속도가 비슷하니 반반이었다.

게다가 뗀 자리가 빈 곳이 되면 그 누름이 지도까지 흘러가서 **창이 닫힌다** —
유저가 본 "이전 가게를 눌렀는데 창이 닫힌다"가 이것이다. 순환은 원래 되고
있었다(대장간에서 ‹ 를 누르면 마지막 가게로 간다).

고친 방법: **손가락이 닿아 있는 동안은 다시 그리지 않는다.**
꾹 눌러 강화하는 중일 때만 예외로 다시 그린다(숫자가 올라가는 걸 봐야 하니까).
그리고 레벨업은 '눌렸다'가 아니라 **'누르기 시작했다'**에 걸었다 — 그래야
다시 그려지든 말든 한 단계는 확실히 오른다.

### 2. 다음 가게가 없으면 비활성

열린 가게가 **하나뿐일 때** 양쪽 화살표가 꺼진다. 둘 이상이면 순환하므로
'다음이 없는' 경우가 없다(5번이 원한 것이 순환이다).

### 1. 장부 정리를 뺐다

유저 판단: *"어차피 만렙은 언젠가 나옴."* 맞다. 게다가 오늘 **꾹 눌러 강화**와
**'최대' 단추**가 생겨서, 자동 강화가 덜어 주던 수고의 대부분이 이미 사라졌다.

**재 봤다 (8시간 × 씨앗 셋, 누적매출):**

| | 씨앗1 | 씨앗2 | 씨앗3 | 중앙값 |
|---|---|---|---|---|
| 장부 정리 있음 | 1.11T | 1.16T | 1.13T | 1.13T |
| **뺌** | 1.10T | 1.29T | 1.37T | **1.29T** |

빠지는 쪽이 오히려 높다(45,000을 안 내니까). 곡선이 무너지지 않는다.

**sim에서 도려내지는 않았다.** 자바스크립트판이 답안지라 규칙을 지우면
대조가 그 자리에서 깨지고, 옛 저장본도 못 읽는다. 대신 **살 길을 없앴다** —
단추를 지웠고, 재는 도구도 안 사게 했다(재는 판과 노는 판이 다르면
잰 값이 아무 말도 안 해준다). 대조 시험 쪽 배우만 답안지에 맞춰 그대로 산다.

### 6. 직원 값을 절반으로 · 마을 바깥을 두 칸 깔았다

**직원 값** 첫 승급값의 12% → **6%**. 직원 한 명은 기다림을 79% → 0.7%로
지우는, 이 게임에서 가장 확실한 한 방이다. 그런 걸 한참 모아야 살 수 있게
두면 그 구간 내내 손님이 물건을 못 받고 돌아간다 — **벌 받는 쪽이 사는 사람**이다.

재 보니 중앙값은 그대로(1.29T)인데 **손님 열둘이 다 오는 시각이
6시간31~33분으로 모였다**(전에는 6:34~6:45로 흩어졌다). 값이 싸지면서
초반 병목이 사라진 것이지, 돈이 불어난 게 아니다.

**마을 바깥** 두 칸(`Iso.EDGE`)을 풀밭으로 더 깔고, 카메라도 거기까지
밀 수 있게 했다. 딱 마을 끝에서 막으면 구석 가게는 영영 화면 구석에 붙어
있는다 — 줌을 키우면 더 답답하다. 창이 열려 있으면 그 높이(540px)만큼
더 내려갈 수 있게 했다. 안 그러면 창 뒤에 깔린 가게를 끌어올릴 수가 없다.

### 7. 손님이 한 군데서만 나오던 것

`ENTRY`가 (6,0) 하나였다. 길이 여섯 갈래인데 쓰는 목이 하나면 마을이 좁아 보인다.
길이 마을 가장자리에 닿는 칸 **열둘**을 `Iso.GATES`로 모아, 들어오는 목과
나가는 목을 손님마다 따로 고른다.

**주사위는 화면 전용이다**(`Village._grng`). sim의 주사위를 한 번이라도
굴리면 대조 시험이 그 자리에서 어긋난다 — 화면은 sim을 읽기만 한다.

---

## 2026-08-21 · "승급하면 뭐가 좋아지나"를 화면에 적었다 · 그리고 직원은 안 늘어난다

### 유저 물음 — 직원은 언제 늘어나나

**안 늘어난다.** 등급을 올려도 가게마다 한 명 그대로다.

그런데 `content.js` 주석에는 *"자리 수는 등급이 정한다: 무쇠급 1명 · 참쇠급
2명 · 강철급 3명"*이라고 적혀 있었다. **코드는 한 번도 그런 적이 없다.**
그건 재기 전에 적어 둔 계획이고, 재 보니 둘째부터 아무 효과가 없어서 1로
못 박았는데(8시간 측정: 직원 2·3·4·6명이 전부 오차 범위) 주석만 옛말로 남았다.

> **문서가 코드보다 오래 산 예다.** 유저가 그 주석을 읽고 물어본 것이니,
> 실제로 사람을 헷갈리게 했다. 주석을 사실로 고쳤고, 게임 화면에도 적었다 —
> "일손은 여기까지다. 승급해도 안 늘어난다"와 그 이유까지.
> 안 적으면 "승급하면 늘겠지" 하고 기다리는데, 그런 날은 오지 않는다.

### 승급하면 무엇이 좋아지나 — 재서 적었다

조건만 다섯 줄 늘어놓고 좋은 점을 안 적으면 **"이걸 왜 해야 하지"**가 된다.
목표는 조건이 아니라 그 뒤에 오는 것이다. 이제 이렇게 나온다:

```
승급하면
· 값이 ×4 — 물건 이름도 '참쇠'로 바뀐다
· 레벨 상한 30 → 60
· 매대 4칸 → 6칸
· 레벨은 1로 되돌아간다 — 벌이가 잠깐 46%로 떨어졌다가,
  9레벨이면 본전이고 상한까지 올리면 14배다
```

### 나쁜 소식을 같은 크기로 적은 이유

승급은 **모든 레벨을 1로 되돌린다.** 재 봤더니 무쇠 30레벨 곡괭이가
참쇠 1레벨이 되면서 초당 벌이가 **0.46배**로 떨어진다(값은 ×4인데 레벨
보정 4.48배와 만드는 시간 단축이 통째로 날아가서 그렇다).

이걸 안 적으면 누른 사람은 **"고장 났나"** 한다. 그리고 그게 게임을 끄는
이유가 된다. 그래서 떨어지는 폭, 본전 되는 레벨, 끝까지 올렸을 때의 배수를
전부 적는다 — 나쁜 소식과 좋은 소식을 같은 크기로.

### 미리보기는 **진짜 식에게 물어본다**

`Sim.promote_gain()` — 값 계산을 여기서 다시 쓰지 않는다. 베낀 식은 반드시
뒤처지고, 그러면 미리보기만 조용히 옛말을 하게 된다. 대신 **잠깐 승급시켜
보고 재고 그대로 되돌린다.** 없던 칸을 만들어 놓고 오지 않도록 지우는 것까지
챙겼다 — 안 그러면 창을 한 번 열었다는 이유로 저장본이 달라진다.

---

## 2026-08-21 · 그림을 받을 자리를 만들었다 — 폴더가 바뀌었다

유저가 이제 그림을 만들기 시작한다. 그 전에 **넣으면 바로 나오는 상태**로
만들어 뒀다. 목록만 채워 놓고 코드가 안 읽으면, 57장을 그려 놓고도
화면은 그대로인 채로 "왜 안 나오지"를 하게 된다.

### 폴더가 바뀌었다 — `art/` → `godot/art/`

**Godot은 제 프로젝트 폴더 밖의 파일을 못 읽는다.** 저장소 뿌리의 `art/`에
넣으면 주문서 목록에서는 사라지는데(도구가 폴더를 훑으니까) 화면에는 영영
안 나온다 — 제일 나쁜 종류의 어긋남이다. 통째로 옮기고 도구도 그쪽을 보게 했다.

### 크기를 두 배로 올렸다

| | 전 | 후(파일) | 화면 |
|---|---|---|---|
| 점장 | 72×72 | **144×144** | 72×72 |
| 물건 | 64×112 | **128×224** | 64×112 |
| 손님·나쁜 놈 | 64×64 | **128×128** | 64×64 |

요즘 폰은 한 자리에 점을 2~3개 찍는다. 화면 크기 그대로 받으면 뿌옇다.
게다가 이 게임은 이제 **0.45~1.8배로 크게·작게** 하므로 여유가 더 필요하다.
그림을 다 그린 뒤에 알면 57장을 다시 그려야 하니, **지금** 못 박는다.

### 없으면 없는 대로 그린다

`view/art.gd` — 있으면 그림, 없으면 여태 그리던 도형·이모지.
한 번 찾아본 것은 **없다는 사실까지** 기억한다(매 프레임 파일을 뒤지면
그리는 것보다 그 일이 더 비싸진다).

넣자마자 되는지 확인했다 — 지금 들어와 있는 세 장(점장 만들기·팔기, 토끼)이
바로 화면에 섰다. 점장은 팔 때와 만들 때 자세가 바뀐다(두 장으로 돌려 쓴다).

**발끝을 기준으로 놓는다.** 주문서에 "발끝이 아래 변에 닿게"라고 적은 이유가
이것이다 — 발끝이 곧 서 있는 자리이고, 앞뒤 가리기도 발끝 높이로 정한다.
왼쪽을 볼 때는 코드가 뒤집는다. 두 장씩 받으면 장수가 두 배가 되고
그 둘이 미묘하게 달라진다.

### 넣은 뒤 한 번

```sh
godot --headless --path godot --import
```

에디터를 켜 두었다면 저절로 한다. 안 하면 파일은 있는데 게임이 못 찾는다.
이것도 주문서 첫 칸에 적어 뒀다.

---

## 2026-08-21 · 점장과 직원을 가른다 · 그리고 **직원은 화면에 없었다**

유저 안: 점장 너구리와 직원 너구리를 구분하고, 제일 좋은 건 가게별 점장
(대장간은 망치, 필방은 앞치마). 알바는 모자로 등급 — 머리띠 → 초립 →
패랭이 → 백립.

### 그림을 주문하려다 알았다 — 직원이 한 마리도 안 그려지고 있었다

`village.gd`에 `staff`라는 글자가 **한 번도 안 나온다.** 자바스크립트판에는
`_staff`가 있었는데 옮기면서 빠졌다. 직원을 뽑으면 sim은 제대로 셈하지만
**화면에는 아무도 안 나타난다** — 돈만 나가고 아무 일도 안 일어나는,
이 프로젝트가 제일 싫어하는 종류다.

> 더 뼈아픈 건 **같은 증상을 이미 한 번 고쳤다는 것**이다. 8월 20일에
> "3×3 마당은 안쪽 칸이 하나뿐인데 작업대가 그걸 써서 직원 설 자리가 0개"를
> 잡았다. 그때 **자리 규칙(`Iso.staff_spots`)은 옮겼는데 그리는 쪽을 안 옮겼다.**
> 그림을 주문하려고 목록을 짜지 않았으면 계속 몰랐을 것이다.

### 그리고 나서 또 하나 — 매대 뒤에 서서 머리만 보였다

그려 놓고 찍어 보니 직원이 매대 뒤에 가려 **머리만 삐죽** 나와 있었다.
3×3 마당에서 직원에게 남는 칸은 담벼락 칸뿐이고, 그 앞에는 매대가 서기 때문이다.
480K를 주고 머리 하나를 사는 셈이다.

칸 한가운데가 아니라 **마당 안쪽으로 26px 당겨** 세운다. 앞뒤 순서도 그 자리로
정한다 — 안 그러면 그림과 순서가 따로 논다.

*코드만 읽었으면 "직원 그리기 옮김"으로 끝났을 일이다. 찍어 봐서 잡았다.*

### 점장 자세 다섯이 이제 진짜로 쓰인다

여태 `raccoon-make`·`raccoon-sell` 둘만 썼다. 걷기 둘과 조는 것은
주문서에만 있고 코드가 안 봤다 — **주문할 이유가 없는 그림**이었다.

| 자세 | 언제 |
|---|---|
| sell | 파는 중(계산대 여운) |
| walk1 · walk2 | 작업대 ↔ 계산대를 오갈 때 번갈아 |
| sleep | **열린 물건이 전부 진열대까지 찼을 때** — 늘리라는 신호다 |
| make | 그 밖에 |

`sleep`은 직원에게도 쓴다. 다 찬 가게는 점장도 직원도 존다.

### 가게별 점장은 '있으면 쓰고 없으면 공통'

`clerks/smith-make.png`가 있으면 대장간만 그걸 쓰고, 없으면 공통 점장이 선다.
**한 가게만 그려도 그 가게만 바뀐다.** 그래서 합계(68장)에서는 뺐다 —
없어도 게임이 완성되는 것과, 없으면 빈자리가 되는 것을 같은 칸에 세면
"몇 장 남았나"가 거짓말이 된다.

### 직원 등급 이름은 `content.js`에 먼저 적었다

규칙은 아직 없다(전부 머리띠 알바 하나). 그런데 이름과 모자를 지금 적는 이유는
**그림을 지금 그리기 때문**이다. 주문서는 이 목록에서 자동으로 만들어진다.
목록을 도구 안에 적으면 나중에 규칙이 생길 때 두 군데가 되고, 두 군데는
반드시 어긋난다.

앞으로 붙일 규칙도 거기 적어 뒀다 — 낮은 등급일수록 잘 지치고 잘 존다.
**지킬 선: 알바 한 마리가 지금 직원 한 마리보다 나쁘면 안 된다.**
그러면 돈 주고 산 것이 손해가 된다(네 번째 규칙).

### 주문서 최종

**68장**(선택 10장 별도). 순서는 점장 5 → 직원 8 → 손님 12 → 물건 40 → 나쁜 놈 3.
"누가 누구인지"를 표로 못 박았다 — 점장은 크고(72) 그 가게의 일이 보이는 차림,
직원은 조금 작고(60) 모자로 등급, 손님은 아예 다른 짐승.

---

## 2026-08-21 · 점장과 직원은 **같은 크기**다 (유저 정정)

내가 "점장은 크게(72), 직원은 작게(60)"로 잡았는데 유저가 되돌렸다.
*"점장 너구리 클 필요 없어. 점장은 망치를 들고 있고 직원은 머리띠만 있고."*

맞는 지적이다. **크기로 가르면 직원이 덜 자란 너구리처럼 보인다.**
둘은 같은 너구리고 맡은 일만 다르다. 게다가 이 게임에는 이미 더 확실한
구분이 있다 — **서 있는 자리.** 점장은 작업대와 계산대를 오가고, 직원은
마당 안쪽에 붙박이로 선다. 자리가 이미 말해 주니 그림까지 크게 다를 필요가 없다.

가르는 것은 **손에 든 것과 머리에 쓴 것**뿐이다.

| | 어디가 다른가 |
|---|---|
| 점장 | **손에 연장** — 대장간은 망치, 필방은 붓과 앞치마 |
| 직원 | 연장 없이 **머리에 쓴 것**만 — 머리띠 → 초립 → 패랭이 → 백립 |

그림 값이 싸진다는 덤도 있다. 몸이 같으니 직원 네 등급은 **모자만 갈아
끼우면 된다.** 주문서도 그렇게 고쳤다(직원 그림도 144×144로 올렸다).

### 덤으로 하나 — 임시 도형이 그림과 키가 안 맞았다

그림이 들어온 너구리(점장)만 커 보이고, 아직 도형인 너구리는 작아 보였다.
그림 65장이 다 모일 때까지 몇 주는 이 상태로 볼 텐데, 그동안 화면이
들쭉날쭉하다.

그림 상자가 72px이니 도형도 60으로 키웠다 — **그리고 한 번에 틀렸다.**
찍어 보니 도형이 가게를 덮어 버렸다. **그림은 상자를 꽉 채우지 않는다.**
너구리 둘레에 투명한 여백이 있어서, 상자 크기에 맞추면 두 배가 된다.
찍어 보고 40으로 잡았다. *짐작으로 맞춘 숫자는 이렇게 한 번에 틀린다 —
이 프로젝트 첫 번째 규칙 그대로다.*

---

## 2026-08-21 · 손님 머리 위는 **주문표**다 · 등급 그림 · 마을을 숲으로 둘렀다

### 1. 동전이 있을 자리가 아니었다 (유저 지적)

손님 머리 위에 `🪙12.0K`가 떠 있었다. 유저가 "주문 리스트가 아닌 동전이
보이는데 잘못된 것 아닌가" — **맞다.**

그건 **아직 안 일어난 일**이다. 줄에 선 손님 머리 위의 돈은 "얼마 낼 사람"이라는
뜻이 되는데, 정작 **"무엇을 원하는지"는 어디에도 안 보였다.** 자바스크립트판에는
주문표(`_order`)가 있었는데 옮기면서 값만 남기고 빠뜨린 것이다.

- 머리 위 → **주문표**: `🪓 1 · 🪴 1` 처럼 무엇을 몇 개. 셋까지, 넘으면 `+N`
- 값 → **팔린 순간** 계산대 위로 떠올랐다 사라진다(`floats`)
- 주문표는 **맨 앞사람만.** 줄이 길 때 전부 띄우면 서로 덮어 하나도 안 읽힌다 —
  이건 자바스크립트판에서 이미 얻은 답이라 그대로 가져왔다

찍는 도구에 `SHOT_UNTIL=coin`을 붙였다. 엽전 표는 1.3초만 뜨는 물건이라
그냥 찍으면 안 걸린다.

### 2. 승급하면 물건 그림도 바뀐다

무쇠곡괭이 → 참쇠곡괭이 → 강철곡괭이. 이름은 이미 바뀌는데 그림은 그대로였다.

**그런데 40종 × 3등급 = 120장을 그릴 이유는 없다.** 그래서 두 겹으로 만들었다:

1. `pick-1.png`(2등급) · `pick-2.png`(3등급)가 있으면 **그것부터 쓴다**
2. 없으면 기본 그림에 **등급 테**를 두른다 — 2등급은 은빛, 3등급은 금빛

값을 **색으로** 말하는 것이라 물건이 쇠든 종이든 상관없이 통한다.
그리고 싶은 물건만 등급 그림을 그리면 된다. 가게별 점장과 같은 수법이다 —
**있으면 쓰고 없으면 공통**, 어디서 멈춰도 게임은 돈다.

### 3. 배경 — 마을을 숲으로 둘렀다

"배경 디자인도 안 잡히네." 마을 밖이 그냥 초록 벌판이었다. 둘을 했다:

- **마을 밖은 한 톤 어둡게.** 같은 색으로 깔면 어디까지가 마을인지 안 보이고,
  밀 수 있는 데까지 밀어 놓고 "여긴 뭐지" 하게 된다
- **테두리에 나무를 빽빽하게.** 마을이 벌판 한가운데 뚝 떨어져 있는 것보다
  숲으로 둘러싸인 편이 "여기가 골짜기"라는 말이 된다.
  **길은 비웠다** — 길은 숲 사이로 이어져 나가는 것처럼 보여야 한다

이건 첫 삽이다. 하늘·안개·계절 같은 건 그림이 들어온 뒤에 봐야 한다 —
지금 도형 위에 분위기를 얹으면, 그림이 오면 다시 만들게 된다.

---

## 2026-08-21 · "레벨업 누르면 창이 닫힌다" — 같은 고장을 두 번 냈다

유저가 잡았다. 원인은 **이미 한 번 고쳤던 그것**이다.

창은 0.3초마다 통째로 다시 그린다(돈이 오르면 단추가 켜져야 하니까).
누르고 있는 사이에 그러면 단추가 사라지고, 손을 뗀 자리가 빈 곳이 되어
그 누름이 지도로 흘러가 **창이 닫힌다.**

화살표 때 고쳤는데, **레벨업만 예외로 열어 뒀다가 다시 밟았다** —
꾹 누르는 동안 숫자가 오르는 걸 보여주려고 "누르는 중에도 다시 그리기"를
허용했던 것이다. 편의 하나를 위해 방금 막은 구멍을 도로 뚫은 셈이다.

**고친 방법:** 손가락이 닿아 있는 동안은 **무엇도 지우지 않는다.**
대신 그 줄의 **글자만 고쳐 쓴다**(`_refresh_held`). 숫자는 그대로 올라가고
단추는 사라지지 않는다.

덤으로 창에 `MOUSE_FILTER_STOP`을 박았다. 창 위를 누른 것이 지도로 새어
나가는 일 자체를 없앤다 — 이 종류의 고장이 다시는 안 나온다.

### 시험을 만들었다 — 그리고 **그 시험이 세 번 거짓말했다**

눈으로는 "가끔 그러네"까지밖에 못 간다. 그래서 누르기 시험에 넣었다.
그런데 시험을 만드는 동안 **고장 난 판을 넣어도 통과**하는 일이 세 번 있었다.

| 왜 통과했나 | 무엇을 몰랐나 |
|---|---|
| 돈을 조금만 줬다 | 꾹 누르기가 돈이 떨어져 곧 끝나서, 보려던 순간이 안 왔다 |
| 단추한테 물어봤다 | `rebuild()`는 **줄**에 지움 표시를 건다. 그 안의 단추는 "나는 멀쩡하다"고 답한다 |
| 창을 두 번 그렸다 | 옛 줄이 지움 표시만 단 채 가지에 남아 있어서, **이미 죽은 단추**를 집었다 |

**시험이 못 보는 것은 없는 것이다.** 그래서 마지막에 반드시 확인했다 —
고친 판에서 초록불, **일부러 되돌린 판에서 빨간불.** 이 두 번을 다 보기
전까지는 시험을 믿지 않는다.

## 2026-08-21 · 매대도 가게마다 · 점장도 승급하면

- **매대(좌판) 5장** — 지금은 다섯 가게가 똑같은 나무 상자다. 그 가게에서
  무엇을 파는지가 좌판에서 먼저 읽혀야 한다(대장간은 모루, 약재상은 약재 서랍장).
- **점장도 등급 그림** — `smith-make-1.png`가 있으면 참쇠 대장간 점장은 그걸 쓴다.
  없으면 그 가게 점장, 그것도 없으면 공통 점장. 세 겹 다 없으면 도형이다.

물건·점장·매대가 전부 같은 수법을 쓴다 — **있으면 쓰고 없으면 아래 것.**
그래서 그림 73장 중 어디서 멈춰도 화면이 안 깨진다.

---

## 2026-08-21 · 촌장 너구리가 마을을 돈다 · 의뢰는 '고르는 것'이 된다

### 넣은 것 — 화면뿐이다

촌장이 길을 따라 마을을 돌아다닌다. 아무 길 한 곳을 골라 걸어가고, 닿으면
잠깐 쉬었다 또 간다. 걸린 의뢰가 있으면 **머리 위에 ❗**가 뜨고, 누르면
의뢰 창이 열린다.

**sim은 한 줄도 안 건드렸다.** 걸음도 목적지도 화면 전용 주사위(`_grng`)를 쓴다.
sim의 주사위를 한 번이라도 굴리면 그 자리에서 대조 시험이 깨진다.

셋째 너구리를 무엇으로 가르나 — **크기가 아니라 다니는 범위**다.
촌장은 마당 밖으로 나오는 유일한 너구리다. 그림은 흰 수염과 지팡이면 충분하다.

### 그리고 갈림길 하나를 미리 알렸다

만드는 사람이 원한 것은 *"촌장이 쓰레기를 주우면 돈이나 젬이 나온다"*인데,
**그건 화면이 아니라 경제다.** 자바스크립트 답안지에는 촌장이 없으니,
촌장이 돈을 만드는 순간부터 두 판은 영영 다른 게임이 된다.

길 셋을 `FLOW.md`에 적고 **'나'를 권했다** — 자동으로 들어오게 하되,
재는 도구가 마을 화면까지 돌리게 고친다. 그러면 원하던 그림이 그대로 살고
그 수입도 숫자로 잡힌다. 그때 자바스크립트 답안지는 거기까지가 끝이고,
**답안지가 자기 자신이 된다**(저장 시험이 이미 그렇게 한다).

*이걸 코드 쓰기 전에 말한 이유*: 넣고 나서 대조가 빨간불이 되면, 그때는
"고장인가 원래 그런가"를 가릴 수가 없다. 갈라질 것을 알고 갈라져야 한다.

### 의뢰를 고르는 것으로 (정함, 만들기는 나중)

지금은 저절로 걸린다. 앞으로는 촌장이 가져오고 **받을지 말지를 플레이어가 정한다.**
기한·오는 간격·거절의 값은 **재고 나서** 정한다 — 특히 오는 간격은
손님 등장 문턱을 잡을 때처럼 목표 시각을 정하고 수렴시키는 방법이 있다.

---

## 2026-08-21 · 대장간을 잠갔다 · 점장 카드가 열린다

### 1. 시작이 생겼다

`shops = ["smith"]` → `[]`. 이제 켜면 **무너진 집 다섯 채**뿐이고,
대장간 위에 `여기부터 되살리세!`가 아래를 가리키며 깜빡인다.
대장간 값은 0이라 켜자마자 누르면 열린다.

**같이 걸린 것 하나:** `items`에 곡괭이가 열린 채로 시작하고 있었다.
가게를 잠그고 나니 **"가게는 없는데 곡괭이는 팔린다"**가 됐다.
품목도 비우고, 가게를 열 때 첫 품목이 들어오게 했다(`open_shop`이 원래 그렇게 한다).

**자바스크립트판도 같이 고쳤다.** 이건 새 규칙이 아니라 **시작 상태**라
답안지와 갈라질 이유가 없다. 양쪽을 같이 바꿔서 대조 여덟 개가 그대로 초록불이다.

**재 봤다 (8시간 × 씨앗 셋):** 1.47T · 1.54T · 1.06T (중앙값 1.47T).
전이 1.29T였으니 올라 보이지만 **씨앗 폭(1.06~1.54T)이 그보다 넓다** —
시작 상태가 바뀌면서 난수가 다르게 흐른 것이지, 경제가 세진 게 아니다.

### 2. 점장 카드

가게를 되살리거나 승급하면 화면 한가운데에 카드가 뜬다.
뒤는 어둡게 덮는다 — 안 덮으면 '떠 있는 딱지'로 보이고 뒤를 눌러 지나쳐 버린다.

**카드를 따로 저장하지 않는다.** 어떤 카드를 가졌는지는 이미 sim이 안다 —
가게가 열렸으면 0등급 카드가 있는 것이고, 승급했으면 그만큼 더 있다.
담는 칸을 새로 만들면 저장본을 깨뜨리고(다섯 번째 규칙) **두 군데가 어긋날
여지**가 생긴다. 있는 것에서 계산할 수 있으면 계산한다.

**도감은 한 창에 갈피 둘**(손님 · 점장 카드). 창을 따로 만들면 아래 단추가
넷이 되고, 폰에서 넷은 누르다 틀린다 — `FLOW.md`에 적어 둔 그 이유다.

### 3. 승급의 좌절을 카드가 덮는다

승급하면 레벨이 1로 돌아가 벌이가 46%로 떨어진다(재서 얻은 값).
그 순간에 **카드가 열린다.** 두 문제가 서로를 푼다 — 잃는 것이 보이는 자리에
얻는 것도 같이 놓는다.

### 걸린 것 — `set_anchors_preset`은 화면을 안 채운다

카드가 화면 왼쪽 위 구석에 붙어 나왔다. `set_anchors_preset(FULL_RECT)`는
**앵커만 바꾸고 크기는 그대로 둔다.** 채우려면 `set_anchors_and_offsets_preset`을
써야 한다. 이름이 비슷해서 한 번에 틀리는 자리다.

### 도구도 고쳤다

- `SHOT_CARD=<가게id>` — 카드는 누르는 순간에만 뜨는데 도구는 누를 수 없다
- `SHOT_TAB`을 **모든 창**에 걸었다. 가게 창에만 걸어 뒀더니
  도감의 '점장 카드' 갈피를 도구가 영영 못 봤다

---

## 2026-08-21 · 아이폰 내보내기가 실패하던 진짜 이유 — **앱 아이콘**

유저가 Xcode에서 이 말을 봤다:

> The project 'godot_apple_embedded' is damaged and cannot be opened due to a parse error.

**손상된 게 아니라 아예 없었다.** 폴더를 열어 보니 `.xcodeproj` 안이 **텅 비어
있었다**(0바이트). Xcode는 없는 파일을 읽으려다 "망가졌다"고 말한 것이다.

명령줄로 내보내 보니 이유가 한 줄로 나왔다:

```
ERROR: 아이콘 내보내기: Invalid icon (icons/settings_58x58): ''.
ERROR: Project export for preset "iOS" failed.
```

**앱 아이콘이 한 장도 없어서** 내보내기가 도중에 멈췄고, 멈추기 전까지 만든
것(라이브러리·pck)만 남고 정작 Xcode 파일은 안 만들어졌다.

**고친 것:** 있는 그림(점장 너구리)으로 1024×1024 아이콘을 만들어
`godot/art/icon.png`에 넣고, 내보내기 설정의 빈 아이콘 칸 **51개**를 전부
그것으로 채웠다(Godot이 크기별로 알아서 줄인다). 이제 끝까지 간다 —
`project.pbxproj`가 만들어지는 것을 확인했다.

> **오류 메시지를 곧이곧대로 믿으면 엉뚱한 데를 판다.** "손상됐다"는 말에
> 이끌려 파일을 고치려 들었다면 하루를 버렸을 것이다. 폴더를 열어 보는 데
> 10초, 명령줄로 다시 내보내 보는 데 1분이면 진짜 이유가 나온다.

### 남은 것 — Xcode가 아직 없다

지금 이 맥에는 **명령줄 도구만** 깔려 있다(`xcode-select`가 CommandLineTools를
가리킨다). Xcode 본체가 없으면 만들어진 Xcode 파일을 열 수도, 아이폰에 넣을
수도 없다. 시뮬레이터도 Xcode에 딸려 온다(`simctl`이 없다).

앱스토어에서 Xcode를 받아야 하고, 몇 GB에 시간이 걸린다.

### 이어서 — 아이폰 내보내기, 나머지 걸림돌 둘

**내 앞 진단은 절반이 틀렸다.** "Xcode가 안 깔려 있다"고 적었는데 깔려 있다.
`xcode-select`가 명령줄 도구를 가리키고 있어서 **명령줄이 Xcode를 못 볼 뿐**이고,
Xcode 앱 자체는 멀쩡하다. 명령줄로 확인한 것을 앱의 상태로 넘겨짚었다.

아이콘을 고친 뒤에도 둘이 더 걸렸다:

| 걸린 것 | 왜 | 고친 것 |
|---|---|---|
| `Apple 스토어 팀 ID가 지정되지 않았습니다` | 비워 두면 Godot이 아예 안 내보낸다 | 자리채움 값을 넣었다. 진짜 팀은 Xcode의 Signing 칸에서 고른다 |
| `xcodebuild를 실행하는 데 실패했습니다` | Godot이 **직접 빌드까지** 하려 했는데, 명령줄이 Xcode를 못 본다 | `export_project_only=true` — Xcode 프로젝트만 만들고 빌드는 Xcode에게 맡긴다 |

그리고 `bundle_identifier`가 `"test"`였다. iOS에서 못 쓰는 이름이라
`com.raccoonhollow.nogur`로 바꿨다.

**결과:** `/Users/apple/Downloads/nogur-ios/nogur.xcodeproj` (430MB).
오류 없이 끝까지 나온다.

### 왜 'Xcode의 깃 클론'이 답이 아닌가 (유저 물음)

Xcode의 깃 클론은 **Xcode용 프로젝트**를 받아오는 기능이다. 우리 저장소는
Godot 프로젝트라, 클론해 봤자 Xcode에게는 **글자 파일 뭉치**일 뿐이다.

순서는 반대다:

```
Godot(내보내기) → nogur.xcodeproj → Xcode로 열기 → 아이폰에 설치
```

Godot이 Xcode 프로젝트를 **만들어 준다.** 저장소를 Xcode로 여는 게 아니다.

---

## 2026-08-21 · 진짜 폰에서 만져 보고 — 화면 타이밍이 거짓말을 하고 있었다

아이폰에 올려 보고 유저가 잡은 것들. **셋은 진짜 고장이었다.**

### 점장이 손님보다 먼저 움직였다

> *"첫 번째 손님이 카운터로 안 왔는데 점장이 계산대에 물건을 미리 옮김"*

맞다. sim이 "팔렸다"고 알리는 **그 순간** 화면이 두 가지를 같이 했다 —
손님을 마을 입구에 만들어 걷게 하고, **점장을 계산대로 즉시 보냈다**
(`clerks[i].busy = 1.4`). 손님은 몇 초를 걸어와야 하는데 점장은 이미 가 있다.

이제 점장은 **손님이 계산대에 실제로 닿아 있는 동안**만 그 자리에 선다.

### 줄이 앞을 비워 두고 뒤에서 기다렸다

> *"두 번째 손님이 안 왔는데 세 번째 손님이 기다림"*

같은 뿌리다. 줄자리를 **문에 들어서는 순간** 잡아 두고 있었다
(`line.find(wk)`). 앞사람이 아직 길 위에 있는데 그 자리는 이미 찜해져 있으니,
**맨 앞이 비었는데 뒷사람이 두 번째 자리에 서는** 그림이 됐다.

줄 번호를 **도착한 사람끼리만** 세게 고쳤다(`_line_index`).
기다림 시계와 주문표도 같은 기준으로 바꿨다.

> **이 둘은 코드만 읽어서는 안 보인다.** 각각은 맞는 코드다 — 판매가 났으니
> 점장을 보내고, 줄에 들어왔으니 자리를 잡는다. **틀린 것은 순서**뿐이라,
> 실제로 움직이는 걸 눈으로 봐야 드러난다. 스크린샷도 한 장짜리라 못 잡았다.

### 걸음이 다 똑같았다

`content.js`에 손님마다 `speed`가 처음부터 있었는데(토끼 1.4 · 거북 0.45)
**화면이 그걸 안 쓰고 있었다.** 이제 쓴다. 길을 걸을 때만 좌우로 조금씩
흩뜨리기도 한다 — 다들 한 줄로 겹쳐 걸으면 열 마리가 한 마리로 보인다.
줄에 설 때는 안 흩뜨린다(줄이 흐트러진다).

### 창이 너무 크고 화살표가 너무 작았다

창 높이 540 → **430**. 화면의 6할을 덮으면 마을이 안 보이고,
그러면 "지금 뭘 하고 있었지"를 잃는다.

화살표는 글자 크기대로여서 폰에서 너무 작았다 — **52×48로 키웠다.**
애플이 권하는 최소가 44pt인데 글자 하나짜리 단추는 그 절반도 안 된다.
*책상에서는 마우스로 눌러서 몰랐다.*

---

## 2026-08-22 · 뽑기와 룰렛 · 손님 서른 · 그리고 **답안지가 갈라졌다**

만드는 사람이 외출한 사이 통째로 만들어 둔 것. 규칙과 확률부터 박고,
잰 다음, 화면을 붙였다.

### 손님 서른 · 등급 여섯

열둘에서 **서른**으로 늘렸다. 등급은 여섯 — 흔함 8 · 드묾 7 · 귀함 6 ·
진귀 5 · 영물 3 · 신수 1.

**등급은 세기 순서와 같게 뒀다.** 귀한 카드일수록 실제로 좋은 손님이다.
이게 어긋나면 "귀한데 쓸모없다"가 되고, 그건 뽑기를 죽인다.

새로 넣은 열여덟은 조선 산야의 짐승들이다 — 참새·개구리·두더지·고슴도치·
오리 · 수달·노루·족제비·살쾡이·산양 · 담비·원앙·늑대·백로 · 표범·사향노루·
반달곰 · 그리고 영물 **해태**.

### 뽑기 — 확률을 숨기지 않는다

| 뽑기 단계 | 흔함 | 드묾 | 귀함 | 진귀 | 영물 | 신수 |
|---|---|---|---|---|---|---|
| 1 | 100 | | | | | |
| 2 | 95 | 5 | | | | |
| 3 | 88 | 11 | 1 | | | |
| 4 | 80 | 17 | 3 | | | |
| 5 | 70 | 23 | 6 | 1 | | |
| 6 | 60 | 28 | 10 | 2 | | |
| 7 | 50 | 31 | 15 | 3.5 | 0.5 | |
| 8 | 40 | 33 | 20 | 6 | 1 | |
| 9 | 30 | 33 | 26 | 9 | 1.8 | 0.2 |
| 10 | 20 | 32 | 32 | 13 | 2.5 | 0.5 |

- **1단계는 흔함만 나온다.** 막 시작한 사람에게 신수가 나오면 그 뒤로 뽑을
  이유가 사라진다 — 올라갈 곳을 남겨 둔다
- 단계는 **누적 뽑기 횟수**로 오른다: 10 · 40 · 100 · 200 · 350 · 600 ·
  1,000 · 1,700 · 3,000회
- 값은 젬으로 **1회 💎1 · 10회 💎9 · 30회 💎25**. 강화가 3~62젬이라
  "강화를 살까 뽑을까"가 매번 고민이 된다 — 그 고민이 재미다
- 열 장 이상 뽑으면 **드묾 이상 한 장은 반드시** 나온다(2단계부터).
  열 번 뽑아 전부 흔함이면 뽑은 기억이 안 남는다

**게임 안에 이 표를 그대로 보여준다.** 확률을 숨기는 뽑기는 만들지 않는다 —
법으로 정해 놓은 나라도 있고, 무엇보다 숨기면 "속았다"가 남는다.

### 카드로 성을 올린다 — 그리고 **눌러서** 올린다

1성→2성 5장, 2→3 10장 … 19→20 **700장**. 한 손님을 20성까지 올리는 데
**3,755장**이다. 손님이 서른이니 사실상 끝이 없다.

**저절로 안 오른다.** 만드는 사람이 "왜 자동으로 승급하냐"고 물었던 그것 —
이제 카드가 모이면 도감에서 눌러 올린다. 모은 카드를 쓰는 그 순간이 있어야
모을 마음이 생긴다.

### 손님은 이제 뽑기로만 온다

예전에는 누적 매출이 문턱을 넘으면 저절로 왔다. 그러면 *"기다리면 열린다"*가
되는데, 그건 내가 한 일이 아니라 **시간이 한 일**이다.

### 룰렛 — 하루 네 번

무료 1회 + 광고 3회(**광고는 더미**, 2초 기다렸다 준다). 칸 열둘:

| 칸 | 확률 | | 칸 | 확률 |
|---|---|---|---|---|
| 엽전 한 줌(30초치) | 18% + 12% | | 💎3 | 14% |
| 엽전 한 꾸러미(3분치) | 12% | | 💎5 | 9% |
| 엽전 한 자루(10분치) | 6% | | 💎10 | 5% |
| 엽전 한 궤(30분치) | 4% | | 💎30 | 2% |
| 손님 카드 1장 | 10% | | 카드 2장 | 5% |
| 카드 5장 | 3% | | | |

**엽전은 '지금 초당 수입의 몇 초치'다.** 고정 금액이면 초반엔 후하고
후반엔 먼지가 된다 — 언제 돌려도 값이 비슷해야 매일 돌린다.

### 삽살개 여러 마리

가게 넷마다 한 마리. 값은 2.2배씩 오른다(2.5만 → 5.5만 → 12.1만 → 26.6만).

**무는 확률에 상한 90%를 걸었다.** 그냥 곱하면 넷에 97%, 다섯에 99%가 되어
나쁜 놈이 사실상 사라진다 — **돈으로 재미를 지우는 셈**이다.
열에 하나는 반드시 사람 몫으로 남긴다.

---

## 그리고 오늘, **답안지가 갈라졌다**

`FLOW.md` §7에서 미리 적어 둔 그 날이 왔다. 손님이 오는 규칙이 바뀌었는데
(문턱 → 뽑기) 자바스크립트판에는 뽑기가 없다. 두 판은 이제 **영영 다른 게임**이라
서로 대조할 수가 없다.

**시험을 버리지 않았다.** `sim` 대조를 **어제의 Godot과 오늘의 Godot** 사이로
바꿨다 — 정답을 파일로 박아 둔다(`golden_sim.txt`). 규칙을 일부러 고쳤으면
정답을 다시 박고, **그 차이를 커밋에 남긴다**:

```sh
GOLDEN=write tools/crosscheck.sh sim
```

저장 시험이 이미 쓰던 방법이다 — 답안지가 없어지는 게 아니라 **자기 자신**이 된다.
`fmt` · `rng` · `content`는 순수한 함수와 데이터라 자바스크립트와 계속 대조한다.

### 새 시험 — 확률은 눈으로 못 본다

`tests/gacha.gd` — 단계마다 **십만 번**씩 뽑아 표와 견준다(0.5%p 안쪽).
룰렛 열두 칸도, 20성까지의 카드 장수도, 개의 확률 상한도 같이 본다.

*열 번 뽑아 다 흔함이 나와도 그게 고장인지 운인지 사람은 못 가린다.
확률을 고지하는 기능이라 더더욱 — 적어 놓은 값과 실제가 다르면 거짓말이 된다.*

### 시험이 잡아낸 진짜 고장 둘

1. **저장했다 켜면 성이 제멋대로 올랐다.** 옛 저장본(1판)을 옮기는 코드가
   **새 저장본(2판)에도 돌고 있었다** — 방문 횟수로 성을 다시 매겨 버린 것이다.
   판 번호를 보고 결정하게 고쳤다. 저장 시험이 아니었으면 못 잡는다.
2. **큰 수가 양쪽에서 다르게 찍혔다.** 새 손님의 `at`을 `1e30`으로 뒀더니
   자바스크립트는 `1e+30`, Godot은 자릿수를 다 펴서 찍었다. 999조로 바꿨다.

---

## 2026-08-24 · Godot 로고를 껐다 · 룰렛을 진짜 바퀴로

### 켤 때 뜨던 Godot 로고

**끌 수 있다.** Godot은 무료·로열티 0이고 **로고를 지우는 것도 허락한다**(MIT 허가서).
유니티처럼 "만든 도구를 반드시 보여줘야 한다"는 조건이 없다.

```
application/boot_splash/show_image=false
application/boot_splash/bg_color=Color(0.435, 0.506, 0.349, 1)
```

배경색을 **마을 풀색**으로 맞춰 뒀다. 검정에서 초록으로 튀는 것보다,
처음부터 그 색이면 '켜졌다'는 느낌만 남고 로고 자리가 안 보인다.
나중에 우리 그림을 넣으려면 `image`에 파일을 대고 `show_image`를 도로 켠다.

### 룰렛 — 원형 바퀴로

칸 열두 개가 나뉜 바퀴가 돌다가 멈춘다. 색은 **상의 종류**로 갈랐다
(엽전은 누런빛, 젬은 푸른빛, 카드는 자줏빛). 무게(확률)로 색을 갈랐으면
큰 상이 어디 있는지 안 보여서 도는 동안 볼 것이 없다.

**끝에서 느려지고(다섯 제곱), 마지막에 살짝 되튄다.** 딱 멈추면 기계 같고
조금 되튀면 바퀴 같다 — 손맛은 그 0.16초에 거의 다 들어 있다.

> **상은 돌리기 전에 이미 정해져 있다.** `sim.spin`이 정하고, 바퀴는 그 칸이
> 바늘 밑에 오도록 각도를 맞춰 돌 뿐이다. 순서를 뒤집으면 — 바퀴가 멈춘
> 자리를 보고 상을 정하면 — **화면의 오차가 확률이 되어 고지한 표가
> 거짓말이 된다.** `MOTION.md`에 적어 둔 그 규칙이다.

### 시험이 먼저 빨간불을 켰다 (좋은 신호)

바퀴를 붙이자 누르기 시험이 *"룰렛을 돌렸는데 결과가 안 떴다"*로 실패했다.
**고장이 아니라 시험이 옛 세상을 알고 있던 것** — 예전엔 누르면 결과가
곧바로 떴는데 이제 2.36초를 돈다.

시험을 고치면서 **더 좋은 것을 하나 넣었다**: 누른 직후에는
결과가 **안 떠 있어야** 한다는 것도 본다. 먼저 떠 버리면 바퀴를 볼 이유가
없어지는데, 그건 눈으로는 "잘 도네" 하고 지나칠 종류다.
