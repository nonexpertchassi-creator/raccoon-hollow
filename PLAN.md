# 다음 작업 계획

2026-08-18 · 시뮬레이션(`tools/balance.mjs`) 결과를 근거로 작성.

## 진행 상황

| 단계 | 상태 |
|---|---|
| Phase 0 · 진단 도구 | ✅ 완료 |
| Phase 1 · 장바구니 | ✅ 완료 (무작위 방식으로) |
| Phase 2 · 콘텐츠 수명 | ✅ 완료 |
| Phase 3 · 조작 피로 | ✅ 완료 (자동 강화) |
| Phase 4 · `core/` 정리 | ⬜ 남음 |
| 엔드게임 (5시간 이후) | ⬜ 남음 — 아래 참조 |

### 지금까지의 결과 (8시간 · 씨앗 1·2·3)

| 항목 | 전 | 후 |
|---|---|---|
| 안 팔려 멈춘 품목 | 15개 중 7개 | **0개** |
| 콘텐츠 소진 | 4시간 | **5시간 15분** |
| 손님 7마리가 다 오는 시점 | 1시간 4분 | **4시간 44분** |
| 강화 클릭 | 1,986회 | **약 550회** (자동 강화 구입 뒤 0) |

### 재고 부족을 게임 규칙으로 쓸 수 있는가 — 못 쓴다 (측정 완료)

"손님이 원하는 물건이 없으면 기다리거나 평판이 깎이게 하자"를 검토했다.
**지금 구조로는 튜닝으로 도달할 수 없다.**

먼저 지금 상태:

| | 값 |
|---|---|
| 원하는 개수를 못 채운 거래 | 0.4% (전부 첫 1시간, 2시간째부터 0.0%) |
| 진열대가 텅 빈 시간 | 1.7% |
| 진열대가 꽉 찬 시간 | 16.5% |

병목이 반대쪽에 있다. 물건이 모자란 게 아니라 손님이 모자라 물건이 쌓인다.

손잡이를 다섯 개 돌려봤고 전부 실패했다.

| 손잡이 | 결과 |
|---|---|
| 손님 구매량 1.5~2배 | 과도하게 뒤집힘. 매출이 같이 늘어 콘텐츠 소진이 4시간대로 단축 |
| 생산 하한 0.4→0.45~0.8 | 앞 4시간 꽉 참 + 뒤 3시간 텅 빔. 출렁이지 않고 두 국면으로 갈릴 뿐 |
| 이정표를 생산 2배로 이전 | **더 나빠짐.** 8시간째 진열대 91% 꽉 참 |
| 수요가 품목·가게 수 따라 증가 | 2시간째 재고 0, 거래의 46~59%를 못 채움 |
| 요청 간격 14→60초 | **효과 없음.** 칸을 여는 관문은 타이머가 아니라 돈이다 |

원인은 구조적이다. **생산에는 천장이 있고(`timeFloor`) 수요에는 없다.**
수요가 그 천장을 넘으면 영구히 바닥, 안 넘으면 영구히 꽉 참이라
중간에 머무는 설정이 존재하지 않는다.

주의: 총합 평균은 이 문제를 숨긴다. 생산 하한 0.45의 평균 재고는 19.7/40으로
"딱 중간"처럼 보이지만 시간대별로는 36~39와 0.7의 평균일 뿐이다.
**밸런스는 반드시 시간대별로 볼 것.**

쓰려면 튜닝이 아니라 구조를 바꿔야 한다 — 진열대 확장을 업그레이드로 만들거나,
하위 품목을 상위 품목의 재료로 쓰거나(초기 검토안 (c)), 손님 대기열을 넣는 식이다.

**후속:** 튜닝 대신 구조를 하나 얹어서 이 문제를 부분적으로 풀었다 — '장'이다.
작은 건물이 가끔 북적이고, 그때 누르면 30초간 손님이 두 배로 온다.
수요 쪽에 붙였기 때문에 쌓인 재고가 실제로 빠진다. 8시간 측정:

| | 안 누름 | 볼 때마다 누름 |
|---|---|---|
| 진열대가 꽉 찬 시간 | 15.8% | **6.7%** |
| 평균 재고 | 31.3/40 | **13.3/40** |
| 누적 매출 | 288조 | 335조 (+16%) |
| 콘텐츠 소진 | 5시간15분 | 4시간40분 |

매출이 16%만 오르면서 꽉 찬 시간이 절반 이하로 떨어진다. 튜닝으로는
못 하던 일을 구조 하나로 한 셈이다. 대신 콘텐츠 소진이 35분 앞당겨진다.
231회는 매 기회를 다 누른 상한이므로 실제 플레이는 이보다 완만하다.

### 가게 승급 — 볼거리가 1시간 51분에 끝나던 문제

측정으로 드러난 것: **마을이 1시간 51분이면 다 채워진다.** 가게 다섯, 작은 건물
넷이 전부 서고 나면 지도 위에 새로 생길 게 없고, 그 뒤로는 이미 본 건물 안에서
숫자만 오른다. "1~2시간이면 끝"이라는 체감의 정체가 이것이다.

돌도끼 → 쇠도끼 → 강철도끼. 가게마다 3등급이고, 승급하면 레벨이 1로 돌아가는
대신 밑천이 통째로 좋아진다 — **그 가게만의 작은 환생**이다.

설계에서 지킨 것:

- **승급 직후 값이 직전 만렙보다 커야 한다.** 작으면 애써 만렙을 찍고 승급했는데
  수입이 떨어지는 꼴이 된다. 1급 만렙 ×31 → 2급 시작 ×40, 2급 만렙 ×4,880 →
  3급 시작 ×6,000으로 잡았다.
- **조건은 항목별로 보여준다.** 통과/미통과를 하나씩 나열해야 목표가 된다.
  boolean 하나만 넘기면 잠긴 버튼으로만 남는다.
- **속도 조절은 돈으로 한다.** 처음엔 업적 조건(만렙·손님 수·초당 수입)만 걸었더니
  손님 5종이 열리는 2시간 33분에 네 가게가 동시에 승급하고 3시간 26분에 다 끝났다.
  이 게임에서 실제로 속도를 정하는 건 돈이다(가게 값 9천→90만→1.2억→200억이
  그 증거다). 승급값을 붙이고 나서야 계단이 생겼다.

결과: 마지막 콘텐츠가 **3시간 26분 → 29시간 7분**.

| 승급 | 시점 |
|---|---|
| 대장간 쇠 · 필방 고운 | 2시간 33분 |
| 지물포 고운 | 2시간 41분 |
| 옹기점 오지 | 3시간 55분 |
| 약재상 묵은 | 4시간 49분 |
| 대장간 강철 | 5시간 10분 |
| 필방 명품 | 5시간 51분 |
| 지물포 진상 | 9시간 19분 |
| 옹기점 왕실 | 19시간 53분 |
| 약재상 천년 | 29시간 7분 |

건물 모양은 축을 둘로 나눴다 — 지붕은 등급(초가→기와→단청), 금테·화분은
칸을 다 열었는가. 등급 하나로만 하면 첫 승급까지 두 시간 반 동안 건물이
전혀 안 변한다.

### 단골 — 손님이 굳어 있던 문제

손님은 마을에 온 뒤로 아무 변화가 없었다. 마지막 손님이 4시간대에 오고 나면
일곱이 그대로 굳는다. 자주 오는 손님이 단골이 되게 했다.

    뜨내기 → 낯익은 → 단골 → 귀한 → 터줏대감
    (누적 0 · 500 · 2,500 · 10,000 · 30,000개)

문턱은 실측으로 잡았다 — 토끼가 10분에 515개, 8시간에 34,000개를 사간다.
결과: **단골 승급 23회가 10분부터 11시간 23분까지** 이어진다.

올리는 건 값이 아니라 주로 **개수**다(터줏대감 ×3). 이 게임은 물건이 남아도는
쪽이 병목이라, 수요를 늘려야 쌓인 재고도 같이 빠진다. 장(場) 보너스와 같은 이유다.

가게 승급 조건의 '손님 N종'을 '단골 등급 합계 N'으로 바꿨다. 종류 수는 4시간이면
7로 굳어 조건 구실을 못 하지만, 등급 합계는 계속 자란다(60분 6 → 6시간 20).
합계 문턱은 8·18로 잡았다 — 5·7로 두면 50분에 통과해 조건이 되지 못한다.

화면에서는 단골이 **갓을 쓴다**. 등급이 오를수록 갓이 커지고 색이 진해지며,
터줏대감은 금관자를 단다. 머리 위에 글자를 얹으면 손님이 몰릴 때 다시 겹치므로
모양으로 알린다 — 예전에 금액 글자가 겹쳤던 것과 같은 이유다.

### 손님 12종과 성격 — 그리고 병목이 뒤집힌 사건

손님이 일곱이었고, `every·qty·pay`가 뒤로 갈수록 나란히 커졌다. 그래서 나중 손님이
앞 손님의 **상위호환**이었고, 일곱이든 스물이든 체감이 같았다. 축을 다섯으로 쪼갰다.

| | 오는 간격 | 개수 | 값 | 훑는 종류 | 걸음 |
|---|---|---|---|---|---|
| 까치 | 3.1초 | 2 | ×0.85 | 2 | 1.7 |
| 다람쥐 | 4.2초 | 2 | ×1.15 | 2 | 1.5 |
| 멧돼지 | 80초 | 12 | ×2.2 | **1** | 1.1 |
| 거북 | 110초 | 14 | ×7 | 3 | 0.45 |
| 호랑이 | 400초 | 34 | ×13 | 2 | 0.95 |

`spread:1`(멧돼지·곰)은 한 종류를 통째로 쓸어간다 = 진열대 한 칸이 비는 사건이다.
`speed`는 화면에서만 쓴다 — 거북이 실제로 느리게 걸어야 성격이 읽힌다.

**여기서 병목이 반대로 뒤집혔다.** 일곱이 열둘이 되자 수요가 초당 7.5개 → 14.7개로
뛰었는데 공급은 7.8개가 상한이었다. 진열대 평균 재고 31.3 → **0.2/40**,
못 채우고 돌아간 손님 64%. 여태 "물건이 남아돈다"가 문제였는데 하루아침에
"물건이 없다"가 됐다.

간격에 **일괄 배수**를 거는 건 답이 아니었다. ×2.6을 걸면 앞 네 시간은 37~38로
그대로 꽉 차고 뒤 네 시간만 1로 비는 **두 국면**이 남는다 — 초반 손님까지 뜸해져
심심해지기만 한다. 손님마다 따로 잡았다: **앞은 자주, 뒤 큰손은 드물게.**

    4.6 · 3.1 · 4.2 · 17 · 32 · 50 · 80 · 120 · 110 · 145 · 260 · 400 (초)

|  | 1h | 2h | 3h | 4h | 5h | 6h | 7h | 8h | 못채움 | 꽉참 | 텅빔 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 손보기 전 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 64% | 0% | 87% |
| 일괄 ×2.6 | 37 | 38 | 38 | 36 | 5 | 1 | 1 | 1 | 15% | 11% | 23% |
| **지금** | 25 | 31 | 37 | 32 | 31 | 28 | 2 | 2 | **4%** | 6% | 10% |

씨앗 다섯 개에서 편차 ±0.3. 앞의 여섯 시간이 20~37 사이에 머문다.

**7~8시간대가 2로 주저앉는 건 남겨둔 문제다.** 단골 배수는 ×3까지 계속 자라는데
생산은 `timeFloor`에 막혀 천장이 있다. 콘텐츠 소진(7시간56분) 이후 구간이라
환생 루프가 들어갈 자리와 정확히 겹친다 — 조절이 아니라 구조로 풀 문제다.

**앞 세 마리는 손대면 안 된다.** 토끼를 4.6 → 4.0으로만 줄여도 평균 재고가
15.5 → 7.0으로 반토막 난다. 자주 오는 손님이 총수요를 지배하기 때문이다.

### 남은 문제 두 가지

1. **5시간 15분 이후 할 게 없다.** 마지막 품목(우황)이 열린 뒤로는 자동 강화가
   숫자만 올린다. 환생/이사 같은 반복 루프를 넣을지 결정해야 한다.
2. **4시간~5시간 15분 사이에 79분짜리 빈 구간이 있다.** 우황(12조)을 모으는
   시간이다. 방치형에서 '앱을 끄고 나중에 오는' 구간이라 나쁘다고만 볼 수는
   없지만, 그 앞뒤보다 유독 길다.

---

## 아래는 최초 계획 원문 (기록용)

---

## 지금 상태 진단

6시간 시뮬을 돌려 나온 숫자 세 개가 이 계획의 전부다.

| 관측 | 숫자 | 의미 |
|---|---|---|
| 재고 상한에 박혀 생산이 멈춘 품목 | 12개 중 7개 (2시간 시점) | 콘텐츠의 절반이 죽어 있다 |
| 마지막 해금 | 3시간 33분 (약재상) | 6시간이면 할 게 없다 |
| 손님 전원 등장 | 1시간 4분 | 제일 센 카드를 첫 1시간에 다 쓴다 |
| 레벨업 : 의미 있는 선택 | 1945 : 14 | 조작의 99.3%가 버튼 연타 |

### 근본 원인 — `sim.js:_buy()`

```js
open.sort((a, b) => this.price(b) - this.price(a));
const id = open[0];        // 손님이 "제일 비싼 것 한 종류"만 사간다
```

손님 한 명이 품목 **한 종류**만, 그것도 항상 최고가부터 사간다.
그래서 하위 품목은 재고가 `STOCK_CAP`(40)에 도달하고, `tick()`의

```js
if (st.stock >= STOCK_CAP) continue;   // 진열대가 꽉 차면 생산 정지
```

에 걸려 **생산까지 멈춘다.** 한 번 멈추면 영영 안 팔리므로 영구히 죽는다.

2시간 시점 실측:

```
곡괭이 lv160  재고 40/40  ✗ 죽음      부채   lv 89  재고  2/40  ✓
낫     lv150  재고 40/40  ✗ 죽음      창호지 lv 80  재고  1/40  ✓
호미   lv141  재고 40/40  ✗ 죽음      옹기   lv 71  재고  1/40  ✓
붓     lv124  재고 40/40  ✗ 죽음      사발   lv 62  재고  0/40  ✓
도끼   lv132  재고 40/40  ✗ 죽음      한지   lv 97  재고 25/40  △
먹     lv115  재고 40/40  ✗ 죽음
벼루   lv106  재고 40/40  ✗ 죽음
```

곡괭이를 160레벨까지 올렸는데 초당 수입 기여가 **정확히 0**이다.

---

## Phase 0 — 진단 도구 (선행 필수)

다른 어떤 것보다 먼저다. **지금 `balance.mjs`는 위 문제를 못 잡고 "문제 없음"이라고 보고한다.**
도구가 거짓말하는 상태로 밸런스를 고치면 고쳤는지 알 수가 없다.

### 0-1. 정지 판정 수정 — `tools/balance.mjs`

```js
if (Object.values(s.items).every((x) => x.stock >= 40)) idle++;
```

`every`라서 **전부** 꽉 차야 센다. 상위 품목은 계속 팔리므로 조건이 영원히 거짓 →
7개가 죽어 있는데도 `생산 정지 0.0%`로 나온다.

→ 전역 플래그를 버리고 **품목별로** 정지 시간을 누적한다.

### 0-2. 품목별 매출 기여도 표 추가

죽은 품목이 `0.0%`로 한눈에 보이게 한다. 이게 Phase 1의 합격/불합격 판정 기준이 된다.

```
■ 품목별 매출 기여
   곡괭이    0.0%   정지 94%    ← 죽음
   낫        0.0%   정지 91%    ← 죽음
   ...
```

### 0-3. 콘텐츠 소진 시점 출력

마지막 해금(가게·품목·손님)이 일어난 시각 = **더 할 게 없어지는 시각**.
Phase 2의 목표 수치가 여기서 나온다.

### 0-4. 진단 규칙 추가

```
if (죽은 품목이 하나라도 있음)  ⚠ 품목 N개가 재고 상한에 박혀 생산 정지
if (마지막 해금 < 플레이 시간)  ⚠ N시간 시점에 콘텐츠 소진
```

**완료 기준:** 고친 도구를 지금 코드에 돌렸을 때 위 문제 3건이 전부 ⚠로 잡혀야 한다.
안 잡히면 도구가 아직 틀린 것이다.

---

## Phase 1 — 장바구니 (핵심)

**결정: 손님 한 명이 여러 품목을 섞어서 산다.**

검토했던 대안:
- *(a) 손님별 취향* — 손님마다 가격대가 다름. 손님이 캐릭터가 되지만 `content.js` 확장 필요.
- *(c) 재료 체인* — 하위 품목이 상위 품목 재료로 소모됨. 가장 깊지만 `sim.js` 재설계 수준.

(b)를 고른 이유: **`_buy()` 하나만 고치면 되고, 죽은 품목 7개가 즉시 되살아난다.**
가장 큰 버그를 가장 작은 변경으로 잡는다. 취향·재료 체인은 이게 안정된 뒤에 얹어도 늦지 않다.

### 1-1. 분배 규칙

재고가 **많은** 품목부터 사간다. 상한에 박힌 품목이 먼저 빠져나가므로 정지가 자동으로 풀린다.

```js
_buy(g) {
  const have = Object.keys(this.items).filter((id) => this.items[id].stock > 0);
  if (!have.length) return null;

  // 재고 내림차순 — 캡에 박힌 품목을 우선 비워 생산을 재개시킨다
  have.sort((a, b) => this.items[b].stock - this.items[a].stock);

  const perItem = Math.max(1, Math.ceil(g.qty / 3));   // 손님 하나가 최소 3종류를 건드린다
  let left = g.qty, gain = 0;
  const lines = [];

  for (const id of have) {
    if (left <= 0) break;
    const n = Math.min(left, perItem, this.items[id].stock);
    if (n <= 0) continue;
    const g2 = Math.floor(this.price(id) * g.pay * n);
    this.items[id].stock -= n;
    left -= n; gain += g2; this.sold += n;
    lines.push({ item: itemById(id), n, gain: g2 });
  }

  this.money += gain;
  this.revenue += gain;
  return { guest: g, lines, gain, n: g.qty - left };
}
```

`perItem`의 분모 `3`은 시작값일 뿐이다. **Phase 0 도구로 튜닝해서 결정한다.**

### 1-2. 파급 — `index.html` 판매 메시지

반환 모양이 `{ item, n }` → `{ lines[], gain }`으로 바뀐다. 이걸 쓰는 곳:

```js
// index.html:265 부근 — 지금은 단일 품목 가정
`${s.guest.name}가 <b>${s.item.name}</b> ${s.n}개를 사갔다 · <b>${fmt(s.gain)}냥</b>`
```

→ `곰이 옹기 외 2종 12개를 사갔다 · 3.4억냥` 같은 형태로 바꾼다.
**여기를 안 고치면 화면에 `undefined`가 뜬다.** Phase 1에서 같이 처리한다.

### 1-3. 확인해야 할 부작용

- **매출 폭주.** 죽어 있던 7개가 되살아나면 초당 수입이 뛴다. 6시간 곡선을 다시 재고,
  필요하면 `STOCK_CAP`이나 손님 `qty`로 조인다.
- **최고가 우선의 상실.** "비싼 걸 사간다"는 손맛이 약해질 수 있다.
  재고 우선이 지나치면 재고 순 + 가격 순 가중 혼합으로 조정한다.

**완료 기준:** Phase 0 도구에서 **매출 기여 0.0%인 품목이 하나도 없어야** 한다.

---

## Phase 2 — 콘텐츠 수명

Phase 1이 끝난 뒤 곡선을 다시 재고 착수한다.

- **손님 등장을 6시간에 배분.** 지금은 `GUESTS`의 `at` 문턱이 낮아 1시간 4분에 7명이 다 온다.
  방치형에서 제일 강한 해금 이벤트를 초반에 다 소모하는 셈이다.
- **약재상 품목 보강.** 지금 2개뿐 (다른 가게는 3~4개). 마지막 가게가 제일 빈약하다.
- **엔드게임 판단.** 마지막 해금 이후 남는 게 레벨업뿐이다.
  환생/이사 루프가 필요한지 여기서 결정한다.

---

## Phase 3 — 조작 피로

6시간에 레벨업 **1945회**. 손가락 노동이다.

- 일괄 레벨업 버튼(×10 / 최대), 또는
- 자동 레벨업 해금 → 플레이어는 "무엇을 열지"만 결정하게 한다

Phase 1·2로 곡선이 바뀌면 레벨업 횟수도 같이 바뀌므로 **순서상 마지막이어야** 한다.

---

## Phase 4 — `core/` 정리

| 파일 | 상태 | 처리 |
|---|---|---|
| `store.js` | ✅ 사용 중 | 유지 |
| `juice.js` | ✅ 사용 중 | 유지 |
| `telemetry.js` | ⬜ 미연결 | **연결** — 해금·이탈 지점 계측 |
| `engine.js` | ⬜ 미연결 | **삭제** — 캔버스 엔진, 이 게임은 DOM 렌더 |
| `ui.js` | ⬜ 미연결 | **삭제** — 광고 오버레이, 붙일 단계 아님 |

---

## 순서 요약

```
Phase 0  진단 도구        ← 선행 필수. 도구가 거짓말하면 나머지가 무의미
   ↓
Phase 1  장바구니         ← 가장 큰 버그. 죽은 품목 7개 부활
   ↓
Phase 2  콘텐츠 수명      ← 곡선이 바뀐 뒤에 재측정
   ↓
Phase 3  조작 피로        ← 곡선 확정 후에야 횟수가 의미 있음
   ↓
Phase 4  core/ 정리       ← 독립적. 아무 때나 가능
```
