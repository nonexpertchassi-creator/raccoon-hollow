# 다음 작업 계획

2026-08-18 · 시뮬레이션(`tools/balance.mjs`) 결과를 근거로 작성.

## 진행 상황

| 단계 | 상태 |
|---|---|
| Phase 0 · 진단 도구 | ✅ 완료 |
| Phase 1 · 장바구니 | ✅ 완료 (무작위 방식으로) |
| Phase 2 · 콘텐츠 수명 | ✅ 완료 |
| Phase 3 · 조작 피로 | ✅ 완료 (자동 강화) |
| Phase 4 · `core/` 정리 | ⬜ 남음 |
| 엔드게임 (5시간 이후) | ⬜ 남음 — 아래 참조 |

### 지금까지의 결과 (8시간 · 씨앗 1·2·3)

| 항목 | 전 | 후 |
|---|---|---|
| 안 팔려 멈춘 품목 | 15개 중 7개 | **0개** |
| 콘텐츠 소진 | 4시간 | **5시간 15분** |
| 손님 7마리가 다 오는 시점 | 1시간 4분 | **4시간 44분** |
| 강화 클릭 | 1,986회 | **약 550회** (자동 강화 구입 뒤 0) |

### 재고 부족을 게임 규칙으로 쓸 수 있는가 — 못 쓴다 (측정 완료)

"손님이 원하는 물건이 없으면 기다리거나 평판이 깎이게 하자"를 검토했다.
**지금 구조로는 튜닝으로 도달할 수 없다.**

먼저 지금 상태:

| | 값 |
|---|---|
| 원하는 개수를 못 채운 거래 | 0.4% (전부 첫 1시간, 2시간째부터 0.0%) |
| 진열대가 텅 빈 시간 | 1.7% |
| 진열대가 꽉 찬 시간 | 16.5% |

병목이 반대쪽에 있다. 물건이 모자란 게 아니라 손님이 모자라 물건이 쌓인다.

손잡이를 다섯 개 돌려봤고 전부 실패했다.

| 손잡이 | 결과 |
|---|---|
| 손님 구매량 1.5~2배 | 과도하게 뒤집힘. 매출이 같이 늘어 콘텐츠 소진이 4시간대로 단축 |
| 생산 하한 0.4→0.45~0.8 | 앞 4시간 꽉 참 + 뒤 3시간 텅 빔. 출렁이지 않고 두 국면으로 갈릴 뿐 |
| 이정표를 생산 2배로 이전 | **더 나빠짐.** 8시간째 진열대 91% 꽉 참 |
| 수요가 품목·가게 수 따라 증가 | 2시간째 재고 0, 거래의 46~59%를 못 채움 |
| 요청 간격 14→60초 | **효과 없음.** 칸을 여는 관문은 타이머가 아니라 돈이다 |

원인은 구조적이다. **생산에는 천장이 있고(`timeFloor`) 수요에는 없다.**
수요가 그 천장을 넘으면 영구히 바닥, 안 넘으면 영구히 꽉 참이라
중간에 머무는 설정이 존재하지 않는다.

주의: 총합 평균은 이 문제를 숨긴다. 생산 하한 0.45의 평균 재고는 19.7/40으로
"딱 중간"처럼 보이지만 시간대별로는 36~39와 0.7의 평균일 뿐이다.
**밸런스는 반드시 시간대별로 볼 것.**

쓰려면 튜닝이 아니라 구조를 바꿔야 한다 — 진열대 확장을 업그레이드로 만들거나,
하위 품목을 상위 품목의 재료로 쓰거나(초기 검토안 (c)), 손님 대기열을 넣는 식이다.

**후속:** 튜닝 대신 구조를 하나 얹어서 이 문제를 부분적으로 풀었다 — '장'이다.
작은 건물이 가끔 북적이고, 그때 누르면 30초간 손님이 두 배로 온다.
수요 쪽에 붙였기 때문에 쌓인 재고가 실제로 빠진다. 8시간 측정:

| | 안 누름 | 볼 때마다 누름 |
|---|---|---|
| 진열대가 꽉 찬 시간 | 15.8% | **6.7%** |
| 평균 재고 | 31.3/40 | **13.3/40** |
| 누적 매출 | 288조 | 335조 (+16%) |
| 콘텐츠 소진 | 5시간15분 | 4시간40분 |

매출이 16%만 오르면서 꽉 찬 시간이 절반 이하로 떨어진다. 튜닝으로는
못 하던 일을 구조 하나로 한 셈이다. 대신 콘텐츠 소진이 35분 앞당겨진다.
231회는 매 기회를 다 누른 상한이므로 실제 플레이는 이보다 완만하다.

### 가게 승급 — 볼거리가 1시간 51분에 끝나던 문제

측정으로 드러난 것: **마을이 1시간 51분이면 다 채워진다.** 가게 다섯, 작은 건물
넷이 전부 서고 나면 지도 위에 새로 생길 게 없고, 그 뒤로는 이미 본 건물 안에서
숫자만 오른다. "1~2시간이면 끝"이라는 체감의 정체가 이것이다.

돌도끼 → 쇠도끼 → 강철도끼. 가게마다 3등급이고, 승급하면 레벨이 1로 돌아가는
대신 밑천이 통째로 좋아진다 — **그 가게만의 작은 환생**이다.

설계에서 지킨 것:

- **승급 직후 값이 직전 만렙보다 커야 한다.** 작으면 애써 만렙을 찍고 승급했는데
  수입이 떨어지는 꼴이 된다. 1급 만렙 ×31 → 2급 시작 ×40, 2급 만렙 ×4,880 →
  3급 시작 ×6,000으로 잡았다.
- **조건은 항목별로 보여준다.** 통과/미통과를 하나씩 나열해야 목표가 된다.
  boolean 하나만 넘기면 잠긴 버튼으로만 남는다.
- **속도 조절은 돈으로 한다.** 처음엔 업적 조건(만렙·손님 수·초당 수입)만 걸었더니
  손님 5종이 열리는 2시간 33분에 네 가게가 동시에 승급하고 3시간 26분에 다 끝났다.
  이 게임에서 실제로 속도를 정하는 건 돈이다(가게 값 9천→90만→1.2억→200억이
  그 증거다). 승급값을 붙이고 나서야 계단이 생겼다.

결과: 마지막 콘텐츠가 **3시간 26분 → 29시간 7분**.

| 승급 | 시점 |
|---|---|
| 대장간 쇠 · 필방 고운 | 2시간 33분 |
| 지물포 고운 | 2시간 41분 |
| 옹기점 오지 | 3시간 55분 |
| 약재상 묵은 | 4시간 49분 |
| 대장간 강철 | 5시간 10분 |
| 필방 명품 | 5시간 51분 |
| 지물포 진상 | 9시간 19분 |
| 옹기점 왕실 | 19시간 53분 |
| 약재상 천년 | 29시간 7분 |

건물 모양은 축을 둘로 나눴다 — 지붕은 등급(초가→기와→단청), 금테·화분은
칸을 다 열었는가. 등급 하나로만 하면 첫 승급까지 두 시간 반 동안 건물이
전혀 안 변한다.

### 단골 — 손님이 굳어 있던 문제

손님은 마을에 온 뒤로 아무 변화가 없었다. 마지막 손님이 4시간대에 오고 나면
일곱이 그대로 굳는다. 자주 오는 손님이 단골이 되게 했다.

    뜨내기 → 낯익은 → 단골 → 귀한 → 터줏대감
    (누적 0 · 500 · 2,500 · 10,000 · 30,000개)

문턱은 실측으로 잡았다 — 토끼가 10분에 515개, 8시간에 34,000개를 사간다.
결과: **단골 승급 23회가 10분부터 11시간 23분까지** 이어진다.

올리는 건 값이 아니라 주로 **개수**다(터줏대감 ×3). 이 게임은 물건이 남아도는
쪽이 병목이라, 수요를 늘려야 쌓인 재고도 같이 빠진다. 장(場) 보너스와 같은 이유다.

가게 승급 조건의 '손님 N종'을 '단골 등급 합계 N'으로 바꿨다. 종류 수는 4시간이면
7로 굳어 조건 구실을 못 하지만, 등급 합계는 계속 자란다(60분 6 → 6시간 20).
합계 문턱은 8·18로 잡았다 — 5·7로 두면 50분에 통과해 조건이 되지 못한다.

화면에서는 단골이 **갓을 쓴다**. 등급이 오를수록 갓이 커지고 색이 진해지며,
터줏대감은 금관자를 단다. 머리 위에 글자를 얹으면 손님이 몰릴 때 다시 겹치므로
모양으로 알린다 — 예전에 금액 글자가 겹쳤던 것과 같은 이유다.

### 손님 12종과 성격 — 그리고 병목이 뒤집힌 사건

손님이 일곱이었고, `every·qty·pay`가 뒤로 갈수록 나란히 커졌다. 그래서 나중 손님이
앞 손님의 **상위호환**이었고, 일곱이든 스물이든 체감이 같았다. 축을 다섯으로 쪼갰다.

| | 오는 간격 | 개수 | 값 | 훑는 종류 | 걸음 |
|---|---|---|---|---|---|
| 까치 | 3.1초 | 2 | ×0.85 | 2 | 1.7 |
| 다람쥐 | 4.2초 | 2 | ×1.15 | 2 | 1.5 |
| 멧돼지 | 80초 | 12 | ×2.2 | **1** | 1.1 |
| 거북 | 110초 | 14 | ×7 | 3 | 0.45 |
| 호랑이 | 400초 | 34 | ×13 | 2 | 0.95 |

`spread:1`(멧돼지·곰)은 한 종류를 통째로 쓸어간다 = 진열대 한 칸이 비는 사건이다.
`speed`는 화면에서만 쓴다 — 거북이 실제로 느리게 걸어야 성격이 읽힌다.

**여기서 병목이 반대로 뒤집혔다.** 일곱이 열둘이 되자 수요가 초당 7.5개 → 14.7개로
뛰었는데 공급은 7.8개가 상한이었다. 진열대 평균 재고 31.3 → **0.2/40**,
못 채우고 돌아간 손님 64%. 여태 "물건이 남아돈다"가 문제였는데 하루아침에
"물건이 없다"가 됐다.

간격에 **일괄 배수**를 거는 건 답이 아니었다. ×2.6을 걸면 앞 네 시간은 37~38로
그대로 꽉 차고 뒤 네 시간만 1로 비는 **두 국면**이 남는다 — 초반 손님까지 뜸해져
심심해지기만 한다. 손님마다 따로 잡았다: **앞은 자주, 뒤 큰손은 드물게.**

    4.6 · 3.1 · 4.2 · 17 · 32 · 50 · 80 · 120 · 110 · 145 · 260 · 400 (초)

|  | 1h | 2h | 3h | 4h | 5h | 6h | 7h | 8h | 못채움 | 꽉참 | 텅빔 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 손보기 전 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 64% | 0% | 87% |
| 일괄 ×2.6 | 37 | 38 | 38 | 36 | 5 | 1 | 1 | 1 | 15% | 11% | 23% |
| **지금** | 25 | 31 | 37 | 32 | 31 | 28 | 2 | 2 | **4%** | 6% | 10% |

씨앗 다섯 개에서 편차 ±0.3. 앞의 여섯 시간이 20~37 사이에 머문다.

**7~8시간대가 2로 주저앉는 건 남겨둔 문제다.** 단골 배수는 ×3까지 계속 자라는데
생산은 `timeFloor`에 막혀 천장이 있다. 콘텐츠 소진(7시간56분) 이후 구간이라
환생 루프가 들어갈 자리와 정확히 겹친다 — 조절이 아니라 구조로 풀 문제다.

**앞 세 마리는 손대면 안 된다.** 토끼를 4.6 → 4.0으로만 줄여도 평균 재고가
15.5 → 7.0으로 반토막 난다. 자주 오는 손님이 총수요를 지배하기 때문이다.

### 단골 20성 — 그리고 눈덩이 사고

5단계 × 12손님 = 작은 사건 60번이었다. 20단계면 240번이다. 호칭은 조선 벼슬로
올라가다 20성에서 **상감마마**가 된다(1 뜨내기 · 5 단골 · 10 터줏대감 · 16 어사또 ·
18 판서 · 19 정승 · 20 상감마마).

**세는 단위를 두 번 갈아엎었다.**

1. *누적 개수* — 손님마다 사가는 속도가 8배까지 벌어진 지금은 못 쓴다.
   까치는 순식간에 상감마마가 되고 호랑이는 영원히 뜨내기다.
2. *속도로 나눈 개수* — 여기서 **눈덩이**가 굴렀다. 등급이 오르면 한 번에
   사가는 개수가 늘고, 그러면 다음 등급이 더 빨리 온다. 두 시간 만에 12성이
   됐고 진열대가 텅 비었다(못채움 28%).
3. **방문 횟수** ← 지금. 등급이 올라도 안 늘어나므로 눈덩이가 안 생긴다.
   `at`(초)을 그 손님의 `every`로 나눠 횟수로 바꾼다. 그래서 **누구든 마을에
   온 뒤 같은 시간이 흐르면 같은 성**이 된다.

배수도 뒤집었다. 예전엔 개수를 ×3까지 밀고 값은 ×1.35만 올렸다 — 그때는
물건이 남아도는 게 병목이었으니까. 손님이 열둘이 된 지금은 정반대라
**개수와 값을 똑같이 ×2.4**로 간다. ×4.2로 두고 재보니 못 사고 간 손님이 32%였다.

| qty 꼭대기 | 평균 재고 | 못채움 | 텅빔 |
|---|---|---|---|
| ×4.2 | 0.9 | 32% | 53% |
| ×3.2 | 3.6 | 13% | 34% |
| **×2.4** | **24.0** | **1%** | **4%** |
| ×1.8 | 36.6 | 0% | 0% |

승급 조건의 '단골 등급 합계'도 8·18 → **32·135**로 다시 잡았다(20성 × 12명이라
최대가 228로 늘었다). 135를 150으로 올리면 대장간과 필방이 같은 분에 승급해
사건이 뭉치고, 175로 올리면 10시간 안에 아예 안 열린다.

콘텐츠 소진: 7시간56분 → **9시간24분**.

### 등장이 시계 같던 문제 — `wild`

`every`만 있으면 호랑이도 **400초마다 정확히** 온다. 시계처럼 오는 희귀 손님은
희귀하게 느껴지지 않는다. 손님마다 `wild`(0~1)를 줬다.

- `wild: 0` — every 그대로. 토끼·까치가 여기 있다. 길에 사람이 끊기면 안 되니까
- `wild: 1` — "초당 1/every 확률로 온다"와 같은 것(지수분포). 호랑이가 여기 있다
- 사이는 섞는다

**평균 간격은 어느 쪽이든 every 그대로**다. 그래서 위에서 잰 밸런스 표를 다시
잴 필요가 없다 — 흔들리기만 한다. 씨앗별 편차가 커진 것(평균 재고 21.7~25.5)이
그 흔들림이고, 이건 없애야 할 게 아니라 넣으려던 것이다.

### 나쁜 놈들 — 화면을 볼 이유

이 게임에는 **지켜볼 이유가 없었다.** 켜 두면 알아서 돌아가고 가끔 강화만 누르면
됐다. 방치형에서 제일 귀한 게 "지금 화면을 봐야 하는 이유"다.

쥐가 평균 75초마다, 까마귀가 평균 110초마다 나타난다(`wild:1`이라 언제일지
모른다). 정해진 시간 안에 누르면 잡고, 놓치면 조금 물고 달아난다.

**설계에서 딱 하나 지킨 것: 놓쳐도 손해가 크면 안 된다.**

방치형 유저는 폰을 꺼놓고 잔다. 아침에 "밤새 다 털렸다"가 뜨면 그 자리에서
지운다. 그래서 벌이 아니라 **상**으로 뒤집었다. 실측:

```
8시간 · 한 번도 안 잡았을 때 — 잃는 것이 누적매출의 0.62%
8시간 · 매번 잡았을 때       — 버는 것이 누적매출의 9.3%
```

**놓쳐서 잃는 것보다 잡아서 버는 것이 열다섯 배 크다.** 안 보고 있으면
잃는 게 아니라, 보고 있으면 버는 구조다. 폰을 꺼놓고 자도 게임은 멀쩡하다.

화면에서 두 가지를 챙겼다.

- **남은 시간 고리.** 몇 초 안에 눌러야 하는지 안 보이면 그냥 지나친다
- **마을 안쪽으로 달아난다.** 처음엔 가까운 마을 끝으로 보냈는데, 대장간처럼
  가장자리에 붙은 가게에서는 화면 밖으로 반쯤 잘린 채 서 있어 누를 수가 없었다
- 판정 반경 30~32 — 손가락은 뭉툭하고 놈들은 뛰거나 날고 있다

한 가지 알아둘 것: 4시간 무렵 곡선이 제일 가파른 구간에서는 "매번 잡음"이
누적매출을 열 배로 벌린다. 쥐가 센 게 아니라 그 시점의 곡선이 가파른 것이다 —
잡으면 **10분쯤 앞서간다**는 뜻이고, 활동 보상으로는 그 정도가 맞다.

#### 까마귀 — 같은 걸 두 번 만들지 않기

쥐를 색만 바꿔 내면 두 번째는 아무 느낌도 없다. 갈라놓은 축이 셋이다.

|  | 쥐 | 까마귀 |
|---|---|---|
| 훔치는 것 | 진열대 **물건** | 전대의 **엽전** |
| 어디에 | 그 가게 앞, 땅 | **보이는 화면을 가로질러**, 하늘 |
| 시간 | 5.5초 | 4.0초 |

엽전은 **가진 돈의 비율이 아니라 초당수입 2초치**로 잡았다. 비율로 하면
아껴 모은 사람만 크게 털린다 — 모으는 게 벌이 되면 안 된다.

까마귀를 화면 기준으로 날린 이유: 쥐처럼 가게에 묶어 두면 마을 다른 데를
보고 있을 때 아예 못 본다. 하늘에 있으니 무엇에도 안 가리고, 마을 어디를
보고 있든 눈앞을 지나간다.

한 가지 더 — **일정한 속도로 지나가면 시간의 절반을 화면 밖에서 쓴다.**
4초를 줘도 실제로 누를 수 있는 건 3.4초였다. 들어올 땐 빠르게, 가운데선
천천히 맴돌다, 나갈 때 다시 빠르게 나가도록 곡선을 넣었다.

#### 지금 수치 (8시간, 안 잡았을 때 대 매번 잡았을 때)

```
쥐      334마리   잃는 것 0.17%   버는 것 3.4%
까마귀  233마리   잃는 것 0.45%   버는 것 5.8%
─ 합계           잃는 것 0.62%   버는 것 9.3%  (15배)
```

**한 번에 한 마리만** 나온다. 둘이 겹치면 화면 위 알림 띠가 서로를 밀어내
어느 쪽도 안 읽힌다.

#### 삽살개 — 자리를 비운 사이를 맡는다

나쁜 놈은 "보고 있으면 이득"이지만 하루 종일 볼 수는 없다. 삽살개는 그 빈 곳을
메운다. 마을 어귀 오른쪽에 개집이 서고, 200만냥에 들인다.

**여기서 제일 조심한 것: 개가 더 잘 잡으면 안 된다.** 개가 사람보다 나으면
아무도 화면을 안 보게 되고, 그러면 나쁜 놈을 넣은 이유가 통째로 사라진다.
그래서 두 겹으로 깎았다 — 무는 확률 60%, 벌금은 직접 잡을 때의 1/6(3배 대 20·13배).

```
10시간 · 개 없이 자리 비움     -0.87%
10시간 · 삽살개만 믿고 비움    +1.2%
10시간 · 직접 매번 잡음       +13.9%   ← 개의 11배
```

개를 들여도 **직접 잡는 게 열한 배 낫다.** 개는 "안 보면 손해"를 지워줄 뿐,
"보면 이득"을 대신해 주지는 않는다.

화면에서는 개가 개집 앞에 앉아 졸다가, 나쁜 놈이 나오면 **일어나 들썩이며 '멍!'**
한다. 지금 지켜주고 있다는 걸 글자 없이 알리는 장치다.

지금은 돈으로 산다. 뽑기·인앱구매가 들어오면 그쪽으로 옮긴다 —
`sim.buyGuard()` 하나만 갈아 끼우면 된다.

### 주인공 너구리 — 세웠다가, 뺐다

게임 이름이 **너구리 만물상**인데 화면에 너구리가 없어서 '사장 너구리' 한
마리를 세웠다. 마을을 돌며 방금 판 가게로 가서 물건을 건네게 했다.

**그리고 뺐다.** 이유가 둘이다.

1. **사장은 플레이어다.** 화면 밖에서 마을을 보고 있는 사람이 곧 사장인데,
   그 사람의 분신이 필드를 종종거리면 내가 둘이 된다.
2. **하는 일이 없었다.** 초당 0.9건씩 팔리니 어느 가게에도 제때 못 닿고
   왔다 갔다만 했다.

대신 **가게마다 점장**을 두고, 그 점장이 만들고 팔고 존다. 사장은 화면에
안 나온다 — 방치형 타이쿤의 표준 구도이기도 하다(사장은 매니저를 고용한다).

이렇게 하면 나중에 **점장 고용·승급**이 그대로 축이 된다. 뽑기·인앱구매를
붙일 자리도 여기다.

그리고 그림 두 장(`raccoon-make` · `raccoon-sell`)은 안 버렸다 —
점장의 기본 그림으로 그대로 쓴다.

### 가게마다 점장 너구리 — 만들고 · 팔고 · 존다

주인공 하나가 마을을 왔다 갔다 하니 **가게에 사람이 없는 시간**이 생겼다.
가게마다 점원 너구리를 하나씩 붙였다.

점장은 자기 가게 안에서 **자리를 두 개** 오간다.

    작업대 자리   가게 안쪽 0.18/0.82    평소
    손님 자리     0.52/0.48              손님이 오면 몇 걸음 나온다
    (손님)        길 쪽 0.74/0.26        41px 떨어진 자리

64px를 130px/s로 가면 0.5초다. 손님이 서 있는 2.3초, 물건이 건네지는 건
1.1초 시점이니 **항상 제때 닿는다.** 예전 사장 너구리는 마을 반대편에서
출발하느라 한 번도 제때 못 닿았고, 그래서 '파는 중' 자세가 실제로는 화면에
나온 적이 없었다.

세 모습이다: **만듦**(작업대 옆) · **팖**(나가서 건넴) · **졺**(진열대가 꽉 참).

#### 계기판은 캐릭터에게 시키지 않는다 — 좌판의 고리

처음엔 점원이 **실제 제작 주기(1.2~3.6초)에 맞춰 두드리게** 했다. 정보는
정확했지만 **캐릭터에게 계기판을 시킨 셈**이었다. 다섯 마리가 각자 다른
박자로 쉬지 않고 움직이는데, 정작 "다음 게 언제 나오나"는 여전히 눈으로
세어야 했다.

정확한 진행은 **좌판 칸의 고리**가 맡는다. 칸 하나가 두 가지를 같이 보여준다.

    가운데 숫자 = 지금 쌓인 개수
    두른 고리   = 다음 한 개까지 얼마나 남았나 (prog / craftTime)

이게 강화를 처음으로 **눈에 보이게** 만든다 — 지금까지 강화는 숫자만 바뀌고
화면은 그대로였다. 이제 고리가 눈에 띄게 빨리 돈다.

너구리는 계기판에서 풀려나 **분위기만 맡는다.** 2.4초짜리 느긋한 한 박자로
움직이고, 제작 주기와는 아무 상관이 없다.

고리에서 두 번 손본 것:

- **막힌 칸은 빨간 고리를 한 바퀴 다 그린다.** 꽉 차면 생산이 멈추고 고리도
  멈추는데, 하필 거의 빈 상태로 멈추면 '막혔다'가 아니라 '방금 시작했다'로
  보인다. 조는 점원과 같은 신호를 색과 모양 둘 다로 준다
- **바탕 고리를 진하게(.18 → .32).** 좌판이 차면 배경이 주황으로 바뀌는데
  옅은 바탕 고리는 거기서 통째로 사라졌다

#### "생산을 늦춰야 하나"에 대한 답 — 안 늦춰도 된다

다섯이 동시에 두드리면 정신없을까 봐 실제 제작 시간을 재봤다.
(지금은 고리가 그 일을 맡지만, 이 숫자는 고리의 회전 속도라 그대로 유효하다.)

| | 1분 | 15분 | 60분 | 120분 | 240분 |
|---|---|---|---|---|---|
| 대장간 | 2.82초 | 1.94 | 1.94 | 1.23 | 1.23 |
| 필방 | — | 2.71 | 2.71 | 1.72 | 1.72 |
| 약재상 | — | — | 3.61 | 3.61 | 2.51 |

**제일 빠를 때가 1.2초에 한 번**이다. `LEVEL.timeFloor`(0.40)가 이미 천장을
막고 있어서 아무리 강화해도 이보다 빨라지지 않는다. 1~3초에 한 번 두드리는
건 느긋한 박자다 — **생산을 늦출 이유가 없다.**

가게마다 위상을 어긋나게 뒀다(`i * 0.37`). 다섯이 똑같이 맞춰 움직이면
기계처럼 보인다.

#### 조는 점원은 귀여움이 아니라 알림이다

진열대가 꽉 차면 `sim`이 실제로 생산을 멈춘다(`stock >= STOCK_CAP`).
**그 사실이 화면에 안 보여서 유저가 눈치를 못 챘다** — 어느 칸이 막혔는지
알려면 숫자를 하나씩 봐야 했다.

이제 그 가게 점원이 존다(💤). 조는 너구리가 보이면 "저기 물건이 남아돈다,
팔 곳을 늘리거나 다른 걸 강화해라"는 뜻이다. 8시간 중 진열대가 꽉 찬 시간이
5~6%라 자주 뜨지도 않는다.

#### 칸마다 한 마리(대장간 4마리)는 왜 안 했나

자리는 **넣을 수는 있다** — 188px에 4마리를 0.12/0.37/0.62/0.87로 놓으면
간격 47px, 28px짜리라 19px씩 남는다. 다만 그러면 손님과 주인이 설 자리가
없어져 줄을 둘로 나눠야 한다.

진짜 걸림돌은 자리가 아니라 **그림이다.** 칸마다 다른 점원을 그리면
17종 × (일함·졺) = **34장**이 더 든다. 지금 계획 전체가 34장인데 그게 두 배가 된다.

그래서 순서를 이렇게 잡았다:
1. **지금** 가게마다 1마리 (그림 5종 × 2 = 10장)
2. **나중** '점원 고용'으로 칸마다 늘리기 — 그때는 그림값이 비용이 아니라
   **콘텐츠**가 된다. 돈을 모아 두 번째 대장장이를 들이는 것이 곧 성장이다

#### "주문 받고 생산"은 다른 장르다

지금은 `만든다 → 쌓인다 → 팔린다`다. 주문부터 받으면 `주문 → 만든다 → 준다`가
되는데, 이건 방치형이 아니라 **주문 처리 게임**(디너대쉬 계열)이다.
자리를 비우면 주문이 밀리므로 방치가 성립하지 않고, 여태 맞춘 밸런스
(진열대 평균 22~25, 못채움 1%)가 통째로 무의미해진다.

넣는다면 기본 순환이 아니라 **특별 주문** 같은 사건으로 얹는 게 맞다 —
"산삼 20뿌리를 30분 안에" 같은 것. 그건 지금 구조 위에 그대로 올라간다.

### 손님이 자판기처럼 굴던 문제 — 주문 · 전달 · 퇴장

"명령 주입된 손님들이 가게 방문해서 호다닥 사가는 기분"이라는 말이 정확했다.
도착하는 **그 프레임에** 재고가 빠지고 엽전이 튀고 0.7초 뒤 떠났다.
장사가 아니라 자판기였다.

세 박자로 나눴다.

```
0.0초   도착. 머리 위에 "3개 · 2종" 말풍선 — 주문
1.1초   주인 너구리가 건넨다. 이때 재고가 빠지고 엽전이 튄다
2.3초   짐을 지고 떠난다
```

걸음도 늦췄다: `155 → 95` (곁걸음 175 → 105, 퇴장 150 → 92).
**손님 수가 아니라 한 명이 오래 머무는 것이 마을을 붐비게 만든다.**

#### 자리를 늘려야 했다

한 명이 화면에 남는 시간이 5~7초에서 10~12초로 늘었다. 자리가 여섯이던
시절 그대로 두면 거래가 줄줄이 잘린다. 실측으로 맞췄다.

| 자리 | 실제 판매 | 화면에 나온 손님 | 동시 최대 |
|---|---|---|---|
| 10 | 27건 | 21명 (78%) | 11 |
| **14** | 26건 | **25명 (96%)** | 15 |

초당 판매가 0.87~0.90건이니 14자리면 거의 다 나온다. 마을 세로가 1560인데
한 화면에는 절반쯤만 보이므로 **동시에 눈에 들어오는 건 예닐곱**이다.
넘치면 그 거래는 화면에 안 나온다 — 계산은 이미 끝나 있으니 손해는 없다.

#### 말풍선을 두 번 옮겼다

- 글자는 **개수만.** 품목 이름까지 넣으면 손님이 몰릴 때 서로 겹친다 —
  예전에 금액 글자로 똑같은 실수를 했다
- **깊이 정렬 밖(z=9e8)에 그린다.** 줄에 세우면 앞에 선 것에 가려 못 읽는다
- **가게 반대쪽(길 쪽)으로 20px 비껴 띄운다.** 머리 바로 위에 두면
  좌판 칸을 덮어 재고 숫자가 안 보인다

### 뒤뚱뒤뚱은 그림이 아니라 코드로

"이동할 때 뒤뚱뒤뚱 모션이 있으면 좋겠다, 꼬리가 흔들리면 그렇게 보이려나"
— 꼬리보다 **몸통을 기울이는 게 훨씬 세게 읽힌다.** 뒤뚱거림의 정체가
무게중심이 좌우로 넘어가는 것이라, 흔들려야 하는 건 꼬리가 아니라 몸이다.

걷는 동안 스프라이트를 **±6도 기울인다.** 위아래 들썩임과 반박자 어긋나게
둬서 두 발로 걷는 것처럼 만든다. **그림은 한 장도 안 든다.**

걷는 그림을 준다면 **두 장이면 충분하다**(왼발·오른발). 30px짜리에서
4장은 2장과 구별이 안 된다. 코드가 `-walk1`/`-walk2`를 찾고, 없으면
`-idle`, 그것도 없으면 `-work`로 내려간다. 기울기는 어느 쪽이든 그대로 얹힌다.

#### 상하좌우 — 좌우만 있으면 된다

점장이 오가는 두 자리는 **가로 64px, 세로 3px** 떨어져 있다. 사실상 좌우
운동이라 정면·뒷면은 화면에 나올 일이 없다.

손님은 길을 따라 위아래로 걷지만, 위에서 살짝 내려다본 3/4 시점에서는
**움직이는 방향과 무관하게 카메라를 보는 것이 표준**이다. 뒷모습을 넣으면
그림이 두 배가 되고 얻는 건 거의 없다. **오른쪽 보는 것 하나만** 그리고
왼쪽은 코드가 뒤집는다.

#### 자세 목록이 넷이 됐다

`-work`(작업대에서 숙임) · `-idle`(차렷, 걸어갈 때) · `-sell`(앞발 내밈) ·
`-sleep`(앉아서 졺). 지금 쓰는 임시 그림은 앞발을 가슴께에 모으고 있어
**돈을 들고 있는 것처럼** 보인다 — 걸어갈 때 쓸 차렷 자세가 따로 필요하다.

### 폰에서 얼마나 작은가 — 재고, 높이로 풀었다

"생각보다 작을 것 같다"는 걱정이 맞았다. 캔버스는 가로 480을 화면 폭에
맞추는데 폰이 375~393이라 **배율이 0.78~0.82**다. 코드에 적은 값의 8할이
화면에 나온다. 좌판 물건 이름이 **7.1 CSS px — 1.9mm**였다.

#### 넓히기로는 못 푼다

가게 폭을 재봤더니 이미 한계였다. 길이 가운데를 지나므로 양옆에 179px씩밖에
안 남는다 — 대장간 188, 넓힐 수 있는 최대 **190**. 2px이다.

#### 높이로 풀었다 — 좌판을 두 줄로

한 줄에 네 칸을 두 줄에 두 칸씩으로 바꿨다. **칸 폭이 37 → 71.**

| | 전 | 후 |
|---|---|---|
| 칸 폭 | 37px | **71px** |
| 물건 이름 | 7.1 CSS px | **11.1 CSS px** |
| 재고 숫자 | 10.3 | **15.0** |

폰에서 글씨는 11 CSS px부터 편하게 읽힌다 — **둘 다 넘겼다.**

값은 마을 길이다. 가게 높이 150 → 192, 마을 1560 → **1750**(1.12배).
스와이프가 조금 길어지는 건 감수하기로 했다.

곁들여 바꾼 것:
- 길 휨 ±24 → ±18, 폭 74 → 66 (가게 자리를 벌기 위해)
- 가게 위치를 길의 곡선에 맞춰 다시 잡았다. 길이 오른쪽으로 부풀면 왼쪽에
  자리가 남는다 — 다섯 곳 다 길과 25~48px 여유를 두고 배치했다
- 작은 건물·개집은 **가게 맞은편**으로. 가게 사이(80px)에 끼웠더니 위 가게
  점장이 서는 자리를 깔고 앉았다
- 등급을 좌판에서 현판으로(`⚒ 대장간 · 쇠`). 칸마다 '쇠곡괭이'를 넣을 이유가 없다

#### ★ 캔버스가 화면보다 46px 컸다 (오래된 고장)

이걸 재다가 발견했다. 캔버스 상자가 **위로 23px, 아래로 23px 삐져나와**
있었다. 위는 버튼 줄 뒤로 숨고 아래는 화면 밖이었다 — **마을이 계속 잘려
있었고 아무도 몰랐다.**

원인: 캔버스 크기를 처음 맞추는 시점이 머리말·버튼 줄이 채워지기 **전**이라,
그 뒤 상자가 줄어들어도 창 크기(resize)는 안 바뀌니 다시 맞출 일이 없었다.

고침: 창이 아니라 **담는 상자를 ResizeObserver로 지켜본다.** 여섯 기기에서
캔버스와 상자가 픽셀 단위로 일치하는 것을 확인했다(아이폰SE·15·갤S24·
폴드 접음/폄·아이패드).

덤으로 카메라도 고쳤다. 창이 바뀔 때 **끝에 붙어 있었으면 끝에 그대로**
붙여 둔다. 가운데만 붙잡으면 마을 어귀를 보다가 어귀에서 밀려나는데,
처음 켤 때가 정확히 그 경우였다.

#### 다음 단계는 물건 그림

이름 글자가 11.1px까지 왔지만, **물건 그림 17장이 들어오면 이름은 뺀다.**
칸이 71px이니 30×52 그림이 넉넉히 들어가고, 그림이 글자보다 훨씬 잘 읽힌다.

### 화면이 둘이 됐다 — 마을과 가게 안

"지금 상태를 지도라 칠 때, 대장간을 누르면 그 안을 보게 할 수 없나"

할 수 있고, **작다는 문제를 구조로 푸는 방법**이기도 하다.

마을 한 화면에 가게 다섯을 다 담으니 가게 하나가 화면 폭의 35%였다. 그래서
좌판 글자가 1.9mm가 됐고, 두 줄로 바꿔 겨우 11px을 만들었다. **한 화면에
하나만 보여주면 같은 물건이 저절로 2.5배가 된다.**

| | 마을 | 가게 안 |
|---|---|---|
| 재고 숫자 | 15 CSS px | **23** |
| 진행 고리 지름 | 32 | **50** |
| 점장 | 33 | **52** |

역할도 갈렸다.

    마을     어디에 무엇이 있나 · 어느 폐허를 되살릴까 · 나쁜 놈 잡기
    가게 안  이 가게에서 무슨 일이 벌어지나 · 강화 · 칸 열기 · 승급

#### 시점 — 아이소메트릭 바닥 + 정면 캐릭터

바닥을 마름모로 깔면 평면이 아니라 공간으로 읽힌다. 사람은 **지금 그림
그대로 정면**을 본다. 흔한 조합이고, 그래야 이미 그린 너구리·손님을
다시 안 그린다. 새로 그려야 하는 건 바닥·벽·작업대뿐인데 그건 지금 코드로
그린다.

#### 배치를 네 번 고쳤다

1. **방이 화면 가운데 작게 뜨고 위아래가 텅 빔** — 마름모를 화면 폭 안에
   다 넣으려 했다. 실제 이런 게임은 카메라가 방 안에 있어 벽이 잘린다.
   **화면보다 크게** 잡아 좌우가 잘리게 했다
2. **작업대 넷을 마름모 네 꼭짓점에 놓았더니** 앞쪽 것이 문·손님·점장과
   자리를 다퉜다 → **뒤쪽 두 벽에 붙이고 앞 절반을 통째로 비웠다**
3. **2:1 정통 비율로 했더니 벽에 붙인 작업대 둘이 화면 밖으로** 나갔다.
   벽을 따라 움직이면 가로가 세로의 두 배로 벌어지기 때문이다 →
   폭에 맞추고 깊이만 늘린 **눌린 아이소메트릭**(1.13:1)으로
4. **작업대 하나가 세로 108px을 써서** 앞 작업대가 뒤 작업대의 이름표를
   덮었다 → 단추 층을 없애고 이름·레벨을 한 줄에 합쳐 93px로

지금 자리는 벽을 따라 0.30과 0.70이다. 더 붙이면 뒤쪽 두 이름표가 겹치고,
더 벌리면 앞쪽 둘이 화면 밖으로 나간다.

#### 번들러에 이름 충돌 검사기를 붙였다

`scene.js`와 `interior.js`가 둘 다 `W`와 `C`를 쓰다가, 한 파일로 묶는
순간 터졌다. 파일로 나눠 쓸 때는 각자 자기 안에서만 사는 이름이지만
이어 붙이면 한 덩어리가 된다. **브라우저를 켜야만 보이는 종류의 고장**이라
`tools/bundle.mjs`가 묶기 전에 잡는다.

#### 두 화면을 잇는 것 — 0.3초 확대

화면이 툭 갈아치워지면 '다른 메뉴로 갔다'가 된다. 눌린 가게 자리에서 방이
부풀어 오르면 '그 안으로 들어갔다'가 된다. 같은 두 화면인데 이어져 있다는
느낌이 여기서 나온다.

한 건물을 이어 붙인 도면(고양이 게임들)이 우리보다 나은 점이 정확히 이
'끊기지 않음'인데, **그 값의 대부분을 0.3초짜리 확대로 산다.**

확대되는 동안은 손가락을 안 받는다 — 움직이는 자리를 누르면 엉뚱한 게 눌린다.

#### 왜 '한 건물 도면'으로 안 갔나

고양이 게임처럼 방을 여러 개 이어 붙여 하나로 만들 수도 있었다. 안 한 이유:

1. **바깥이 우리 정체성이다.** 무너진 마을, 구불구불한 길, 장승, 길을 걷는
   손님, 쥐와 까마귀, 삽살개 — 저쪽 게임들은 전부 실내다. 한 건물로 바꾸면
   이야기가 '마을을 되살린다'에서 '내 건물을 넓힌다'로 바뀌고, 동시에
   똑같이 생긴 게임이 된다
2. **크기 문제는 어느 쪽이든 똑같이 풀린다.** 둘 다 "한 방이 화면을 채우게
   당긴다"이고, 차이는 이어져 있느냐뿐이다 — 그건 확대로 샀다
3. **값이 그림에 붙는다.** 한 건물로 이으면 방마다 바닥·벽·가구가 한 결로
   있어야 한다. 저 게임들이 돈을 쓰는 곳이 정확히 거기다(수백 장)

**'도면 느낌'은 가게 안에서 낼 수 있다.** 승급하면 방이 넓어지고 칸이
늘어나게 하면 된다(돌급 한 칸 → 쇠급 작업방+창고 → 강철급 손님방까지).
이미 있는 승급 축을 그대로 쓰고, 방금 만든 가게 안 화면 위에 얹힌다.

#### 남은 것

- 방이 아직 휑하다. 화덕·물통·선반 같은 소품이 필요하다(코드로 그릴 수 있다)
- 물건 그림이 들어오면 작업대 위에 올라간다. 여기선 32×48로 그려진다
- 가게마다 방을 다르게 할지(대장간은 화덕, 옹기점은 가마) — 그림값이 붙는다

### 걱정 두 가지 — 들락날락하는 수고와 그림값

#### 1. 들어갈 이유가 없으면 안 들어가면 된다

**"일일 던전처럼 숙제가 되면 떠난다"** — 맞는 걱정이고, 처음 문구가 정확히
그 실수였다. `2가지 할 일`이라고 적었는데 그건 숙제다.

일일 던전이 의무가 되는 진짜 이유는 **만료**다. 하루 놓치면 손해가 나니까
안 하면 안 되는 게 된다. **여기선 아무것도 사라지지 않는다** — 언제 들어가도
그대로 있다. 그러니 남은 건 문구뿐이었고, 개수 대신 **뭐가 좋은지**를 적었다:
`승급!` · `새 칸!`. 초대장에 가깝게.

두 화면이 되면 오가는 수고 때문에 유저가 떠날까 하는 걱정. 대책은 하나다 —
**"혹시 뭐 생겼나" 하고 들여다볼 일을 없애는 것.**

지도의 가게 위에 **할 일 표**를 띄운다. 표가 없으면 안 들어가도 된다.

세는 것: 열 수 있는 칸 · 승급.
**안 세는 것: 강화** — 늘 할 수 있어서 표가 항상 켜져 있게 되고,
항상 켜진 표는 없는 것과 같다. 게다가 자동 강화를 사면 아예 할 필요가 없다.

10시간을 돌려 실제로 셌다.

```
가게에 들어갈 이유가 생긴 횟수: 20번
시간대별: 9 · 4 · 1 · 3 · 0 · 1 · 1 · 0 · 1 · 0
자동 강화 구입: 18분 (이후로는 강화하러 들어갈 일이 없다)
```

첫 시간에 9번은 배우는 구간이라 오히려 좋다. **그 뒤로는 한 시간에 한 번꼴**
이다. 오가는 수고가 문제가 될 빈도가 아니다.

#### 2. 가게 안 때문에 늘어난 그림은 0장이다

바깥과 안이 **같은 그림을 크기만 다르게** 쓴다. 코드에서 확인된다 —
`interior.js`가 요청하는 그림 종류가 `scene.js`와 완전히 같다
(`clerks` · `guests` · `items` · `pests` · `hero`).

| | 마을에서 | 가게 안에서 | 만드는 크기 |
|---|---|---|---|
| 점장 | 33 | 52 | `72×72` 한 장 |
| 손님 | 33 | 46 | `64×64` 한 장 |
| 물건 | 44×50 | 32×48 | `64×112` 한 장 |

가게 안의 바닥·벽·작업대는 전부 코드다.

**그리고 목록 자체를 줄였다.** 점장을 가게마다 4포즈씩 20장으로 잡아 두었던
것을 **공용 4장(필수) + 가게별 16장(선택)**으로 갈랐다.

```
필수 36장 = 점장 4 + 물건 17 + 손님 12 + 나쁜 놈 3
선택 16장 = 가게별 점장
```

네 차례로 나눠 **어디서 멈춰도 게임이 돈다.** 안 그린 것은 이모지로 나온다.

### 마을을 격자로 갈아탔다 — scene.js에서 iso.js로

"이미지만 갈아끼우면 사실상 바둑판형 마을도 될 수 있다"는 지적이 결정타였다.
고양이 게임의 본질은 실내가 아니라 **격자**였고, 격자 위에 우리 바깥 마을을
그대로 올릴 수 있다.

세로 두루마리(scene.js)의 진짜 약점은 **가게가 늘 때마다 아래로만 길어지는
것**이었다(1560 → 1750, 가게 열이면 3000). 격자는 가로로도 퍼지고, 빈 칸이
눈에 보여서 "폐허를 하나씩 채운다"가 지도 자체로 읽힌다.

- 바닥 한 칸 96×48(가로:세로 2:1 — 이 시점의 기본 비율), 마을 10×14칸
- 가게 2×2칸, 곁가게·개집 1×1칸, 건물은 전부 코드(상자+지붕) — **새 그림 0장**
- 카메라가 2차원이 됐다. 위아래만 밀던 것을 어느 방향으로든 민다
- 경제(sim.js)는 한 줄도 안 바뀌었다. 좌판 두 줄·진행 고리·주문 박자·
  단골 갓·나쁜 놈·삽살개·점장, 전부 그대로 옮겨 탔다

#### 자리는 (tx−ty)가 정한다 — 밟은 함정

격자에서 화면의 가로 위치는 tx가 아니라 **tx−ty**다. '왼쪽/오른쪽'을 tx로
갈랐다가 대장간(5,11)과 필방(1,7)이 화면에서 **정확히 같은 세로줄**(둘 다
tx−ty=−6)에 포개졌다 — 필방 좌판이 대장간 지붕 뒤로 사라졌다.

지금은 세 세로줄(−240 / 0 / +240px)에 번갈아 놓고, 같은 줄 안에서는 세로로
240px 이상 벌린다. 건물+현판+표가 230px쯤을 쓰기 때문이다. 곁가게도 같은
이유로 대장간 세로줄을 통째로 피해서 놓았다.

#### 손님은 직선으로 걷는다

세로 두루마리에선 큰길을 따라 내려오다 가게 앞에서 꺾었다. 격자에서 그
규칙을 그대로 쓰면 마을 반대편까지 도는 우회가 생긴다. 직선으로 바꿨다 —
아이소메트릭에서 직선은 어차피 비스듬한 길로 보인다.

#### scene.js는 은퇴

파일은 남겨 두되 게임은 iso.js를 쓴다. 묶음(bundle)에서도 뺐다.
가게 안(interior.js)과 0.3초 확대는 그대로 산다.

### 가게를 상자에서 마당으로 — '가게 안' 화면 은퇴

"이제 건물 안으로 들어가지 말자. 내부 간판대를 밖으로 빼자"는 결정.
지붕 덮인 2×2 상자를 **3×3 마당**으로 열어젖혔다. 안이 지도에서 다 보이니
들어갈 이유가 없고, interior.js(가게 안 화면)는 은퇴했다.

마당 아홉 칸의 쓰임:

```
가마(1) · 작업대(1) · 매대(품목마다 1, 최대 4) · 계산대(1) · 빈 바닥(2)
```

- **매대 한 칸 = 좌대 + 물건 + 재고·진행 계기 + 이름패.** "책상과 학생이
  한 칸"인 참고 게임의 문법 그대로다
- **계산대가 보인다** — 손님이 그 앞에 서고, 점장이 나와서 건넨다.
  엽전 접시까지 올려놨다
- 매대를 **직접 누르면 강화**, 잠긴 매대는 누르면 열린다. 마당의 나머지를
  누르면 가게 창(승급·자세한 숫자)이 열린다
- 1×1칸 = 96×48 가상 px (폰에서 약 20×10mm). 소수점 배치도 자유다 —
  가마는 0.56칸, 계산대는 0.68×0.4칸을 쓴다

품목이 늘어나는 날엔 **대장간2가 아니라 승급으로 마당을 넓힌다**
(3×3 → 3×4 → 4×4). 이미 있는 승급 축이 '땅이 넓어지는 것'으로 보이게 된다.
지금은 안 만든다 — 품목 4개에 3×3이면 맞는다.

#### 밟은 함정 둘

1. **매대 2×2 뭉치기.** 마당 가운데 2×2로 놓았더니 화면에서 마름모로
   포개져 앞 매대의 계기가 뒤 매대를 덮었다. **뒤 두 담벼락을 따라**
   두 개씩(ㄱ자 진열)으로 바꿨다.
2. **터치 판정 겹침.** 이웃 칸은 화면에서 48px밖에 안 떨어진다.
   '먼저 맞은 것'을 고르면 점포가 도끼 매대의 터치를 삼켰다(실제로 그랬다).
   매대는 **제일 가까운 것**을 고르고, 점포는 한 칸 옆으로 옮겼다.

### 마당이 등급 따라 자란다 — 그리고 콘셉트·가림·주문 셋 손봄

**① "대장간이 물건 4개만 팔면 섭섭하다"** — 대장간에 가위·부엌칼을 더해
품목 6개가 됐고, 매대 수를 마당 크기에 묶었다.

    무쇠급 3×3 마당 = 매대 4칸 → 참쇠급 4×4 = 6칸 → 강철급 5×5 = 8칸

대장간2가 아니라 **같은 마당이 넓어진다.** 격자라서 승급이 "땅이 커지는
것"으로 공짜로 보인다. 격자도 4분면 배치(13×18, 길이 십자로 가름)로 넓혀
마당이 5×5까지 자라도 길·이웃을 안 밟는다.

밟은 함정: 승급 조건이 "모든 칸을 만렙"인데 5번째 칸은 승급해야 생긴다 —
**서로를 영원히 기다렸다.** 실제로 10시간 내내 무쇠급에 잠겼다. 조건을
"지금 있는 매대"로 고쳤다. 그리고 손님이 매대 없는 물건을 조르면 약만
오르므로, 물어보기도 매대가 있는 물건만 하게 했다.

**② "돌부터 시작하는 콘셉트를 잘못 잡았다"** — 맞는 지적이었다. 대장장이가
돌도끼부터 시작하는 건 말이 안 된다. 등급을 **무쇠 → 참쇠 → 강철**로 바꿨다.
무쇠(부어 만든 거친 쇠), 참쇠(두드려 단련한 쇠) — 실제 조선의 쇠 등급이라
시대 콘셉트에도 맞고, 첫날부터 '무쇠곡괭이'라 대장간답다.

**③ "매장이 동물을 가린다"** — 마당을 한 덩어리로 그려서 생긴 버그였다.
마당의 z가 아래 꼭짓점이라, 마당 **안에 서 있는** 손님·점장보다 항상 앞이었다.
바닥은 맨 뒤로 보내고 **가구(가마·매대·계산대) 하나하나를 자기 발끝 z로**
깊이 정렬에 태웠다. 이제 손님이 계산대 앞에 서면 계산대를 가리고, 매대
뒤로 걸으면 매대에 가려진다 — 당연한 일이 당연해졌다.

**④ "주문 받고 생산하는 게 낫지 않나"** — 경제는 안 바꾸고 **연출만** 바꿨다.
손님이 주문하면 점장이 **그 물건의 매대에 들러 집어서** 계산대로 가져와
건넨다. '주문 받고 내온다'가 눈에 보인다. 경제까지 주문식으로 바꾸면
방치가 성립하지 않고(자리를 비우면 주문이 밀린다) 여태 잰 밸런스가 통째로
무의미해진다 — 진짜 주문제작은 나중에 '특별 주문'(산삼 20뿌리를 30분 안에)
같은 **사건**으로 얹는 것이 맞다.

### 직원 고용 — "주문 후 생산"의 진짜 정체

"주문 받고 생산"을 다시 짚어보니 원하던 건 주문식 경제가 아니라 **직원**이었다.
점장 혼자 만들고 팔다가 벅차지면 일손을 들이고, 한 마리는 생산 · 한 마리는
판매, 손이 비면 같이 만드는 것.

넣었다. 자리 수는 등급이 정한다 — **무쇠급 0명 · 참쇠급 1명 · 강철급 2명.**
무쇠급에 자리가 없는 이유: 매대 4칸은 점장 혼자로 충분하고, 마당이 6칸으로
커지는 순간이 정확히 '혼자서는 벅차지는' 순간이다. 값은 승급값÷4라 승급
직후마다 모을 목표가 하나 더 생긴다.

**효과를 정하는 데 두 번 실패했다.**

1. *생산 +22%* — 진열대의 25%가 꽉 찬 채 생산이 멈췄다. 공급·수요를 몇
   시간 재서 맞춰 놨는데 공급만 22%씩 올리면 균형이 통째로 무너진다.
2. *진열대 +10칸* ← 지금. 초반을 안 건드리고, 쌓아둔 것이 후반 큰손
   (곰·호랑이)의 싹쓸이를 받아낸다.

| | 직원 전 | 직원 후 |
|---|---|---|
| 못 사고 간 손님 | 4% | **0%** |
| 텅 빈 칸 | 5~10% | **0%** |
| 꽉 찬 칸 | 6~9% | 12% |
| 콘텐츠 소진 | 9시간43분 | **9시간49분** |

측정 함정도 하나: 상한이 40이 아니게 됐는데 도구들이 40을 '꽉참'으로 세서
꽉참 58%라는 유령 수치가 나왔다. 도구를 `capOf`로 고치니 12%였다.
**지표를 만들면 상한이 변수라는 것도 같이 기억해야 한다.**

화면: 직원은 자리를 안 옮긴다. 자기 모루만 지키고, 점장이 계산대로 불려가도
계속 만든다 — '한 마리는 생산, 한 마리는 판매'가 그림으로 보인다.
고용은 가게 창에서 하고, 살 수 있으면 지도에 '일손!' 표가 뜬다.

### 남은 문제 두 가지

1. **5시간 15분 이후 할 게 없다.** 마지막 품목(우황)이 열린 뒤로는 자동 강화가
   숫자만 올린다. 환생/이사 같은 반복 루프를 넣을지 결정해야 한다.
2. **4시간~5시간 15분 사이에 79분짜리 빈 구간이 있다.** 우황(12조)을 모으는
   시간이다. 방치형에서 '앱을 끄고 나중에 오는' 구간이라 나쁘다고만 볼 수는
   없지만, 그 앞뒤보다 유독 길다.

---

## 아래는 최초 계획 원문 (기록용)

---

## 지금 상태 진단

6시간 시뮬을 돌려 나온 숫자 세 개가 이 계획의 전부다.

| 관측 | 숫자 | 의미 |
|---|---|---|
| 재고 상한에 박혀 생산이 멈춘 품목 | 12개 중 7개 (2시간 시점) | 콘텐츠의 절반이 죽어 있다 |
| 마지막 해금 | 3시간 33분 (약재상) | 6시간이면 할 게 없다 |
| 손님 전원 등장 | 1시간 4분 | 제일 센 카드를 첫 1시간에 다 쓴다 |
| 레벨업 : 의미 있는 선택 | 1945 : 14 | 조작의 99.3%가 버튼 연타 |

### 근본 원인 — `sim.js:_buy()`

```js
open.sort((a, b) => this.price(b) - this.price(a));
const id = open[0];        // 손님이 "제일 비싼 것 한 종류"만 사간다
```

손님 한 명이 품목 **한 종류**만, 그것도 항상 최고가부터 사간다.
그래서 하위 품목은 재고가 `STOCK_CAP`(40)에 도달하고, `tick()`의

```js
if (st.stock >= STOCK_CAP) continue;   // 진열대가 꽉 차면 생산 정지
```

에 걸려 **생산까지 멈춘다.** 한 번 멈추면 영영 안 팔리므로 영구히 죽는다.

2시간 시점 실측:

```
곡괭이 lv160  재고 40/40  ✗ 죽음      부채   lv 89  재고  2/40  ✓
낫     lv150  재고 40/40  ✗ 죽음      창호지 lv 80  재고  1/40  ✓
호미   lv141  재고 40/40  ✗ 죽음      옹기   lv 71  재고  1/40  ✓
붓     lv124  재고 40/40  ✗ 죽음      사발   lv 62  재고  0/40  ✓
도끼   lv132  재고 40/40  ✗ 죽음      한지   lv 97  재고 25/40  △
먹     lv115  재고 40/40  ✗ 죽음
벼루   lv106  재고 40/40  ✗ 죽음
```

곡괭이를 160레벨까지 올렸는데 초당 수입 기여가 **정확히 0**이다.

---

## Phase 0 — 진단 도구 (선행 필수)

다른 어떤 것보다 먼저다. **지금 `balance.mjs`는 위 문제를 못 잡고 "문제 없음"이라고 보고한다.**
도구가 거짓말하는 상태로 밸런스를 고치면 고쳤는지 알 수가 없다.

### 0-1. 정지 판정 수정 — `tools/balance.mjs`

```js
if (Object.values(s.items).every((x) => x.stock >= 40)) idle++;
```

`every`라서 **전부** 꽉 차야 센다. 상위 품목은 계속 팔리므로 조건이 영원히 거짓 →
7개가 죽어 있는데도 `생산 정지 0.0%`로 나온다.

→ 전역 플래그를 버리고 **품목별로** 정지 시간을 누적한다.

### 0-2. 품목별 매출 기여도 표 추가

죽은 품목이 `0.0%`로 한눈에 보이게 한다. 이게 Phase 1의 합격/불합격 판정 기준이 된다.

```
■ 품목별 매출 기여
   곡괭이    0.0%   정지 94%    ← 죽음
   낫        0.0%   정지 91%    ← 죽음
   ...
```

### 0-3. 콘텐츠 소진 시점 출력

마지막 해금(가게·품목·손님)이 일어난 시각 = **더 할 게 없어지는 시각**.
Phase 2의 목표 수치가 여기서 나온다.

### 0-4. 진단 규칙 추가

```
if (죽은 품목이 하나라도 있음)  ⚠ 품목 N개가 재고 상한에 박혀 생산 정지
if (마지막 해금 < 플레이 시간)  ⚠ N시간 시점에 콘텐츠 소진
```

**완료 기준:** 고친 도구를 지금 코드에 돌렸을 때 위 문제 3건이 전부 ⚠로 잡혀야 한다.
안 잡히면 도구가 아직 틀린 것이다.

---

## Phase 1 — 장바구니 (핵심)

**결정: 손님 한 명이 여러 품목을 섞어서 산다.**

검토했던 대안:
- *(a) 손님별 취향* — 손님마다 가격대가 다름. 손님이 캐릭터가 되지만 `content.js` 확장 필요.
- *(c) 재료 체인* — 하위 품목이 상위 품목 재료로 소모됨. 가장 깊지만 `sim.js` 재설계 수준.

(b)를 고른 이유: **`_buy()` 하나만 고치면 되고, 죽은 품목 7개가 즉시 되살아난다.**
가장 큰 버그를 가장 작은 변경으로 잡는다. 취향·재료 체인은 이게 안정된 뒤에 얹어도 늦지 않다.

### 1-1. 분배 규칙

재고가 **많은** 품목부터 사간다. 상한에 박힌 품목이 먼저 빠져나가므로 정지가 자동으로 풀린다.

```js
_buy(g) {
  const have = Object.keys(this.items).filter((id) => this.items[id].stock > 0);
  if (!have.length) return null;

  // 재고 내림차순 — 캡에 박힌 품목을 우선 비워 생산을 재개시킨다
  have.sort((a, b) => this.items[b].stock - this.items[a].stock);

  const perItem = Math.max(1, Math.ceil(g.qty / 3));   // 손님 하나가 최소 3종류를 건드린다
  let left = g.qty, gain = 0;
  const lines = [];

  for (const id of have) {
    if (left <= 0) break;
    const n = Math.min(left, perItem, this.items[id].stock);
    if (n <= 0) continue;
    const g2 = Math.floor(this.price(id) * g.pay * n);
    this.items[id].stock -= n;
    left -= n; gain += g2; this.sold += n;
    lines.push({ item: itemById(id), n, gain: g2 });
  }

  this.money += gain;
  this.revenue += gain;
  return { guest: g, lines, gain, n: g.qty - left };
}
```

`perItem`의 분모 `3`은 시작값일 뿐이다. **Phase 0 도구로 튜닝해서 결정한다.**

### 1-2. 파급 — `index.html` 판매 메시지

반환 모양이 `{ item, n }` → `{ lines[], gain }`으로 바뀐다. 이걸 쓰는 곳:

```js
// index.html:265 부근 — 지금은 단일 품목 가정
`${s.guest.name}가 <b>${s.item.name}</b> ${s.n}개를 사갔다 · <b>${fmt(s.gain)}냥</b>`
```

→ `곰이 옹기 외 2종 12개를 사갔다 · 3.4억냥` 같은 형태로 바꾼다.
**여기를 안 고치면 화면에 `undefined`가 뜬다.** Phase 1에서 같이 처리한다.

### 1-3. 확인해야 할 부작용

- **매출 폭주.** 죽어 있던 7개가 되살아나면 초당 수입이 뛴다. 6시간 곡선을 다시 재고,
  필요하면 `STOCK_CAP`이나 손님 `qty`로 조인다.
- **최고가 우선의 상실.** "비싼 걸 사간다"는 손맛이 약해질 수 있다.
  재고 우선이 지나치면 재고 순 + 가격 순 가중 혼합으로 조정한다.

**완료 기준:** Phase 0 도구에서 **매출 기여 0.0%인 품목이 하나도 없어야** 한다.

---

## Phase 2 — 콘텐츠 수명

Phase 1이 끝난 뒤 곡선을 다시 재고 착수한다.

- **손님 등장을 6시간에 배분.** 지금은 `GUESTS`의 `at` 문턱이 낮아 1시간 4분에 7명이 다 온다.
  방치형에서 제일 강한 해금 이벤트를 초반에 다 소모하는 셈이다.
- **약재상 품목 보강.** 지금 2개뿐 (다른 가게는 3~4개). 마지막 가게가 제일 빈약하다.
- **엔드게임 판단.** 마지막 해금 이후 남는 게 레벨업뿐이다.
  환생/이사 루프가 필요한지 여기서 결정한다.

---

## Phase 3 — 조작 피로

6시간에 레벨업 **1945회**. 손가락 노동이다.

- 일괄 레벨업 버튼(×10 / 최대), 또는
- 자동 레벨업 해금 → 플레이어는 "무엇을 열지"만 결정하게 한다

Phase 1·2로 곡선이 바뀌면 레벨업 횟수도 같이 바뀌므로 **순서상 마지막이어야** 한다.

---

## Phase 4 — `core/` 정리

| 파일 | 상태 | 처리 |
|---|---|---|
| `store.js` | ✅ 사용 중 | 유지 |
| `juice.js` | ✅ 사용 중 | 유지 |
| `telemetry.js` | ⬜ 미연결 | **연결** — 해금·이탈 지점 계측 |
| `engine.js` | ⬜ 미연결 | **삭제** — 캔버스 엔진, 이 게임은 DOM 렌더 |
| `ui.js` | ⬜ 미연결 | **삭제** — 광고 오버레이, 붙일 단계 아님 |

---

## 순서 요약

```
Phase 0  진단 도구        ← 선행 필수. 도구가 거짓말하면 나머지가 무의미
   ↓
Phase 1  장바구니         ← 가장 큰 버그. 죽은 품목 7개 부활
   ↓
Phase 2  콘텐츠 수명      ← 곡선이 바뀐 뒤에 재측정
   ↓
Phase 3  조작 피로        ← 곡선 확정 후에야 횟수가 의미 있음
   ↓
Phase 4  core/ 정리       ← 독립적. 아무 때나 가능
```

---

## 2026-08-19 · 계산대 규칙 — 돈·장부의 타이밍, 계산 멈춤, 기다리는 손님

유저 보고 세 가지를 한 번에 고쳤다.

**1) 돈과 장부가 손님보다 먼저 움직였다.**
sim은 계산을 즉시 끝내는데 화면의 손님은 아직 걸어오는 중이라,
"오기도 전에 사갔다고 나온다"가 됐다 — 유저 눈엔 그냥 버그다.
경제는 그대로 두고 **보여주는 쪽만 늦췄다**:
- 위쪽 엽전 표시는 `sim.money − village.inTransit()` — 계산 연출이 아직
  안 끝난 몫을 빼고 보여준다 (`inTransit()`은 걷는 중인 손님들의 미지급 합).
- 아래 장부(로그)는 sim이 아니라 **화면이 쓴다** — 손님이 계산대에서
  물건을 받는 순간 `village.onDelivered` 콜백이 울린다.
  로그 품목명도 등급 붙은 이름(무쇠곡괭이)으로 바뀌었다.

**2) 점장 혼자인데 생산과 판매를 동시에 했다.**
`content.js`의 `SERVICE.servePause`(1.2초): 계산이 한 번 끝날 때마다
직원 0명인 가게는 그 시간만큼 생산이 멈춘다(`sim._hold`).
직원을 들이면 계산 중에도 직원이 계속 만든다 — 고용의 이유가 하나 더 생겼다.
(손님이 주문을 기다리는 동안은 안 멈춘다 — 그때 점장은 그 물건을 만드는 중이다.)

**3) 물건이 없으면 손님이 그냥 사라졌다.**
이제 **기다리는 주문**이 있다 (`sim.orders`):
- 모자란 개수 × 생산 시간(+앞 주문 몫)이 참을성(`SERVICE.patience` 6초)
  이하면 손님이 기다린다. 갓 만든 물건은 진열대보다 주문 먼저 받는다.
- 넘으면 💢 남기고 그 매대만 포기한다 — 재고는 안 건드린다.
  전부 포기하면 가게 앞까지 왔다가 빈손으로 돌아간다(💢 연출).
- 돈·매출·단골 방문은 전부 물건이 다 나온 순간(`_settle`)에 움직인다.
  참을성×2를 넘기면 나온 만큼만 계산하고 보낸다.

**측정 (10시간, 씨앗 1):**

| 항목 | 값 |
|---|---|
| 기다렸다 받아간 방문 | 10.9% (평균 6.1초) |
| 빈손 💢 | 0.6% |
| 생산 정지(꽉 참) | 16~37% — 전엔 곡괭이 53%가 최악, 지금 최악 37% |
| 콘텐츠 소진 | 7시간46분 (전 9시간49분) |

재고 0에서 돌아가던 손님이 이제 주문으로 사가므로 **후반이 빨라졌다**.
소진이 2시간 당겨진 건 나쁜 신호가 아니라 페이즈2 품목(다른 가게들 이름만
지으면 됨)을 넣을 이유가 하나 더 생긴 것.

진단에 빈손 8% 초과 경고를 추가했다 — 참을성·생산 수치를 만질 때 이 표를 볼 것.

### 2026-08-19 · 직원 최대 4명 (유저 제안)

참쇠 1·강철 2였던 직원 자리를 **참쇠 2 · 강철 4**(등급×2, `STAFF.max`)로 늘렸다.
3·4번째 직원 값은 한 명마다 30배씩 뛴다 — 10시간 측정에서 아무도 3번째까지
못 갔고, 그게 의도다. 콘텐츠가 다 열린 뒤에도 살 게 남는 장기 목표.

측정(10시간, 씨앗 1): 콘텐츠 소진 7:46 → **8:42** (2번째 직원들이 돈을 빨아들여
승급이 알맞게 늦어짐). 계산대·빈손·생산정지 수치는 그대로 — 초반은 안 건드렸다.

---

## 2026-08-19 · 돈 단위 개편 — 경(京)에서 억(億)으로 (유저 지적)

"몇 시간 만에 조 단위가 나오는 건 현실감이 없다"는 유저 지적. 맞는 말이다 —
10시간 누적매출이 1.31경이었다. 폭주의 범인은 곱셈 네 겹:
강철 승급 ×6,000 · 레벨당 +50% · 25레벨마다 ×2 · 품목 사다리 ×1,000만(곡괭이→우황).

**네 겹을 전부 눌렀다**: 승급 [1,40,6000]→[1,4,16] · 레벨당 +12% ·
25레벨마다 ×1.25 · 우황 기본가 1.15억→6.5만. 대신 **모든 관문의
'수입 몇 초치' 무게는 기준판에서 옮겨와** 페이스를 지켰다.

방법: 기준판 10시간을 돌려 관문 42개의 (시각, 수입 초치)를 기록하고
(`calibrate.mjs`), 새 곡선 위에서 `새값 = √(지금값 × 목표값)` 감쇠 반복
(`iterate.py`)으로 8판을 수렴시켰다. 진자처럼 흔들리다 평균 어긋남 17%에 정착.

| | 개편 전 | 개편 후 |
|---|---|---|
| 10시간 누적매출 | 1.31경 | **1,229억** |
| 끝 초당 수입 | 2,687억 | **391만** |
| 최대 표시 단위 | 경 | **억** (최고가 목표 = 약재상 천년급 1,000억) |
| 콘텐츠 소진 | 8:42 (+1:17 공백) | **9:22 (+0:37 공백)** |
| 계산대 (기다림/빈손) | 10.9% / 0.6% | 10.8% / 1.1% |

직원값도 같이 손봤다(유저 지적 둘째): 첫 직원이 수입 36초치였다 →
**첫째 = 승급값 ×3 (≈7분치), 둘째 = 2차 승급값 절반 (≈20~40분치)**,
셋째·넷째는 ×3·×9로 10시간 밖의 목표. `STAFF.costMul = [3, 0.5, 3, 9]`.

옛 저장과 물가가 안 맞아 저장 열쇠를 `rs:save2`로 바꿨다 — 전원 새 출발.
씨앗 1·2·3 모두 진단 통과(소진 경고뿐, 9:10~9:22).

---

## 2026-08-20 · 머리 위 표시 정리 — 주문표·매상 표·손해 표

**1) 주문표: '5개 · 2종' → ⛏️3 🪴3 🌾1**
'2종'은 무엇을 사가는지 안 알려준다. 알려면 아래 장부를 봐야 하는데,
그러면 머리 위에 띄운 뜻이 없다. 품목마다 `icon`(이모지)을 붙이고
그림과 개수를 그대로 적는다. 세 가지까지 적고 나머지는 `+N`으로 줄인다 —
넷을 넘기면 말풍선이 손님보다 커져 길을 덮는다.
그림(`art/items/*.png`)이 준비되면 이모지 자리에 그대로 들어간다.

**2) 점장이 가게 밖에 서 있었다 · 계산대가 점장을 덮었다**
둘은 한 뿌리였다. 계산대만 옛 z(마당 끝 `S.y`)로 세워져 있어서
마당 **안**의 점장·손님보다 항상 앞이었다(가구를 하나씩 세운 이유가 바로
이건데 계산대만 빠져 있었다). 게다가 점장의 계산 자리가 `cc+(-34,+12)` —
마당 밖 길가였다. 계산대를 제 발끝 z로 세우고, 점장을 손님 자리의
정반대(`cc+(-32,-16)`, 마당 안)로 옮겼다. 3·4·5칸 마당 전부에서
**점장 마당 안 · 손님 길가 · 깊이 순서 점장 < 계산대 < 손님**을 계산으로 확인했다.
이제 점장은 어떤 상황에서도 제 마당을 안 벗어난다(작업대·매대·계산대 전부 안쪽).

**3) 지붕 위 층 규칙 — 화면이 복잡해지는 걸 막는 방법**
매상 표가 '새 칸!' 배지와 **똑같은 높이**(N.y−100)를 노려 서로 덮고 있었다.
가게가 다섯이 되면 그게 다섯 배가 된다. 자리를 옮기는 대신 **층을 못 박았다**:

```
현판(이름) → 배지(할 일) → 매상 표(방금 번 돈) → 손님 질문
계속 있는 것이 아래, 잠깐 있는 것이 위. 한 층이 비면 위층이 내려온다.
```

지붕 위 빈 하늘은 매대도 동물도 안 가리는 유일한 자리다. 계산대 위에도
띄워 봤지만 매대 이름과 엽전 연출을 덮어서 되돌렸다(`_roofBase`).
**표가 늘지 않는 진짜 장치는 합산이다** — 같은 가게의 연속 판매는 한 장에
숫자만 커진다. 가게가 몇이든 가게당 최대 한 장.

**4) 엽전 → 부호 → 숫자, 그리고 손해(−)**
`+🪙1,387` → `🪙 +1,387`. 도둑이 털어가면 붉은 `🪙 −290`이 그 가게 지붕에
뜬다(엽전을 물어간 까마귀는 날아간 자리에). 삽살개가 물어오면 `🪙 +N`.
`sim.tick()`이 `pests[]`로 넘긴다 — 판매(`sales`)·주문완료(`done`)와 같은 방식.
손해 표는 2.1초로 조금 더 오래 남긴다: 드물게 일어나고, 놓치면
도둑을 잡을 이유 자체가 안 생긴다.

17분 소크(도둑 9회·주문 최대 8건·손님 최대 15마리) 오류 0.

---

## 2026-08-20 · 마을 다시 그리기 — 길로만 다니고, 계산대는 길가에

두 가지 보고가 사실은 **한 뿌리**였다.
"손님이 길 아닌 곳으로 마구잡이로 온다" + "대장간 갔다가 필방 가면 대장간을 뚫고 지나간다".

**뿌리 1 — 계산대가 길에서 제일 먼 자리에 있었다.**
`SHOP_T`는 마당의 **뒷** 모서리였고 마당은 앞으로 자랐다. 계산대는 늘 앞
모서리라, 승급할 때마다 계산대가 길에서 한 칸씩 멀어졌다. 3칸 마당에서는
길에 닿지도 않았다. **뒤집었다** — `SHOP_T`가 이제 마당의 **앞(길 쪽)
모서리**이고, 마당은 길 반대쪽으로 자란다. 가게를 넓힌다고 가게 앞이
옮겨 다니면 안 된다. 넓어지는 건 뒷마당이다.
(`_org(i)`가 뒷 모서리를 계산해 준다. SHOP_T를 쓰던 12곳을 전부 이걸로 바꿨다.)

**뿌리 2 — 손님이 목적지까지 직선으로 걸었다.**
세로 두루마리 시절의 규칙이 격자에 그대로 남아 있었다. 이제 **길칸만 밟는다**:
너비우선탐색으로 길을 찾고(길칸 70여 개, A*까지 갈 일이 아니다), 꺾이는
칸만 남겨 걸음이 잘게 끊기지 않게 하고, 길 모양은 안 변하니 한 번 찾은
길은 넣어 두고 다시 쓴다(`_route`/`_points`/`_send`/`_follow`).

**새 지도 (15×18)**
```
큰길 두 줄(세로 5·6열, 12·13열) · 골목 두 줄(가로 5행, 11행)
→ 여섯 구획. 가게 다섯 + 가운데 장터 하나.
가게는 모두 제 구획의 길 쪽 모서리에 앞을 두고 뒤로 자란다.
```
| | |
|---|---|
| 손님 서는 칸이 길인가 | 다섯 가게 전부 ✓ |
| 마당이 길을 밟나 | 없음 ✓ |
| 정문·가게끼리 다 이어지나 | 전부 ✓ |
| 화면에서 겹칠 뻔한 가게 짝 | 2쌍 → **1쌍**(필방·지물포, 세로 312px) |
| 강철급 전원 6분 30초 실측 | 손님 370명 · **길 이탈 0%** · 갇힘 0 |

작은 건물 넷은 가운데 장터로 모았다 — 예전 자리는 화면에서 대장간 마당
모서리에 얹혀 보였다. 스물다섯 칸을 전부 재서 서로 136px 이상 벌어지는
네 칸을 골랐고, 삽살개는 마을 오른쪽 풀밭(마당에서 193px)으로 옮겼다.

---

## 2026-08-20 · 마당 생김새 넷 · 줄 서기 · 고용 잠금 해제

**1) 가게가 다 똑같이 생겼다 → 마당 생김새 A/B/C/D**
계산대는 늘 마름모 아래 꼭짓점, 매대는 늘 뒤 두 담벼락이었다. 길이 둘로
늘어도 가게는 한 종류였다. 네 가지로 나눴다:

| | 계산대 | 매대 | 가마 |
|---|---|---|---|
| **A 마주보기** (대장간) | 앞 꼭짓점 | 뒤 두 변(Λ자) | 뒤 꼭짓점 |
| **B 왼쪽살림** (필방) | 오른 변 가운데 | 왼쪽 두 변 | 오른 꼭짓점 |
| **C 길가진열** (옹기점·약재상) | 오른 꼭짓점 | 오른쪽 두 변 — 길에서 물건이 보인다 | 왼 꼭짓점 |
| **D 옆문** (지물포) | 왼 변 가운데(골목 쪽) | 오른쪽 두 변 | 뒤 꼭짓점 |

**담은 넷 다 뒤 두 변에만 세운다.** 앞쪽에 담을 세우면 마당 안이 가려지고
깊이 순서까지 뒤집어야 해서 얻는 것보다 잃는 게 크다 — 생김새는 담이 아니라
**계산대·매대·가마의 자리**로 가른다. 계산대는 어느 생김새든 반드시
길에 닿는 변에 둔다(BR은 항상 큰길, BL은 가로 골목이 있는 가게만).
4종 × 3등급 = 12가지를 계산으로 확인: 매대 칸수 정확 · 계산대/가마/직원 겹침 0 ·
전부 마당 안 · 계산대는 늘 길가.

**2) 손님이 겹쳐 서서 말풍선이 뭉갰다 → 줄 서기**
가게마다 줄(`this.line`)을 만들었다. 먼저 온 손님이 계산대 앞 0번,
나머지는 길을 따라 47px 간격으로 뒤에 선다. 앞사람이 빠지면 한 칸씩 당겨 선다.
**주문표는 맨 앞사람만** 띄운다 — 줄을 세운 이유의 절반이 이것이다.
뒤에 줄이 섰으면 계산이 빨라진다(×0.62). 실측: 겹침 5.4%, 최대 줄 6명, 갇힘 0.

**3) 화난 이모지가 너무 늦었다 → 4초부터**
참을성 6초를 다 쓰고 포기할 때만 💢가 떴다. 이제 **기다림이 4초(참을성의 2/3)를
넘으면 😠**가 먼저 뜬다. ★ 줄 서서 기다리는 시간은 안 센다 — 줄은 눈에 보이라고
만든 연출이지 손해가 아니다(계산은 이미 끝나 있다). 그것까지 화를 내게 했더니
가게 앞 손님의 **28.9%**가 성난 얼굴이 됐다. 지금은 **3.4%** — 뜨면 진짜 신호다.

**4) 고용이 승급을 기다렸다 → 돈만 있으면 (유저 지적)**
승급 조건이 다섯 가지라 두 시간 가까이 걸리는데, 일손이 필요한 건 바로
그 전 구간이다. 이제 승급과 무관하게 뽑는다. 자리 수만 등급이 늘린다
(무쇠 2 · 참쇠 3 · 강철 4). 첫 직원 값도 첫 승급값의 3배 → **12%**로 내렸다.

| | 전 | 후 |
|---|---|---|
| 첫 직원 | 84분 (승급 뒤) | **37분** (승급과 무관) |
| 빈손 💢 | 1.1% | **0.5%** |
| 기다렸다 받아감 | 10.8% | **4.9%** |
| 콘텐츠 소진 | 9시간22분 | 8시간26분 (씨앗 2·3: 8:15·8:22) |

**5) 너구리가 매대에 가려졌다 (유저 지적)**
물건을 집을 때 **매대 칸 안**에 서게 해 놨었다(+26,+14). 매대 상자가 낮아도
점장이 그 칸 뒤쪽에 서는 셈이라 머리끝까지 덮였다. 이제 매대 **옆 칸**
(마당 안쪽, 다른 매대·계산대·가마를 피해서)으로 물러선다 — 깊이가 같거나
한 칸 뒤라, 가려도 발끝뿐이다.

---

## 2026-08-20 · 화난 채로 오던 손님 · 숫자 단위 고르기

**1) 손님이 마을에 화난 채로 걸어 들어왔다 (유저 보고) — 진짜 버그였다**
살 게 하나도 없는 손님(`sold`가 빈 손님)을 **그것만으로** 💢로 그렸다.
그래서 그 손님은 아직 묻지도 않았는데 화난 얼굴로 들어와, 줄까지 성난 채
서 있었다. 이제 💢는 **계산이 끝난 뒤에만**(`w.paid`) 뜬다 —
'볼일이 끝났는데 못 샀다'는 뜻이니 그게 맞다.
실측: 도착 즉시 화남 **0 / 248명**.

**2) 화남을 언제 세는가 — 줄 시간은 안 센다 (재보고 내린 결론)**
유저 제안대로 '줄에 선 뒤 4초'로 만들어 재 봤더니:

| | 줄 시간까지 셀 때 | 물건 기다림만 셀 때 |
|---|---|---|
| 초반 | 4.0% | 0.8% |
| 중반 | **79.3%** | 23.4% |
| 후반 | **97.5%** | 16.2% |

이유는 단순하다 — **잘되는 가게는 화면에 세울 수 있는 것보다 훨씬 빨리
팔린다.** 줄은 늘 꽉 차 있으니 '줄 시간'은 가게 사정이 아니라 연출의 한계를
재는 셈이 되고, 그렇게 뜬 화남은 신호가 아니라 소음이다.
지금 😠는 하나만 뜻한다: **물건을 제때 못 만들고 있다.**
문턱은 참을성 6초의 3/4 = **4.5초**('4초대', 유저가 말한 범위 안).

곁들여 고친 것:
- 줄이 꽉 찬 가게(4명)에 도착하면 그 가게는 조용히 지나친다. 예전엔 도착
  시점에 안 막아서 줄이 여섯까지 밀렸다. 지나칠 때 **도로 더해 둔 재고를
  반드시 풀어 준다** — 안 풀면 그 몫이 진열대 숫자에 영원히 얹혀,
  빈 매대가 꽉 찬 것처럼 보인다.
- 붐빌수록 계산이 빨라진다(2명 ×0.7 · 3명 이상 ×0.5).

**3) 숫자 단위 — 만·억 ↔ K·M 단추 (유저 질문)**
머리띠 오른쪽에 단추를 달았다. 기본은 **한국식**이다: 이 게임은 조선
만물상이고 한국말은 네 자리마다 단위가 바뀐다(만 10⁴ · 억 10⁸) —
'1.2억'은 한국 사람이 소리 내어 읽는 그대로다. 세 자리로 끊는 K·M이
편한 사람도 있어서 한 번 누르면 바뀐다(1.23억 ↔ 123M). 소리 설정처럼
따로 기억하고, 누르면 열려 있는 창까지 즉시 다시 그린다.

---

## 2026-08-20 · 직원이 안 보이던 버그 · 직원 상한을 재서 정함 · K 단위

**1) 직원을 뽑았는데 한 마리도 안 보였다 (유저 보고) — 진짜 버그**
직원은 담벼락에 안 붙은 **안쪽 칸**에만 세우게 해 놨는데, 무쇠급 3×3 마당은
안쪽 칸이 딱 하나이고 그걸 작업대가 쓴다 → **설 자리 0개.**
돈만 나가고 화면에는 아무 일도 안 일어나는, 제일 나쁜 종류의 고장이었다.
이제 매대·계산대·가마가 안 쓰는 담벼락 칸을 뒷자리로 내준다.
4종 마당 × 3등급 12가지에서 자리 수 충분 · 겹침 0 확인.

**2) 직원 최대 몇 명이 안전한가 (유저 질문) → 재보니 답은 "한 명"**
고용 비용을 빼고 직원 수만 박아 8시간씩 두 씨앗으로 돌렸다:

| 가게당 직원 | 생산정지(꽉참) | 빈손 💢 | 기다렸다 받음 | 8시간 매출 |
|---|---|---|---|---|
| 0명 | 0.2% | 1.86% | **79.1%** | 486억 |
| **1명** | 27.2% | 0.00% | **0.7%** | **961억** |
| 2명 | 26.6% | 0.00% | 0.6% | 933억 |
| 3명 | 26.1% | 0.00% | 0.8% | 996억 |
| 4명 | 26.2% | 0.00% | 0.8% | 1008억 |
| 6명 | 26.2% | 0.00% | 1.0% | 960억 |

**둘째부터는 아무것도 안 바뀐다**(전부 오차 범위). 이유가 분명하다 —
계산 중 생산 멈춤은 한 명이면 이미 완전히 사라지고, 진열대는 이미 26%가
꽉 차 있어 칸을 더 줘도 쌓을 데가 남아돌 뿐이다. **병목이 생산이 아니라 수요다.**
아무 일도 안 하는 걸 수십억에 파는 건 함정이므로 **가게당 1명**으로 못 박았다.
(상한을 2·4·6·8로 바꿔 10시간을 돌려도 결과가 똑같았다 — 10시간 안에는
상한이 아니라 **돈**이 병목이라 애초에 둘째를 못 뽑는다.)
더 뽑게 하려면 직원에게 **새 일거리**를 줘야 한다(호객 → 손님이 더 자주 옴 같은).

**3) 사자마자 화난 손님 (유저 보고)**
두 가지가 겹쳐 있었다. ① 😠가 기다림이 끝난 뒤에도 안 꺼졌다 — 물건을 받아
들고 돌아서는 내내 성난 얼굴이었다. 이제 **기다리는 동안만** 뜬다.
② 몇 개 사고 한 종류를 포기한 손님에게도 💢를 띄웠다. 이제 💢는
**빈손으로 돌아갈 때만**. 실측: 계산 끝난 손님 94명 중 화남 0명.

**4) 숫자는 K·M 하나로 (유저 결정)**
만·억과 고르는 단추를 달아 봤지만 고르는 게 일이지 얻는 게 없었다.
세 자리로 끊는 K·M·B로 통일하고 단추를 뺐다.
