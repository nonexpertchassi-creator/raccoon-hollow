# 무엇을 세는가

**지표는 거슬러 올라가 모을 수 없다.** 게임을 낸 뒤에 *"첫날 사람들이 어디서
그만뒀지"*가 궁금해져도, 그때 심으면 그날부터의 것만 남는다. 첫 주는 영영 못 본다.

그래서 사람이 오기 **전에** 심는다. 이 문서는 *무엇을 세고, 어디에 쌓고,
그걸로 무엇을 알아내려는지*의 계약서다.

---

## 지금 어디까지 왔나

| | 상태 |
|---|---|
| 세는 자리 (`sim.stats`) | **있다** — 스물여섯 가지 |
| 저장 | **된다** — 껐다 켜도 이어진다 |
| 판(대시보드) | **있다** — `tools/dash.mjs`가 HTML 한 장을 만든다 |
| 봇으로 채워 보기 | **된다** — `tests/balance.gd`가 여덟 시간을 돌면 그 자국이 남는다 |
| 서버로 보내기 | **아직 없다.** 폰 안에만 쌓인다 |
| 게임 안 의견 창구 | **아직 없다** |
| 결제 | **아직 없다** |

---

## 세는 것 — 스물여섯 가지

이름은 `무엇.어떻게` 꼴로 짓는다. 나중에 종류별로 묶어 보려면 이름이 규칙적이어야 한다.

### 손으로 하는 일 (무엇을 하고 노나)

| 이름 | 뜻 | 이걸로 아는 것 |
|---|---|---|
| `tap.level` | 매대를 눌러 강화한 횟수 | 손가락을 얼마나 쓰나 |
| `tap.levelMany` | '최대'로 한 번에 올린 횟수 | 꾹 누르기·최대 단추가 쓸모 있나 |
| `tap.pest` | 나쁜 놈을 손으로 잡은 횟수 | 잡는 재미가 실제로 쓰이나 |
| `tap.fair` | 장을 연 횟수 | |
| `open.item` | 매대 칸을 새로 연 횟수 | |
| `open.shop` | 가게를 되살린 횟수 | |
| `open.promote` | 가게를 승급한 횟수 | 승급까지 가는 사람이 얼마나 되나 |
| `open.staff` · `open.guard` | 직원·삽살개를 들인 횟수 | 편의를 돈으로 사나 |

### 뽑기와 룰렛 (이 게임의 심장)

| 이름 | 뜻 | 이걸로 아는 것 |
|---|---|---|
| `gacha.pull` | 뽑은 총 장수 | |
| `gacha.pull1` · `pull10` · `pull30` | 몇 장씩 뽑나 | 한 장씩 vs 열 장씩 — 어느 쪽을 쓰나 |
| `gacha.grade1`…`grade6` | 등급별로 나온 장수 | **고지한 확률과 실제가 맞나** |
| `gacha.newGuest` | 처음 만난 손님 수 | |
| `star.up` | 성을 올린 횟수 | 카드를 모아 쓰나, 쌓아만 두나 |
| `roul.free` | 무료로 돌린 횟수 | |
| `roul.ad` | **광고 보고 돌린 횟수** | 광고를 얼마나 보나 |
| `roul.adSkip` | 광고를 중간에 끊은 횟수 | 광고가 너무 긴가 |

### 흐름과 이탈

| 이름 | 뜻 | 이걸로 아는 것 |
|---|---|---|
| `run.start` | 게임을 켠 횟수 | 며칠 만에 다시 오나 |
| `run.seconds` | 켜 두었던 시간(초) | 한 번에 얼마나 하나 |
| `run.offline` | 오프라인 수익을 받은 횟수 | 자리를 비웠다 돌아오나 |
| `quest.take` · `quest.done` · `quest.drop` | 의뢰를 받고·마치고·놓친 횟수 | 의뢰가 숙제가 되나 |
| `event.join` · `event.clear` | 기간제 이벤트에 발을 뗀·끝낸 횟수 | **이벤트 참여율** |

### 아직 못 세는 것

| | 왜 |
|---|---|
| 튜토리얼 단계별 도달 | 튜토리얼이 아직 한 칸(대장간 화살표)뿐이다 |
| 결제 | 결제가 없다 |
| 광고 진짜 시청 | 광고가 더미다. `roul.ad`는 '더미를 본 횟수'다 |
| 피드백이 온 곳 | 창구가 없다 |

---

## 어디에 쌓나

**지금은 폰 안에만.** 저장본(`user://save.dat`)에 같이 들어간다.
서버가 없으니 남의 것은 못 본다 — **내 것만 본다.**

그래도 지금 심는 값이 있다:

1. 낸 뒤에 서버를 붙일 때 **셀 자리를 새로 만들 필요가 없다.** 보내는 줄만 붙인다
2. **봇으로 미리 채워 볼 수 있다** — 아래
3. 스스로 놀아 보고 "내가 뭘 많이 했나"를 볼 수 있다

## 봇으로 미리 채워 본다

```sh
BAL_HOURS=8 BAL_SEED=1 godot --headless --path godot --script tests/balance.gd
node tools/dash.mjs           # → dash.html
```

가상 플레이어가 여덟 시간을 놀고 나면 그 자국이 그대로 남는다.
**게임을 낸 날 계측이 처음 도는 게 아니라, 이미 한 번 돌려 본 것이 도는 셈이다.**

회사에서도 이렇게 한다 — 출시 전에 QA가 플레이한 자료로 판을 켜 보고,
**숫자가 안 들어오는 칸**을 찾는다. 계측이 빠진 걸 출시 뒤에 아는 게 제일 비싸다.

> ⚠️ **봇은 사람이 아니다.** 봇은 지치지도 심심해하지도 않고, 광고를 끝까지 본다.
> 봇의 자국으로 알 수 있는 것은 **"셈이 제대로 도는가"**까지다.
> *"사람들이 광고를 볼까"*는 사람이 와야 안다.

---

## 지켜야 할 것

**1. 세는 일이 규칙을 바꾸면 안 된다.** `stats`는 오직 더하기만 한다.
아무것도 읽어서 판단하지 않는다. 그래야 계측을 넣거나 빼도 게임이 같은 게임이다.

**2. 사람을 못 알아보게 한다.** 나중에 서버로 보낼 때도 **누가**는 안 보낸다.
기계마다 무작위 번호 하나면 "며칠 만에 다시 왔나"까지 셀 수 있다.
이름·전화·위치는 필요 없고, 필요 없는 것은 안 받는 게 제일 안전하다.

**3. 저장본을 깨뜨리지 않는다.** 세는 칸이 늘어도 옛 저장본은 그 칸이 없다.
없으면 0으로 시작한다 — 그래서 `stats`는 처음부터 빈 사전이다.
EOF
