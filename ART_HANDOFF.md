# 그림 ↔ Godot 인수인계

> Codex 그림 작업 브랜치: `codex/art-production-20260822`
> 시작 기준: Claude 브랜치 `4c22a91`
> 원칙: 아래 `Hypothesis`는 구현 후보이며 승인된 `Decision`이 아니다.

## 현재 그림 상태

- 자동 주문서: 필수 `74장`, 현재 `11장` 들어옴
- 기존 구규격 초안: 점장 2장, 토끼 1장
- 새 2배 규격 초안: 직원 4등급 × 작업/수면 `8장`
- `node tools/art.mjs --write` 실행 완료
- 실제 그림 장부: `docs/art/image-archive.html`

## Discussion — 그림만 늘리면 장수가 폭발하는 지점

사용자 피드백은 세 가지다.

1. 직원 작업은 `왼손 아래/오른손 위 ↔ 왼손 위/오른손 아래`처럼 투닥거리는 동작이 필요하다.
2. 수면 표식은 계속 붙어 있기보다 나타났다 사라지거나 커지며 옅어지는 편이 자연스럽다.
3. 고객의 짐 표현은 나중이어도 되지만, 판매하는 너구리는 실제 물건을 손에 들어야 한다.

캐릭터와 물건을 한 PNG로 합치면 `캐릭터 수 × 물건 40종 × 자세`만큼 그림이 늘어난다.
따라서 전신 그림을 계속 추가하기보다 코드 움직임과 별도 물건 그림을 섞는 쪽을 시험해야 한다.

## Hypothesis A — 직원 작업 2프레임

- 기존 `<등급>-work.png`는 첫 프레임이자 폴백으로 유지한다.
- `<등급>-work2.png`가 있을 때만 두 장을 약 `4~5fps`로 번갈아 쓴다.
- 몸, 얼굴, 꼬리, 발끝은 고정하고 앞발만 `원본 8~12px` 움직인다.
- 파일이 없으면 지금처럼 `work.png` 한 장과 작은 들썩임으로 계속 작동한다.

아직 `work2`는 자동 주문서의 필수 장수에 넣지 않았다. 한 등급으로 화면 시험을 통과한 뒤
4등급 전체에 추가할지 결정한다.

## Hypothesis B — 수면 표식 분리

- 몸은 `<등급>-sleep.png` 한 장과 `1~2px`의 느린 들숨/날숨 움직임만 사용한다.
- 몽글방울은 공용 투명 PNG 한 장을 따로 얹고 `작게 나타남 → 커지며 위로 감 → 옅어짐`을 반복한다.
- 현재 수면 초안에는 몽글방울이 본체에 붙어 있다. 분리 구현이 정해지기 전에는 그대로 둔다.

## Hypothesis C — 판매 물건을 손에 얹기

- 새 캐릭터×물건 합성 PNG를 만들지 않는다.
- 맨 앞 손님이 주문한 첫 품목의 `items/<id>.png`를 작게 줄여 `raccoon-sell`의 빈 양손 사이에 그린다.
- 점장 몸과 물건의 앞뒤가 어색하면 나중에 공용 `앞발 덮개` 한 장만 추가한다.
- 고객이 나갈 때 드는 짐은 이번 구현에서 제외해도 된다.

현재 `godot/view/village.gd`의 `_clerk()`는 `busy`만 알고 품목 ID는 들고 있지 않다.
`line[i]`의 맨 앞 `buy` 손님이 가진 `sold[0].id`를 화면 전용으로 읽으면 경제 규칙을 건드리지 않고
물건을 고를 수 있다. 판매가 끝나면 즉시 숨긴다.

## 충돌 방지 메모

- 이 브랜치에서는 위 세 상호작용의 Godot 코드를 아직 고치지 않았다.
- Claude 브랜치가 `village.gd`를 자주 바꾸므로 그림 묶음과 문서를 먼저 올리고, 구현은 최신 Claude 코드에서 적용하는 편이 안전하다.
- 구현할 때 경제 규칙(`sim`)은 변경하지 않고 `view`에서 이미 있는 판매 정보를 읽기만 한다.

