# 그림 ↔ Godot 인수인계

> Codex 그림 작업 브랜치: `codex/art-production-20260822`
> 최신 확인 Claude 기준: `5dacb82`
> 원칙: `Discussion → Hypothesis → Decision`. 사용자 승인을 받지 않은 그림은 모두 `초안`이다.

## 현재 그림 상태

- 자동 주문서: 필수 `170장`, 현재 `170장` 들어옴
- 선택 작업: 성장 카드 `90장` + 가게별 점장 `20장`
- 선택 초안: 가게별 점장 `20장` 모두 들어옴, 성장 카드 2단 `30장` + 3단 `25장` 들어옴 · 성장 카드 `35장` 남음
- 구규격 초안: 점장 make/sell 2장, 토끼 1장
- 새 규격 초안: 점장 걷기/수면/촌장 4장, 직원 8장, 손님 29장, 뽑기·룰렛 3장, 손님 카드 1단 30장, 상호작용 3장, 물건 80장, 좌판 10장, 선택 전용 점장 20장, 선택 성장 카드 2단 30장 + 3단 25장
- 실제 그림 장부: `docs/art/image-archive.html`

### 최신 개발 확장

- 가게가 5채에서 10채로 늘었다: 국밥집·주막·꼬치집·떡집·푸줏간 추가.
- 필수 물건이 40종에서 80종, 좌판이 5종에서 10종으로 늘었다.
- 기존 점장·직원·손님·카드·움직임 규격은 바뀌지 않아 이미 만든 그림은 그대로 유효하다.
- 필수 170장은 모두 파일이 있는 `초안` 상태다. 실제 Godot 화면 검수와 사용자 승인 전에는 `확정`으로 바꾸지 않는다.

## Discussion — 그림 장수와 움직임의 경계

사용자는 직원의 손이 투닥거리듯 움직이고, 수면 표식이 살아 있으며, 판매하는
너구리가 실제 물건을 드는 모습을 원한다. 하지만 모든 상태를 여러 PNG로 만들거나
캐릭터와 물건을 한 장에 합치면 `캐릭터 수 × 물건 80종 × 자세`로 작업량이 폭발한다.

Claude가 추가한 `MOTION.md`는 그림을 여러 장 쓰는 범위를 점장 걷기 2장과 점장
상태 make/sell/sleep으로 제한한다. 직원·손님·물건·카드는 한 장을 코드가 흔들고
기울이고 키우는 방식이다.

## Decision — 최신 움직임 계약을 따른다

- 직원 `work2`는 만들지 않는다. 기존 `work.png` 한 장을 코드에서 약 3px 들썩인다.
- 직원의 작업 그림은 허리를 숙이지 않은 차렷 몸과 앞에서 모은 빈손 실루엣을 유지한다.
- 직원·점장 수면 그림의 몽글방울은 현재 PNG 안에 둔다. 별도 프레임을 필수로 늘리지 않는다.
- 손님은 오른쪽을 보는 필드 그림 한 장만 만들고, 좌향 전환과 걸음 들썩임은 코드가 맡는다.
- 점장 걷기만 `raccoon-walk1/2` 두 장을 번갈아 쓴다.
- 카드·룰렛 연출은 정지 그림과 코드 애니메이션을 결합한다.

근거: `MOTION.md`의 “그림이 여러 장 필요한 것 — 딱 두 가지”와 사용자 피드백.
이 결정은 그림 제작 계약에 관한 것이며, 현재 초안 이미지의 미감 승인과는 별개다.

## Hypothesis — 판매 물건을 점장 손에 얹기

- 새 캐릭터×물건 합성 PNG를 만들지 않는다.
- 맨 앞 손님이 주문한 첫 품목의 `items/<id>.png`를 작게 줄여 `raccoon-sell`의 빈 양손 사이에 그린다.
- 점장 몸과 물건의 앞뒤가 어색하면 나중에 공용 `앞발 덮개` 한 장만 추가한다.
- 고객이 나갈 때 드는 짐은 이번 구현에서 제외해도 된다.

현재 `godot/view/village.gd`의 `_clerk()`는 판매 상태만 고르고 품목 ID를 직접 받지 않는다.
화면에 이미 있는 맨 앞 구매자의 `sold[0].id`를 읽으면 경제 규칙을 바꾸지 않고 물건을
고를 수 있다. 판매가 끝나면 즉시 숨긴다. 실제 화면 시험 전까지는 `Hypothesis`다.

## Claude 구현 요청 메모

1. `MOTION.md` 계약대로 직원은 추가 프레임 없이 `work.png`를 3px 안팎으로 들썩인다.
2. `raccoon-sell` 상태에서 선두 구매자의 첫 `sold` 품목 이미지를 양손 사이에 오버레이한다.
3. 카드 화면은 필드용 `guests/<id>.png`가 아니라 카드용 `cards/<id>-1.png`를 우선하고,
   없을 때만 필드 그림으로 폴백한다.
4. 성장 카드 `-2/-3/-4`가 없으면 1단을 계속 쓰는 현재 폴백을 유지한다.
5. 새 가게 5곳도 전용 점장이 없으면 공용 점장으로 폴백한다.
6. 카드 뒷면 경로가 `MOTION.md`의 `cards/back.png`와 `tools/art.mjs`의 `ui/back.png`로 갈려 있다.
   현재 그림 브랜치는 자동 주문서가 세는 `godot/art/ui/back.png`를 사용한다. 로더와 문서를 한 경로로 맞춘다.
7. 쥐·까마귀는 오른쪽 진행 그림 한 장을 위치 이동·들썩임·필요시 좌우 반전으로 사용하고,
   삽살개는 정지 그림의 짧은 튕김으로 반응시킨다. 추가 프레임 요구가 생기기 전 이 방식을 먼저 시험한다.
8. `items/<id>.png`는 `128×224` 세로 원본이라 점장 손에서는 별도 표시 비율을 적용한다. 곡괭이로
   먼저 오버레이 크기와 앞뒤 순서를 검증한 뒤 같은 규칙을 나머지 79종에 재사용한다.
9. 가마솥·벼루처럼 가로 물건도 PNG 캔버스 중앙에 놓는다. 판매 오버레이에서는 투명 여백까지
   포함한 원본 크기가 아니라 실제 알파 경계를 기준으로 공통 최대 폭·높이를 계산한다.
10. 내부 ID `floorp`의 한국어 이름은 `장지(壯紙)`이며 바닥재가 아니라 두껍고 질긴 문서용 종이다.
    UI 설명이나 제작 연출에서 장판·바닥 종이로 해석하지 않는다.
11. 국밥처럼 김이 있는 음식도 품목별 추가 프레임을 요구하지 않는다. 현재 김은 한 PNG 안의 정지 표식으로 쓰고,
    실제 화면에서 움직임이 꼭 필요하면 음식 그림을 다시 만들기보다 여러 음식이 공유하는 절제된 절차형 김 효과를 검토한다.
12. 첫 대장간 좌판을 현재 `_stall()` 좌표로 합성한 `docs/art/qa/stall-smith-overlay-test-16.png`에서 가마솥·자물쇠가
    투명 캔버스 여백만큼 상판 위로 뜬다. 좌판 진열에서는 `items/<id>.png`의 실제 알파 경계 아랫변을 공통 상판 기준선에
    맞추는 전용 그리기 함수를 쓰고, 점장 손 판매 오버레이에서는 같은 경계를 양손 중심에 맞추는 별도 정렬값을 쓴다.
    그림 파일을 품목별로 다시 밀어 넣으면 두 사용처 중 하나가 다시 어긋나므로 원본 캔버스 위치로 해결하지 않는다.
13. `tools/art.mjs`의 실제 자동 목록은 가게 10곳·물건 80종·좌판 10종·전용 점장 20장을 세지만,
    `ASSETS.md`의 자동 목록 바깥 설명에는 아직 `물건 40종`, `좌판 5종`, `가게별 점장 5가게 × 2포즈`가 남아 있다.
    다음 기능 문서 갱신 때 정적 설명도 실제 목록과 맞춘다. 그림 브랜치에서는 자동 목록만 재생성하고 이 설명은 충돌 방지를 위해 건드리지 않았다.
14. 가게별 점장 20장은 `make/sell`만 전용 그림이다. 점장이 계산대와 작업대를 오갈 때 전용 점장의 몸이 갑자기 공용 앞치마로 바뀌지 않게 하려면,
    전용 `walk/sleep` 30장을 더 요구하기보다 가게별 앞치마를 코드에서 공용 걷기·수면 위에 덧씌울 수 있는지 먼저 시험한다. 어렵다면 현재 공용 폴백을 유지하고 화면 전환 때의 이질감을 QA 메모로 남긴다.
15. 성장 카드 그림 단계는 단골 성에 따라 `1~5성 → -1`, `6~10성 → -2`, `11~15성 → -3`, `16~20성 → -4`로 바뀐다.
    현재 `panel.gd`의 손님 목록은 텍스트만 그리고 `Art.ranked()`는 없는 단계에서 접미사 없는 파일로 폴백하므로 카드 규칙에 그대로 쓰기 어렵다.
    카드 화면/도감에 전용 로더를 두어 현재 성 구간 파일을 우선하고, 없으면 가장 가까운 낮은 단계(최종적으로 `-1`)로 폴백한다. 뽑기 공개 직후 어느 성의 그림을 보일지는 `star_up` 반영 뒤 현재 성을 기준으로 한다.

## Hypothesis — 성장 카드 3단 시각 언어

- 같은 개체·자세·계절과 2단의 수선된 장소를 유지한다.
- `익숙한 단골 → 장날의 오래된 벗`으로, 열린 문·낮은 좌판·빛바랜 삼베 그늘막·준비된 광주리와 포장 물품만 늘린다.
- 인물·추가 동물·간판·깃발·의상·마법 효과는 넣지 않는다.
- 첫 다섯 장은 이 가정이 작은 카드 프레임에서도 동물을 묻지 않는지 시험하는 초안이다.

## 충돌 방지 메모

- 이 브랜치에서는 `village.gd`, `card.gd`, 경제 규칙을 수정하지 않았다.
- Claude 브랜치가 화면 코드를 계속 바꾸므로, 그림과 문서만 먼저 올리고 위 요청은 최신
  Claude 코드에서 적용하는 편이 안전하다.
- 자동 생성 구간인 `ASSETS.md`는 수동 병합하지 않고 항상 최신 `tools/art.mjs --write`로 재생성한다.
