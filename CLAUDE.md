# 너구리 만물상 — 작업 규칙

이 파일은 **Claude Code가 이 폴더를 열 때 자동으로 읽는다.** 새 자리에서
시작해도 여기까지 온 맥락과 약속이 이어지도록 적어 둔다.

## 만드는 사람

전문용어를 모른다. **모든 설명은 쉬운 한국어로, 설득하듯이.**
영어 약자·전문용어를 쓰지 말고, 꼭 써야 하면 그 자리에서 풀어서 말한다.
"이게 왜 필요한지"를 먼저 말하고 "무엇을 했는지"를 나중에 말한다.

## 지금 어디까지 왔나

- `index.html` · `sim.js` · `content.js` · `iso.js` — **원래 판(자바스크립트)**.
  아직 돌아가고, **이관의 답안지 역할**을 한다. 함부로 지우지 말 것.
- `godot/` — **이제 여기가 본판이다(Godot 4.7.2)**. 자바스크립트판은 답안지로 굳었고,
  2026-08-22에 **그 답안지도 갈라졌다**(아래 참고).
  가게 10채 · 손님 30종 · 뽑기 · 일꾼 카드 · 저장/오프라인 · 장날 소식.
  남은 것: 소리 파일, 그림 125장, 가게 11~20채, 의뢰 고쳐 만들기.
- `STORY.md` — **앞문. 무엇을 만드는가.** 핵심 재미 한 문장, 결(동화·힐링),
  그리고 새 기능을 재는 자("이게 손님이 돌아오는 걸 느끼게 하나?").
  **뭘 넣을지 헷갈리면 여기부터 읽는다.**
- `PLAN.md` — **하루하루의 결정과 실패가 전부 적혀 있다.** 뭘 왜 그렇게 했는지
  모르겠으면 여기부터 읽는다. 길지만 이 프로젝트의 기억이다.
- `PROCESS.md` — 제작 회고와 다음 순서. 맨 뒤에 **네 갈래 지도**(기획·개발·
  디자인·운영)가 있다 — *"지금 뭘 할 차례지?"*는 거기부터 본다.
- `SYSTEMS.md` — **기능 지도.** *"이걸 고치면 어디가 흔들리나."*
  숫자를 만지기 전에 여기서 파급을 먼저 본다.
- `FLOW.md` — **흐름과 꼬인 곳.** 무엇이 무엇과 이어지는지, 어디가 끊겼는지를
  그림으로 본다. 기능을 더 넣기 전에 여기부터 맞춘다.
- `METRICS.md` — **무엇을 세는가.** 지표는 거슬러 올라가 모을 수 없다 —
  사람이 오기 전에 심는 이유와 목록.
- `MOTION.md` — **움직임과 손맛.** 무엇이 어떻게 움직여야 하는지.
  그림을 그리는 쪽이 "몇 장으로 나눠야 하나"를 물으면 여기를 본다.
- `RACCOON.md` — **너구리 지침.** 누가 몇 마리, 무엇을 하고, 그림은 몇 장인가.
  그림 그리는 쪽이 "이 자세가 왜 필요하냐"를 물으면 여기다.
- `REFS.md` — **다른 게임에서 본 것.** 남이 어떻게 풀었는지와, 우리 자로 다시 잰 결과.
  *"저 게임이 하니까 우리도"는 여기서 금지한다.*
- `PLANNING.md` — **기획서(자동 생성).** 시스템·밸런스·콘텐츠·시나리오·UX·사업
  여섯 갈래로 갈라, 갈래마다 *정한 것*과 **아직 안 정한 것**을 나란히 놓는다.
  *"기획이 지금 어디까지 됐지?"*는 여기부터. 고치려면 `tools/plandoc.mjs`의 `PARTS`.
- `DECISIONS.md` — **누가 정했나.** 유저가 정한 것 · 내가 판단한 것 · 숫자가
  정한 것을 갈라 적는다. *"내 추론까지 같이 승인해 버린다"*를 막는 장치다.
- `NAMES.md` — **이름과 뜻.** 같은 것을 다른 말로, 다른 것을 같은 말로 부르면
  거기서부터 대화가 어긋난다. 새 이름을 만들기 전에 여기부터 본다.
- `ASSETS.md` — 필요한 그림 목록(자동 생성).
- `BESTIARY.md` — **동물 도감**(자동 생성). 손님 서른의 성격·생김새·버릇.
  그림 그리는 쪽이 "이 짐승이 누구냐"를 물으면 여기다. 고치려면 content.js의 `look`.

## 반드시 지키는 것

**1. 짐작하지 말고 잰다. 그리고 한 판으로 판단하지 않는다.**
"이렇게 하면 나아질 것이다"는 이 프로젝트에서 여러 번 틀렸다.
숫자를 바꾸면 재되, **반드시 운 번호를 여럿 써서 가운뎃값을 본다.**

이 게임은 운 번호만 바꿔도 8시간 매출이 0.85T ~ 1.25T로 흔들린다 —
**재려는 효과보다 판마다의 운이 더 크다.** 운 번호 하나로 재면 손해인 강화가
+32% 이득으로 보인다. 실제로 그렇게 속았다(PLAN.md 2026-08-21).

**2. 고친 것은 대조한다.** `tools/crosscheck.sh` — **열한 가지**를 본다.
규칙을 고쳤으면 **반드시 돌리고 초록불을 확인**한다.

- `fmt` · `rng` · `content` — 아직 **자바스크립트 답안지**와 댄다(순수한 함수와 데이터다)
- `sim` — 2026-08-22부터 **어제의 Godot과 오늘의 Godot**을 댄다.
  뽑기가 들어오면서 두 판이 다른 게임이 됐다. 일부러 규칙을 고쳤으면
  `GOLDEN=write tools/crosscheck.sh sim`으로 정답을 다시 박고 **그 차이를 커밋에 남긴다**
- `tap` · `file` · `bits` · `save` · `gacha` — Godot 안에서 스스로 증명한다

소수를 글자로 찍어 비교하지 않는다(JS와 C는 반올림이 다르다). 정수로 비교한다.
**2^53(9천조)보다 큰 수는 글자로 오갈 때 한 비트가 달라진다** — 그 위를 시험할
때는 2의 거듭제곱으로만 한다(`tools/answers.mjs`의 fmt 주석 참고).

**3. 도구가 안 써 보는 기능은 도구에게 없는 것이다. 뺄 때도 마찬가지다.**
이 프로젝트에서 여섯 번 당했다 — 장을 한 번도 안 눌러본 밸런스 도구,
판매를 화면에 안 넘긴 빨리 감기, 열두 개씩 경고를 뱉던 정체 판정,
난수만 뿌려서 한 번은 통과하던 반올림 시험, 이야기 쪽지에 가려 한가운데가
안 보이던 화면 찍기, 없어진 룰렛을 부르다 통째로 멈춘 밸런스 도구.
새 기능을 넣으면 **도구도 그 기능을 쓰게** 고친다.

★ **뺄 때가 더 위험하다**(2026-08-29, 여섯 번째). 룰렛을 뺐는데 밸런스
도구가 아직 부르고 있었다 — GDScript는 없는 함수를 만나면 거기서 봇이 통째로
멈춘다. 뽑기도 승급도 채용도 안 하고 여덟 시간을 돌아, **네 배 넘게 틀린
숫자를 하루 반 동안 봤다.** crosscheck는 balance를 안 돌리니 아무도 몰랐다.
**기능을 빼면 `tools/` 아래를 grep해서 그 이름이 남았나 본다.**

**4. 파는 물건이 사는 사람을 손해 보게 만들면 안 된다.**
일꾼 상한을 4명에서 1명으로 내린 것, 젬 강화를 장(場)에서 생산 쪽으로 옮긴 것이
전부 이 규칙 때문이다. 새 강화를 넣으면 **끄고 켜서 재본다.**

**5. 저장본을 깨뜨리지 않는다 — 단, 낸 날부터.**
게임을 낸 뒤에도 규칙은 계속 고친다. 그런데 이미 놀고 있는 사람의 저장본은
옛 모양이다. **담는 칸을 바꾸면 `Sim.SAVE_VER`를 올리고 옛 판을 받아주는
코드를 남긴다.** 이건 서버 없는 게임의 '운영/테스트 분리'에 해당한다 —
우리에겐 서버가 없으니 지켜야 할 것은 남의 저장본뿐이다.

★ **그 '남'이 아직 없다**(2026-08-29, 유저: *"현재 운영 중인 부분도 아니니
v2로 바뀔 수도 있다 봐"*). 스토어에 낸 적이 없는데 2판부터 15판까지 받아주는
코드 136줄을 짊어지고 있었고, 규칙을 고칠 때마다 그것도 같이 손봐야 했다.
**v2에서 호환을 끊고 `SAVE_VER`를 1로 되돌렸다** — 모르는 판을 만나면 안 읽고
새로 시작한다. **낼 때가 되면 그날부터 이 규칙을 다시 켠다.**

**6. 유저의 결정과 나의 판단을 갈라 적는다.**
유저 말: *"넌 실무고 난 승인자라고 했을 때 가끔 너의 추론도 함께 승인해버리는
거 같아."* 맞는 말이었고 증거도 있었다 — `sim.gd`의 작업대 설명에 `(유저)`를
붙여 놓고 그 아래 세 가지 이유를 적었는데, **셋 중 둘은 내 판단이었다.**

그래서 말할 때도 적을 때도 셋을 갈라 놓는다:

- 🟦 **유저가 정한 것** — `(유저)`라 적으려면 **옮겨 적을 유저의 문장**이 있어야
  한다. 없으면 유저가 아니다. "유저 방향에 맞다"는 유저가 정한 것이 아니다
- 🟨 **내가 판단한 것** — 유저가 따로 승인한 적이 없다고 **먼저** 밝힌다
- 🟩 **숫자가 정한 것** — **운 번호 몇 개로 쟀는지**를 같이 적는다(규칙 1)

이 세 표는 **유저가 노란 것만 골라 읽으면 "내가 멋대로 정한 것"이 다 보이게**
하려고 쓴다. 유저는 *"그 노란 거 아니야"* 한 마디로 물릴 수 있다.

되돌리기 어려운 🟨는 **일을 시작하기 전에** 따로 묻는다. 다 만들어 놓고
"이렇게 했습니다"라고 하면 유저는 결과를 보고 승인하게 되는데, 그건
판단을 승인한 게 아니다.

**결정은 `DECISIONS.md`에 한 줄씩 남긴다.** 되돌릴 수 있는지도 같이 적는다.

**7. 화면은 sim을 읽기만 한다.** `godot/view/`와 `iso.js`에는 경제 계산이 한 줄도 없다.

## 자주 쓰는 명령

```sh
tools/crosscheck.sh              # JS판 ↔ Godot판 대조 (tap·fmt·rng·content·sim)
SIM_HOURS=4 tools/crosscheck.sh sim   # 길게 돌려 어긋남이 쌓이는지
BAL_HOURS=8 BAL_SEED=1 godot --headless --path godot --script tests/balance.gd
                                 # 8시간 밸런스 — **이제 이쪽이 본판이다**
node tools/balance.mjs 8 1       # 옛 자바스크립트판 측정(답안지 쪽, 참고용)
node tools/gen-content.mjs       # content.js → godot/rules/content.gd 다시 뽑기
node tools/guests.mjs 6          # 손님 등장 문턱을 목표 시각에 수렴시킨다
tools/shots.sh                   # **게임의 모든 화면을 한 번에 찍는다** → shots/
godot --headless --path godot --script tests/artfit.gd
                                 # 그림 발끝·가운데 검사 — 새 그림이 오면 돌린다
godot --headless --path godot --script tests/weigh.gd
                                 # 마을이 커지면 무거워지나 — 그리는 개수·저장본·틱
node tools/dash.mjs              # 봇이 남긴 자국(godot/stats.json) → dash.html
node tools/art.mjs --write       # ASSETS.md 그림 목록 갱신
node tools/bestiary.mjs          # BESTIARY.md 동물 도감 갱신
node tools/plandoc.mjs           # PLANNING.md 기획서 갱신 (--html 은 장부용)
node tools/bundle.mjs            # 자바스크립트판을 nogur.html 한 장으로
```

## 맥에서 Godot을 명령줄로 쓰려면

Godot을 응용 프로그램에 넣었다면 실행 파일은 앱 **안에** 있다.
한 번만 이어 두면 `godot`으로 부를 수 있다:

```sh
sudo ln -sf /Applications/Godot.app/Contents/MacOS/Godot /usr/local/bin/godot
godot --headless --version        # 4.7.2.stable 이 나오면 된 것
```

이게 있어야 `tools/crosscheck.sh`가 돈다.

## 두 자리에서 번갈아 일한다 (침대에서는 클라우드, 책상에서는 로컬)

**이 저장소는 두 곳에서 열린다** — 클라우드 상자와 유저의 맥. 같은 브랜치를
쓰므로 규칙 하나만 지키면 안 엉킨다.

**끝낼 때는 반드시 올린다(push).** 클라우드든 로컬이든, 일을 마치면 커밋하고
민다. 안 민 채로 다른 자리에서 이어 하면 두 갈래가 생기고, 그걸 합치는 건
유저가 감당할 일이 아니다.

**시작할 때는 받는다(pull).** 이건 자동이다 — `.claude/settings.json`에
세션이 열릴 때 `git pull --ff-only`를 도는 갈고리(hook)를 걸어 뒀다.

- 이미 최신이면 아무 말도 안 한다
- 받아왔으면 한 줄로 알린다 (Godot은 켜 둔 채로도 바뀐 파일을 다시 읽는다)
- **손으로 고친 게 있으면 건드리지 않고 건너뛴다.** `--ff-only`라 합치지도
  덮어쓰지도 않는다 — 잃을 일이 없다

건너뛰었다고 나오면 그 자리에서 커밋하고 민 다음 다시 받으면 된다.

## 그림은 다른 데서 온다 — 깃이 안 엉키게

그림(GPT가 만든 것)과 코드(여기서 고치는 것)는 **서로 다른 파일**만 만진다.
그래서 부딪힐 일이 거의 없다. 규칙 셋만 지키면 된다.

**1. 그림은 `godot/art/` 안에만 넣는다.** 코드는 그 폴더의 파일 내용을
절대 안 고친다 — 목록(`ASSETS.md`)만 자동으로 다시 쓴다.

**2. `ASSETS.md`는 손으로 고치지 않는다.** `node tools/art.mjs --write`가
폴더를 훑어서 다시 쓴다. 손으로 고치면 그 다음 실행에 지워진다.
(위쪽 설명 칸은 손으로 써도 된다 — 도구는 `<!-- 목록시작 -->` 아래만 건드린다.)

**3. 그림을 넣은 뒤에는 `godot --headless --path godot --import` 한 번.**
`.import` 짝 파일이 같이 생기는데, **그것도 같이 커밋한다.** 안 그러면
다른 자리에서 받았을 때 그림이 안 보인다.

부딪히면(같은 파일을 양쪽에서 고쳤으면) 억지로 합치지 말고 **어느 쪽이
맞는지 물어본다.** 그림 파일은 합칠 수 있는 물건이 아니다.

## 커밋

한국어로, **무엇을 했는지가 아니라 왜 그랬는지**를 적는다.
실패한 시도와 그걸로 알게 된 것도 함께 적는다 — 그게 나중에 제일 값지다.
